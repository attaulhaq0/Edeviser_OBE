-- NO-OP (reconciled). Real, correctly-ordered DDL lives in
-- 20260821000003_add_parent_assignments_read_rls.sql (and the recursion-safe
-- final form in 20260821000004). Version row exists in production history; kept
-- empty to align local set with recorded history. See
-- migration-history-reconciliation steering.
SELECT 1;
