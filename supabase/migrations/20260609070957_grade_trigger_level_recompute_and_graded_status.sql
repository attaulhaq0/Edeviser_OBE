-- NO-OP (reconciled). Real, correctly-ordered DDL lives in
-- 20260821000001_grade_trigger_level_recompute_and_graded_status.sql.
-- This version row exists in production history; kept empty to align the local
-- set with recorded history without re-running. See
-- migration-history-reconciliation steering.
SELECT 1;
