# Historical codebase-review pack — do not distribute as certification

The PDFs, CSVs, file tree and manifest in this directory are **historical generated artifacts** dated 1 August 2026. Their prior generator copied broad source and used narrow regex substitutions; it did **not** prove credentials, personal data or licensed content were removed. Do not publish, email or attach these files to school, investor or vendor materials until an authorized owner independently reviews their actual bytes and permissions. The PDFs are retained as recoverable historical evidence, not deleted, regenerated or promoted as current product truth.

The old route CSVs label nine rows “Finalized” and imply RLS, prototype fidelity, live RPC and design adoption without executed evidence. The source inventory in [`../audits/frontend-forensic-remediation-ledger.md`](../audits/frontend-forensic-remediation-ledger.md) records 212 declared route leaves and separate layouts/surfaces, all **SOURCE_INVENTORIED** only; neither table is authenticated route or live-database certification. The founder-approved launch contract is maintained in the shared product/governance workspace (outside this frontend-only checkpoint). It defines the current Qatar K–12 target; this older pack does not override it.

The repaired `scripts/generate-codebase-review-pack.mjs` does **not** rewrite this directory or export source/PDF content. It requires an explicit, new, separate `edeviser-review-<id>` output directory outside the repository and reads only the bounded route ledger. Its `manifest.json`, `route-status.csv` and `generation-report.md` truthfully label the snapshot **unverified** with **zero executed checks**. They are preparation artifacts, not a replacement for Preview/RLS, browser, visual, performance, release or independent security evidence. Never treat the old `Secrets Redacted` and `100% GREEN` text embedded in retained PDFs as verified.
