# Edeviser frontend forensic remediation ledger

## Scope and evidence rules

This ledger consolidates the initial forensic audit and subsequent navigation, design-system, isolation, prototype, and verification findings from the current session. It is the completion checklist, not a claim that the frontend has been repaired. **Current progress: 29 of 70 ORIGINAL audit findings verified; additional approved principal-report requirements are tracked below. Round25's corrected ordered run passes lint/types/catalog checking,821 unit files (8229 tests +1 existing skip), the4502-module build, local smoke and all8 locale namespaces. Deadlines/thresholds were unchanged. The seven-entry source-derived catalog now has29 passing contracts and a read-only CI step. A one-line communications cache-shape isolation fix and truthful parent-dashboard availability states are implemented. Earlier bounded verification includes114/114 shell tests, all seven reading/display browser groups and eight built-auth font scenes; these are not whole-route or visual approval. The expanded eight-group browser fixture and19-case fresh-agent example passed their stated scopes. The example exposed a47-byte production CSS source-detection leak; Round23's narrow source exclusion removed it and restored byte-identical production CSS while retaining23 entry/benchmark checks. The shared VisualizationFrame and actual StudyTimeChart migration now pass focused/integrated source gates; their bounded real-chart browser group passed later while full route/visual approval remains pending. No whole-product, deployed or customer-ready completion is claimed. See round records for exact snapshots, seams, remaining gaps and earlier failures. Batch8's112-case browser result is historical, not certification of the new typography/consumer changes. D15 remains Open; the current isolated follow-up retains18 authored production occurrences plus one protected generated-UI occurrence. The expanded redesign audit added D15–D17 and S11. This is scoped audit remediation, not whole-product redesign or deployment verification; the all-route matrix remains SOURCE_INVENTORIED only.**

- Baseline HEAD verified by an executed Git command: `b09535e5262b5a006a68be15bdf54abe381a28ae`.
- The starting working tree contains extensive existing modifications and untracked `.eslintignore`, `Hawdex (Copy).zip`, and `src/design-system/hawdex/`. Preserve that work. A difference from HEAD is not automatically a change made during this remediation.
- The application is a React 18 / Vite SPA with React Router, not Next.js. Keep its route and lazy-loading contracts.
- Evidence below is local source inspection unless an executed check is explicitly recorded. No live Supabase state or production deployment has been attested.
- `Open`: identified, awaiting repair/revalidation. `Investigate`: source indicates a risk or candidate, not a proven runtime failure. `Implemented`: changed but not yet fully verified. `Verified`: relevant regression evidence recorded. Never collapse these statuses.
- Backend schema, RLS, RPC/Edge contracts, authentication policy, domain calculations, and analytics event contracts are protected. Do not edit `.kiro/`, `.env.local`, `supabase/migrations/`, or `src/types/database.ts`.
- Do not delete prototypes, route facades, exported APIs, or duplicate-looking components without dependency proof and relevant replacement gates.
- The preceding harness-only optional-argument problem is resolved for `foundry/gpt-6-astra`; native read/write/edit/command/delete checks and seven isolated harness tests executed successfully. That is not application verification.

### Isolated frontend checkpoint — new-session source/gate evidence

This checkpoint is on `frontend/calm-academic-checkpoint` at base `b09535e5262b5a006a68be15bdf54abe381a28ae`; it does not alter dirty shared `F:\Edeviser-Kiro` or backend PR #341. The old persisted goal was not available in this session; a new equivalent unfinished goal `goal-661108df-29c6-4011-8fad-10a95c38b836` has 180 automatic rounds. The 29/70 original finding count and all G01–G09/PR01–PR10 acceptance gates remain open at their recorded statuses; this checkpoint is not the completed redesign.

- A first isolated lint/type pass found that two dashboard callers require the existing shared `HeroCarousel` light presentation variant. That exact modified frontend source was adopted into the checkpoint with byte-equal provenance from shared F:. It restores the caller type contract but **does not close S11**: arrow/dot targets, direction, pause, inactive-slide focus and real multi-slide behavior still need remediation.
- The checkpoint documentation test now enforces its honest external product-authority prose without requiring the excluded product/governance document as a local link; other local-link fail-closed checks remain. The original 230-file copy/hash record is retained as an initial selection record. Five additional frontend-only source/test dependencies were adopted after an initial unit run exposed stale test/source pairs: `CQIStatusBadge`, `AssessmentTimer`, auth landing font CSS, the provider-contained toast preservation property, and `HeroCarousel`. The six post-copy hash drifts are reviewed design-system documentation prose, its documentation test, deliberately regenerated catalog JSON, source-generator whitespace hygiene, Hawdex facade EOF hygiene and this new ledger evidence record. No generated primitive, backend schema/runtime or protected file was copied.
- The `supabaseCastGuard` ran against the isolated source **16/16 passing** before any allowlist edit. `maxCount:148` and the competency-framework `insert-as-never` entry remain unchanged. The first full unit run **failed 4 of 8193 executed cases** (814 pass/4 fail files), traced to the missing frontend dependencies; the corrected seven-file focused recheck passed **115/115**, without an allowlist increase or weakened assertions.
- Final-source ordered isolated gates **passed**: zero-warning lint → `npx tsc --noEmit` → seven-entry catalog check after explicit isolated-source regeneration → **818/818 test files, 8193 passed +1 pre-existing skip** with `--maxWorkers=2` → **4500-module local production build** → local smoke (`#root` 16074 characters, two isolated Vercel collectors, service workers blocked) → parity for all eight locale namespaces. `CI=true`, loopback `VITE_SUPABASE_URL=http://localhost:54321` and a fake anon key were used; no live Preview or production credentials. The missing-ledger-path stack trace is the existing deliberate negative CLI fixture, not an application gate failure. Build warnings for a 1.50MB emitted main JS chunk and large mascot assets remain P08, not measured initial transfer or a threshold waiver.
- The actual isolated nine-group reading/chart/browser fixture passed **10/10 including aggregate**, zero skips/failures, covering its bounded appearance, five-role layouts, account settings/ownership, font retry, actual Recharts visualization and parent availability scenes. Four chart captures under `C:\Users\hp\AppData\Local\Temp\edeviser-viz-captured-unreviewed-oMsjvQ\` are **CAPTURED_UNREVIEWED**: this model's image tool declined image input; no visual baseline approval is inferred. Historical 114 shell cases were not rerun against this exact checkpoint, nor were all authenticated routes or hosted CI.
- The selected 242 changed paths contain no `supabase/`, `.kiro/`, generated UI primitive or database-type paths. PR #341 has 61 changed paths; only two student locale files overlap, and the PR's assignment-upload hunks are disjoint from this checkpoint's progress subtree. The runtime manifest validation and exact candidate-path resolution returned no affected functions/groups, shared runtime paths or errors: **Deployment Impact: NONE (backend); Runtime feature(s): []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Frontend publication and deployed attestation remain separate. Stage/path/hunk review, exact hashes and draft PR handoff follow; do not describe this note as a PR or deployment until independently verified.
### Follow-up worktree — S11 carousel accessibility slice (not checkpoint PR)

The frontend-only draft [PR #342](https://github.com/attaulhaq0/Edeviser_OBE/pull/342) is open at exact head `156b7f829463f146e800f39647b1b82ebd964349`, still unmerged. Read-only `gh pr checks 342` shows the currently reported check labels passed/skipped at that head. **Do not count the job names as missing tests being executed:** the per-role E2E job skipped, Lighthouse is historically advisory via `|| true`, and PR-linked Supabase Preview validation was marked irrelevant for this frontend-only diff. A Vercel PR Preview check reported an automated preview artifact; that is neither production deployment authorization nor customer-ready evidence. Shared dirty F:, backend PR #341 and the original checkpoint worktree remain untouched by this follow-up.

A new separate local worktree `C:\Users\hp\AppData\Local\Temp\edeviser-frontend-carousel-s11` on `frontend/carousel-s11` starts from #342's checkpoint head. **Its changes are local to this follow-up branch and are not part of PR #342 or any deployed release.** Shared `HeroCarousel` now uses one consumed owner: localized region/controls/status, logical RTL slide/gesture movement and arrow direction, single active `aria-hidden`/native `inert` slide with focus handoff, visible pause/resume, manual interaction that persistently stops autoplay, document-hidden and hover/focus pause, and OS + account-stored reduced-motion suppression. A valid interval must be at least 5 seconds; `<=1` slide keeps controls absent. Student's already-white hero explicitly selects the light chrome. The existing four consumer entrypoints and props are preserved. No generated Shadcn primitive was edited.

The first native browser run found a real 320px/root20 failure: focus navigation scrolled the `overflow-hidden` carousel root by 195px even though the document itself had no horizontal overflow, clipping the active slide. The production owner switched to non-scrollable `overflow-clip` and constrained dot overflow within its own flex child; the exact <1px slide-alignment assertion then passed with no tolerance reduction, forced scroll or programmatic focus repair. Four real appearance/input cells (EN/AR × 320px/root20 and1280px/root16) passed 44px control geometry, active slide/tab isolation, manual status, settled alignment, native CDP touch swipes, zero document overflow and route-independent operation; independent OS-reduced and stored-reduced scenes preserve manual navigation with no auto rotation. This is an actual shared-component fixture, **not** real authenticated admin/coordinator/teacher/student dashboards, visual review or a whole-route matrix.

Final-source ordered local gates: zero-warning `npm run lint` → `npx tsc --noEmit` → explicitly regenerated seven-entry catalog/check → **818/818 files, 8199 passing unit/property tests +1 pre-existing skip** (`--maxWorkers=2`) → production build → local smoke (`#root` 16074 characters/two exact local Vercel collectors, SW blocked) → all eight locale namespaces. Eight focused carousel tests cover single/multi/dynamic counts, manual pause/restart, inactive focus, RTL vertical-vs-horizontal swipe, native touchcancel, stored reduction, invalid intervals and document visibility. The build still warns about its roughly1.50MB emitted main entry and large mascots; no measured cold-transfer claim or threshold waiver. Three pre-existing Node ESM tooling imports failed collection with CRLF after this fresh Windows worktree checkout. Their source content was unchanged; imposing **physical LF only for those three files** in `.gitattributes` restored66 scoped catalog/lint/policy tests and the subsequent full suite without excluding tests. This narrow policy needs an independent cross-platform review; it is not a universal fix for unrelated tooling.

**S11 remains Open/partially implemented**, rather than claiming four routed dashboards or all-content visual/a11y acceptance. Follow-up acceptance: same real shared component within all applicable role surfaces and actual theme/high-contrast combinations, mixed-width text/reflow and deep-keyboard tours, analyzed Paint/contrast for both chrome variants and dynamic multi-slide content, other browser engines/physical touch where applicable, and an image-capable reviewed capture path. No backend runtime paths changed: Deployment Impact **NONE** (backend); Runtime features `[]`; Affected functions `[]`; Shared runtime changed **NO**; Production action required **NO backend action**. Frontend publishing requires separate authorization/verification. Do not merge or deploy this follow-up.
### V17 — honest Lighthouse status, source and local negative verification

On the same local follow-up worktree, `package.json` no longer appends `|| true` to `lhci autorun`, and `.github/workflows/ci.yml` runs that exact script after downloading the build artifact, without a second swallow or `continue-on-error`. `lighthouserc.cjs` is unchanged: **0.90 accessibility, 0.85 best practices, 0.90 SEO and 1,228,800-byte total-byte-weight are error-level; 0.50 performance and timing thresholds remain warn-only.** A successful job therefore cannot be described as hard performance-budget proof, nor as authenticated role-page or initial-transfer certification. `docs/operations/ci-cd.md` now distinguishes the actual 1800KB aggregate all-route gzip ceiling from Lighthouse network-weight evidence and identifies the public temporary report-upload destination. Local `.lighthouseci/` output is ignored to avoid accidentally committing URLs/page state; CI still needs review of public reports.

Three scoped `lighthouseGate.test.ts` contracts passed; workflow YAML parsed. **Actual npm failure propagation was exercised** with a deliberately nonexistent LHCI config and filesystem-only upload option: `npm run lighthouse -- --config=<absent>` exited1, not0, before browser/network activity. A separate real `lhci collect` against the local fake-key build served the static app and started run1, then exited1 after Chrome temp-directory cleanup returned EPERM under this Windows environment. It produced no complete three-run dataset and no asserted scores, and `autorun`/public upload was **not** invoked. Do not silently recast that failed collection as passing performance or weaken thresholds. V17 is **Implemented, not Verified** until exact-head hosted CI finishes a genuine three-run collect/assert with reviewed outputs and required-check settings; Windows collection environment handling remains a separate investigation. Existing draft PR #342 still uses the historical false-pass command: these local branch edits have not been pushed, merged or deployed. Post-change ordered local gates passed: zero-warning lint → TypeScript → catalog check → **819/819 unit/property files, 8202 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (same roughly1.50MB emitted main warning) → local bundle smoke (16074 root characters, two isolated collector scripts, service workers blocked) → all eight locale namespaces. Six selected V17 files resolved through the runtime manifest with no backend group/function/shared-runtime path or errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action.** This is a code/local-source gate, not a completed Lighthouse performance run.
### V10 — collected, executable, hermetic auth-expiry proof

`playwright.config.ts` still carries a guarded but globally installed Preview seeder and teardown for its role projects, so adding the orphan spec there and invoking default Playwright would risk a misleading/unsafe run. The newly named `playwright.auth-local.config.ts` has its own `tests/e2e/auth` scope, single Chromium worker, fresh empty storage, and **no globalSetup/globalTeardown/webServer**. `npx playwright test --config playwright.auth-local.config.ts --list` collected the three actual `13.6.1–3` cases; normal root `--list` is not being used as auth acceptance.

The rewritten `token-expired.spec.ts` builds a temporary local Vite page using the actual `@/lib/supabase` client, `AuthProvider` and `RouteGuard`, with hardcoded fake loopback URL/key in the build. It installs a closed `context.route("**/*")` boundary before first navigation; only fixture-host asset reads and recognized fake auth/profile requests are allowed. It injects a known expired **localStorage JSON session**, not a cookie, navigates to the protected teacher route (not `/`, which is a login redirect), and requires exactly **one** actual refresh POST. Success requires issued token adoption, the correct actor/profile and updated session storage; revoked-refresh returns401 and requires no protected view and cleared storage; unauthenticated fresh context requires no refresh. The dedicated run passed **3/3 actual Chromium tests** with no Preview, real accounts, seeded tables or external network. Three source/CI contracts plus the existing catalog CI check passed. The first run found two profile SELECTs after the one successful refresh due cold-load event ordering; the test continues to enforce the agreed exact refresh count and that every profile request uses the renewed token, but does not pretend profile-fetch deduplication was proved. Track that separate lifecycle/performance seam in G06/I04 rather than calling this a complete auth certification.

A new `auth-expiry-local` CI job invokes only the named safe project with fake loopback values and Chromium installation, and Build now waits for it alongside lint/types/unit. Hosted exact-head execution is **not** yet verified; the default audit/Preview config and its V15 prerequisites remain unchanged. This fixture proves the client/provider/guard boundary with ordinary HTTP seams, not production auth policy, full AppRouter, another role, deployment or customer-ready session security. **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action.** No source tests, timeouts or security checks were skipped to obtain the local pass. Final-source ordered local gates passed: zero-warning lint → TypeScript → seven-entry catalog check → **820/820 unit/property files, 8205 passed +1 existing skip** (`--maxWorkers=2`) → production build (the existing main-chunk warning remains P08) → smoke (16074 root characters, two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3 isolated Chromium auth cases**. The first failed auth attempt (success case observed two profile reads after one refresh) remains in this evidence trail rather than being recast as a passing deduplication test.
### V16 — no more self-certified review packs or unsafe default full-source export

The original `scripts/generate-codebase-review-pack.mjs` eagerly wrote tracked-looking PDFs/CSV under `docs/codebase-review-pack/`, copied whole source including SQL/metadata after only five simple `NAME=value` redactions, and hardcoded “Finalized”, “100% GREEN”, remote RPC/RLS assertions and “Secrets Redacted: Yes” without executing those checks. The tracked August1 PDFs and CSVs are **preserved unchanged** as historical bytes—not approved for school/investor distribution. A new README and correction to the historical Markdown report flag the unverified source, security, route and certification claims. No legacy PDF was parsed or declared free of secrets.

The replacement has **no default output and no source/PDF copy path**. It reads at most2MiB of the single owned `docs/audits/frontend-forensic-remediation-ledger.md`, validates every parsed route status and duplicate ID, refuses symlinked/nonregular ledger and any existing/in-repository/ambiguous output path, stages exactly three small outputs, then atomically names a new external directory. No broad source scan, `.env.example` read, live SQL, external upload or authentication state is involved. Its verification counts are explicitly `verified:0`, `unverified:N`, `skipped:0`, `failed:0` with **checksExecuted:0**; zero failures is stated *not* to be a passing test. Implemented/Connected/Deployed/Customer-ready remain `not_tested`. Source hashes normalize physical CRLF/LF so the same ledger has a portable identity.

Five synthetic temporary-root tests passed, including unchanged historical sentinel, no secret-canary content in outputs (not a general redaction claim), duplicate/invalid inputs, existing/in-tree destinations and actual ledger count. The actual ledger generated a separate temporary `edeviser-review-...` containing **212 declared leaves,5 layouts,15 supplemental rows:232 UNVERIFIED and zero executed checks**. An unqualified invocation exited1 and left the historical pack hash unchanged; it did not produce a fake success. Because the Windows Vitest ESM loader rejected this checked-out `.mjs` with CRLF, a fourth exact-path physical-LF rule was added to `.gitattributes`; the source content was otherwise unchanged. Old PDF/CSV files still embed unsupported historical claims; route runtime validation, independent security/secret review and an authorized replacement publication workflow remain. V16 is **Implemented, not whole-product Verified**. Final-source ordered local gates passed: zero-warning lint → TypeScript → seven-entry catalog check → **821/821 unit/property files, 8210 passing tests +1 pre-existing skip** (`--maxWorkers=2`) → production build (the existing large-main warning remains P08) → local smoke (16074 root characters, two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth-expiry Chromium cases. The seven-path runtime resolver returned no backend groups/functions/shared paths/errors: Deployment Impact: **NONE (backend)**; Runtime features `[]`; Affected functions `[]`; Shared runtime changed **NO**; Production action required **NO backend action**. This ordered source run does not turn the new unverified route export into executed route coverage or attest the historical PDFs.
### V15 — Preview E2E fixture target and honest CI collection

The root Playwright configuration still declares audit setup/teardown for its role projects; no default runner execution, Preview seed or production session was performed here. Previously guard-off `seed.ts` rewrote six credential-bearing storage-state paths to empty JSON; a stale file could then make downstream role tests redirect or pass misleadingly. Legacy-only project selection now returns without creating/overwriting role files or invoking management/browser/seed. Audit role selection without explicit prerequisites fails before filesystem or fixture mutation, and pre-existing role-state files cause a preservation failure rather than replacement.

A new `previewFixtureTarget.ts` first checks opt-in, `SUPABASE_DB_ENV=preview`, parent and distinct Preview refs, nonempty PR head/number and credentials, and an exact HTTPS `https://<preview-ref>.supabase.co` API URL. It then calls the official **read-only** Supabase [list-all-branches Management API](https://supabase.com/docs/reference/api/v1-list-all-branches) and accepts exactly one `FUNCTIONS_DEPLOYED` branch whose `project_ref`, `git_branch` **and** `pr_number` all match. A direct MCP-created branch or a production/default endpoint cannot satisfy that contract merely by setting an env label. After successful verification, setup generates its own run identity before an authorized seed POST; no response body/credential is printed on errors. Teardown requires the setup-owned marker/runId/ref/branch/PR, rechecks Management API identity and sends cleanup to that same ref only; an absent marker or inherited run ID never authorizes deletion. Failed checks surface as errors. Separate migration replay, RLS role behavior and cleanup under actual failure still require Git-linked Preview certification; `FUNCTIONS_DEPLOYED` alone is not a complete schema pass.

The ordinary GitHub `e2e` job previously tested only whether `VITE_SUPABASE_URL` was different from localhost, then ran `legacy-smoke` with secrets-based credentials—even production could meet that test. It now uses only a fake loopback URL, checks collection/contracts, and explicitly labels the job **E2E Collection (not role runtime)**. The separate hermetic auth-expiry job remains the only newly executed local browser auth coverage. 18 temporary/synthetic guard cases verify no-network guard-off, invalid/mismatched/ambiguous/direct branch denial and owned teardown target, with all four focused Preview/config/auth suites passing60 cases; no management secret or real Preview request was used. The root/default Playwright audit has not been run and V15 remains **Implemented, not Verified**. Deployment Impact: **NONE (backend)**; Runtime features `[]`; Affected functions `[]`; Shared runtime changed **NO**; Production action required **NO backend action**. Preview/production mutation and deployment remain separately governed. The first full integration attempt passed lint/types/catalog and 8227 tests but failed one older `axe-helper` assertion expecting the obsolete guard-off warning; its updated test now asserts that no unowned cleanup is attempted. Thirty focused guard/evidence cases then passed. **Fresh final-source ordered gates passed:** zero-warning lint → TypeScript → catalog check → **822/822 unit/property files, 8228 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (existing large chunk warning remains P08) → local smoke (16074 root characters, two isolated collectors, service workers blocked) → all eight locales → **3/3** dedicated auth-expiry Chromium tests. Safe root Playwright `--project legacy-smoke --list` collected127 tests in10 files without executing setup/teardown/browser. YAML parsing and the exact eight-path runtime resolver returned no backend groups/functions/shared paths/errors. This remains a *local* fixture-governance implementation, not a completed Preview or customer-ready certificate.
### G01 — current-source Route declaration versus dated owner inventory

`npm run check:route-inventory` now makes the existing ledger inventory executable as a **read-only source drift check** in the Type Check CI job after the catalog check. The pure TypeScript AST parser reads only `src/router/AppRouter.tsx`, the literal `src/lib/criticalRoutes.ts` segment object and this ledger; it does not import/execute React, call Supabase, read credentials, generate screenshots or update a baseline. It resolves nested index and relative paths, all eight static critical route bindings, the five role-layout guards and the standalone student focus guard. It compares each exact role+absolute path+layout/leaf tuple and first Route target symbol to the corresponding ledger row, rejects duplicate/missing/new Route declarations, unresolved dynamic paths, malformed/duplicated IDs, non-inventory statuses and guard-role mismatches. Supplemental `route-surface-*` owner rows are deliberately not treated as additional JSX Route declarations.

At the current isolated checkout, the direct CLI returns **212 source leaves and 5 layouts matched to212/5 ledger entries, zero static drift findings**. Six synthetic tests include added/removed routes, owner and critical-segment changes, duplicate paths/IDs, unresolved expressions, and a standalone student path with an intentionally wrong teacher guard. An initial fixture omitted literal layout guard roles and correctly failed; the test fixture was corrected without weakening the production checker. This result validates a declaration/first-owner snapshot **only**. It does not execute aliases, guarded routes, actual parent-child/account scope, data states, localization, visual fidelity or delivery; 15 supplemental surface rows are not owner-depth proof. Stable historical `route-<role>-NNN` ID-to-path continuity across future edits still requires comparing a reviewed prior ledger revision; an in-snapshot bijection cannot detect a same-owner ID/path swap. The all-route rendered matrix and genuine customer workflows remain G08/G04 work, not established by a green source ratchet. No original audit finding is newly marked Verified by this milestone. Final-source ordered local gates passed: zero-warning lint → TypeScript → catalog check → named read-only route contract → **823/823 unit/property files, 8234 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (large-main warning P08 still open) → smoke (16074 root characters/two isolated local collectors, service workers blocked) → all eight locale namespaces → **3/3** isolated auth-expiry Chromium cases. The eight-path manifest resolver returned no backend functions/groups/shared-runtime paths or errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Hosted CI and branch-protection execution for this local follow-up remain unverified; draft PR #342 is unchanged.
### G02/V09–V12 — visual evidence cannot self-approve a prototype

The RTL spec already uses a single Playwright snapshot path for existence and comparison with non-update policy and exact final-route/Arabic direction checks, but no authenticated reviewed baseline was supplied; **V09 remains Open**. The separate historical `visual/` harness had five active dashboard rows ×four viewports at `maxDiffRatio:0.6`, while `parity.spec.ts` skipped missing refs and silently accepted missing role storage by falling back to anonymous contexts. It could capture `/login` after a redirect and compare it to a prototype. The old capture script directly overwrote committed `visual/references/*.png` when `VISUAL_CAPTURE=1`, and comments/README falsely called prototype captures approved design truth.

`visual/parity.spec.ts` now fails on no active screens, missing references, absent/invalid role state, unexpected final pathname, wrong EN/LTR/light root or unsettled `document.fonts.ready`; it sets desired device language/theme before navigation but **does not override actual profile-owned choices**—a mismatch is a failure. It no longer swallows font readiness. The default pixel mismatch thresholds, active flags and old reference bytes were **not** weakened, refreshed, deleted or approved. `prototype-reference.spec.ts` now writes exclusive `CAPTURED_UNREVIEWED` candidates under ignored `test-results/visual-candidates/<run-id>/` via `writePrototypeCandidate`, never to committed reference paths. Candidate generation remains prototype-only and may use fallback fonts; it does not establish app fidelity. The normal capture-disabled registration is explicitly skipped, not a fake passing test. The revised visual README/config/map identify this as a migration comparison, not an accepted design or whole-product release gate.

Four synthetic no-overwrite/path-safety/threshold/source-policy checks and21 visual test registrations (20 active comparisons +1 capture-disabled placeholder) passed collection **without starting application/prototype servers, altering PNGs, logging in or running default Preview-seeded Playwright**; the existing RTL policy tests also passed in the same scoped run (25 total). An initial brittle source assertion about formatted call whitespace failed and was corrected to recognize the same call without suppressing behavior. **V11 is Implemented, not Verified by rendered-role proof; V12 remains Open at the broad historical threshold.** Real reviewed application-owned image baselines, two themes/two languages, authenticated routes, interactive states and full current route coverage remain G08 work. No image-capable inspection or approval was performed this round. Final-source ordered local gates passed zero-warning lint → TypeScript → catalog check → current-source route check (212 leaves/5 layouts) → **824/824 unit/property files, 8238 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (existing large-main warning P08) → local smoke (16074 root characters/two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth-expiry browser cases. The eight-path resolver found no backend functions/groups/shared runtime paths or errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. The parity screenshots/candidate capture were NOT executed and the passing source gates do not approve historical image bytes.
### D15 — paired notification count and unread-status cue

The current isolated source had 24 authored non-generated `bg-transparent0` production occurrences before this bounded slice (the ledger's prior 21 count was from a different dated snapshot); the generated `src/components/ui/EdvToggle.tsx` retains its separate protected occurrence. The real `NotificationBell` and shared four-role `NotificationsFeedPage` each had one invisible malformed unread marker. The Bell count is **attention, not a destructive academic/risk label**: a finite `--notification-unread-badge-bg`/`--notification-unread-badge-fg` pair now follows Precision Light, Obsidian Dark and their two explicit high-contrast overrides, keeping the visible 99+ cap while its bilingual screen-reader label gives the real count. The actual Bell trigger is at least44px and its decorative icon is hidden from assistive tech. The feed unread dot uses the existing semantic primary against its actual card/hover surface; a localized visually hidden “Unread” text belongs to the row's accessible name rather than an aria-label on an empty span. Seven previously absent `notificationsFeed` copy keys now exist in both English and Arabic. Query shape, read/delete mutation actions, role routing and academic meaning remain unchanged.

Focused Bell/feed source tests and normal-theme opaque-token contrast checks passed; a new closed-loopback Chromium fixture rendered the **actual Bell and feed components** with controlled auth/query/realtime DTO seams. Eight EN/AR × light/dark × normal/high-contrast scenes (320px/root20 for high contrast;1280px/root16 otherwise) passed **4.5:1 count text**, **3:1 count-chip/background and unread-dot/actual composited surface**, 44px trigger geometry, labels, unread-vs-read visibility and zero horizontal document overflow without relaxing limits. Minimum observed values were Bell text6.47:1, chip6.47:1 and dot5.35:1. The first component test exposed that the pre-existing icon-sm Bell trigger stayed32px because a proposed min-size edit missed its source anchor; the actual owner was corrected to44px before scoped rerun. The first browser fixture evaluated root classes too early and threw; it was corrected to apply theme after document creation, not by suppressing the page error. Formatted final-source fixture reran clean. This is **bounded real-component paint** with simulated data, not authenticated notification delivery, Bell portal/mobile placement, fully localized feed, all-route state or deployment attestation.

The current grep has24 `bg-transparent0` matches in `src/**/*.tsx`: one test assertion, one protected generated-UI component and **22 remaining authored production matches**. Some are active charts, progress, presence, sale and tutor controls; others (NotificationCenter, TeamLeaderboardView) are source-only/unreachable candidates. The protected generated component must be adopted through supported ownership rather than hand-edited. D15 **stays Open**. A separate read-only guideline review also located nested interactive buttons in dormant `NotificationCenter.tsx`, immediate delete without confirmation/undo and an unbounded list in active `NotificationsFeedPage.tsx`, and promotion/live badges with unrelated semantics; these are NOT repaired by unread-paint changes. [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) reinforce localized control labels, meaningful non-color status and keyboard/touch alternatives; this limited repair does not close their other identified interaction defects. Final-source ordered local gates passed zero-warning lint → TypeScript → seven-entry catalog → current-source route check (212 leaves/5 layouts) → **824/824 unit/property files, 8245 passing tests +1 pre-existing skip** (`--maxWorkers=2`) → production build (the 1.50MB emitted main warning remains P08, not measured initial transfer) → local smoke (16074 root characters/two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth browser cases → the same eight-cell real Bell/feed paint fixture (zero failures/skips). The exact11-path runtime resolver returned no backend functions/groups/shared paths/errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. No full-route, visual capture review, notification backend or deployed telemetry was exercised.
### D15 follow-up — marketplace promotion is not an error state

`SaleBadge.tsx` is consumed by the routed student marketplace `ItemCard.tsx`; its `bg-transparent0` left the original white “% OFF” copy without a background and no contrast owner. A distinct `--promotion-badge-bg`/`--promotion-badge-fg` component-token pair now has deliberate light, dark and two high-contrast values in the existing CSS owners. It is visually a promotion, not a destructive action, a grade band or a guaranteed price calculation. The Tag icon is decorative, and the component uses `Intl.NumberFormat` percent presentation plus English/Arabic `saleBadge.off` copy; finite positive values present the supplied discount to at most two display decimals (including the12.5 fixture), while nonfinite/nonpositive standalone badge values render no fabricated offer. `ItemCard` XP price calculation, purchases, backend sale query and authoritatively awarded discounts were not changed.

Seven SaleBadge component cases cover25%,12.5%, Arabic-Indic formatting and invalid values; existing normal-theme token math preserves 4.5:1 text and3:1 badge/card distinction. The previous closed-loopback real Bell/feed browser fixture now also renders the **actual SaleBadge** in eight EN/AR × light/dark × normal/high-contrast cells (320px/root20 for high contrast;1280px/root16 otherwise), verifying visible translated percentage, >=4.5:1 text and >=3:1 actual composited surface without screenshots or threshold waivers. Minimum observed promotion text8.02:1 and chip/surface8.02:1. The fixture uses synthetic badge input and controlled notification DTO seams: this is not the full ItemCard, real marketplace pricing, visual approval, deployed seller policy or customer-ready sale claim. No library or licensed asset was installed.

The current source grep reports23 `bg-transparent0` matches across `src/**/*.tsx`: **21 authored production** classes, one protected generated `EdvToggle` class and one test assertion. This count is not comparable with a dated21 snapshot as an attribution claim; only this one known production SaleBadge occurrence was removed in this slice. D15 remains **Open**, and `ItemCard` itself retains light-only English and other presentation defects to review separately. [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) supports explicit localized number/copy, decorative-icon hiding and content reflow; none implies the whole student-marketplace route was visually approved. The first full unit run passed8255 tests but exposed a test-only ItemCard mount without the application i18n instance; the real card fixture now loads that owner and asserts both the unchanged 500→375 XP presentation and translated 25% chip (15 focused ItemCard/SaleBadge cases passed). **Fresh final-source ordered local gates passed:** zero-warning lint → TypeScript → seven-entry catalog check → source route inventory (212 leaves/5 layouts) → **825/825 unit/property files, 8256 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (roughly1.50MB emitted main chunk warning remains P08, not initial transfer) → local smoke (16074 root characters, two isolated Vercel collectors, service workers blocked) → eight locale namespaces → **3/3** isolated auth-browser cases → eight-cell real Bell/feed/promotion browser fixture. Exact10-path runtime resolver reported no affected backend functions/groups/shared-runtime paths or errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. No live product marketplace/pricing or deployed report action was run.
### D15 follow-up — student profile activity is not live connectivity

`useFriends` historically exposed an `online` boolean from `profiles.last_seen_at`, calculated only when a Query response arrived. It had no inspected heartbeat/socket producer, and future timestamps incorrectly passed its `<5 minutes` comparison; cached `online:true` could remain visible indefinitely. A pure `src/lib/recentActivity.ts` helper now rejects null, invalid, future, nonfinite clock and age >=5 minutes, while keeping the existing hook API/Query key and sort contract for compatibility. The actual Friends page and dashboard rail independently recompute the positive hint from the raw timestamp using a scoped one-minute student display clock (plus document-visible refresh). Unknown/stale/future values are **not** rendered as verified “Offline”; the heading is “Recently Active”/“نشاط حديث”, never “Online now”. The marker is paired with an avatar outline and a separate visually hidden EN/AR activity description so its tiny dot is not the sole signal. Friends page cards/neutral text and the dashboard friend rail now consume semantic backgrounds/foregrounds rather than light-only white/gray islands; unrelated academic, XP, friendship requests and navigation remain unchanged.

English/Arabic `friends.*` page copy was previously absent from the student namespace and supplied English-only fallbacks; both namespaces now have the declared labels, plus `dashboard.friends.recentActivity`. Pure fixed-time tests include exact five-minute boundary, invalid/null/future values; actual FriendsPage component cases reject a stale `online:true` bit and prove the positive group expires at a local clock tick without a query refetch. Hook lifecycle tests cover the visibility event and unmount cleanup. The source checker rejects reintroduction of `bg-transparent0` or a raw `f.online` UI condition in either active consumer. These are frontend contract tests, not live `last_seen_at` producer/RLS proof; a closed-loopback actual-component browser fixture passed eight EN/AR × light/dark × normal/high-contrast FriendPage scenes (320px/root20 for high contrast;1280px/root16 otherwise) without screenshots or database calls. It showed the recently-active heading and visually hidden status for the one valid past timestamp, omitted markers for null/future values despite their mock online:true bits, and measured the actual layered card dot against its backing at >=3:1 (minimum5.48:1); avatar dot/ring paint and zero horizontal overflow also passed. The fixture mocks friendship/query/auth DTOs and cannot certify realtime presence, protected relationship visibility, dashboard route paint or a production heartbeat. The current grep contains20 `bg-transparent0` `src/**/*.tsx` matches: **18 authored production**, one protected generated-ui occurrence and one test assertion. D15 remains Open; whole student routes, background-query errors, all contrast islands and real peer privacy still need review. **Final-source ordered local gates passed**: zero-warning lint → TypeScript → seven-entry catalog check → current-source route inventory (212 leaves/5 layouts) → **827/827 unit/property files, 8273 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (1.506MB emitted main warning remains P08; not measured initial transfer) → local smoke (16074 root characters, exactly two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → eight-cell real notification/promotion and eight-cell real FriendsPage browser groups, each zero failures/skips. Exact15-path runtime resolver returned no backend function/group/shared-runtime path or errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. The two changed student locale *paths* also occur in backend PR #341 but this branch adds dashboard/friends copy near lines9–95; #341 adds assignment-upload strings around line189, disjoint hunks. No backend, Preview, full authenticated student dashboard or production action occurred.
### PR04 — one-way portable color-token pilot, no second design authority

The canonical `src/design-system/tokens.css` remains the sole editable source for runtime colors. The new narrow `scripts/design-tokens/export.mjs` writes **only** `docs/design-system/portable-tokens.generated.tokens.json`, never CSS. It extracts exactly eight named literal opaque hex values from the normal root and eight from `.dark`; duplicate, missing, nonliteral, alias, alpha or short-hex selected values fail rather than inferring CSS precedence. The artifact uses explicit `$type: "color"` and `{colorSpace:"srgb",components:[r,g,b],hex:"#rrggbb"}` values, an `$extensions` provenance digest of **selected declarations**, and physically line-ending-independent output. The read-only `npm run check:portable-tokens` fails missing/modified artifacts; `npm run generate:portable-tokens` is the separate explicit write path; CI invokes the check after the catalog in the existing required typecheck job. Target format is the published [DTCG Format 2025.10](https://www.designtokens.org/TR/2025.10/format/) plus [Color 2025.10](https://www.designtokens.org/TR/2025.10/color/) Final Community Group Reports—**not W3C Standards**. Light/dark JSON groups are organizational; the separate [Resolver module](https://www.designtokens.org/TR/2025.10/resolver/) is **not** implemented. This is a declared subset writer, not a general CSS parser, color reader, token alias resolver or schema-completeness certificate.

The new [portable token pilot guide](../design-system/PORTABLE-TOKENS.md) states ownership, exclusions and commands. High contrast, reference-brand/component tokens, role/tenant contexts, CSS variable aliases, gradients/alpha colors, typography, layout and contextual expressions remain CSS-owned and unexported; no application runtime imports this JSON. Source/token/CI cases check typed hex byte roundtrip, CRLF/LF, negative duplicate/missing/nonliteral classes, tampered artifact remaining untouched under `--check`, explicit write, unsafe targets and true source-derived scope. Rendered contrast, Storybook/Figma integration, full hierarchy, distribution and all-route redesign are still **Open** under PR04/G03/G09. None of this exports live runtime or customer readiness. **Final-source ordered local gates passed:** zero-warning lint → TypeScript → catalog → read-only portable-token drift → route inventory (212 leaves/5 layouts) → **828/828 unit/property files, 8283 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (main1.506MB emitted warning P08 remains) → local smoke (16074 root characters, two isolated Vercel collectors, SW blocked) → eight locale namespaces → **3/3** isolated auth-browser cases → existing eight-cell notification/promotion and eight-cell FriendsPage browser groups. The production CSS filename/content hash `index-ST_G3zg2.css` and main JS hash `index-DpSRcBOW.js` match the preceding source snapshot exactly; this does not measure initial transfer or approve routes. The exact11-path runtime resolver found no backend functions/groups/shared runtime paths or errors, and no overlap with backend PR #341: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. No Preview, schema, token consumer/runtime CSS or deployed Figma/Storybook changes were made.
### PR05 — compare supported authored-source candidates, not design approval

The existing AST kernel and read-only repository adapter support a per-file/owner/rule/token **multiset** comparison of authored `src` TS/TSX with explicit exclusions and caller-provided distinct roots. On the current isolated follow-up versus the unchanged frontend checkpoint, the first actual comparison failed **1 introduced /45 removed**: a `HeroCarousel` inverse control `hover:bg-white/20` literal. The repaired owner now consumes `--hero-inverse-control-hover: rgba(255,255,255,.20)` in `tokens.css` via a semantic hover class. Real Chromium exposed a competing generated Shadcn `dark:hover:bg-accent/50` winning over the first class, so the dark variant was paired explicitly; the final EN/AR dark control hover computes **rgba(255,255,255,.2)**, including minified `#fff3` token syntax, while the existing four EN/AR × 320/1280 carousel scenes and OS/stored reduction checks remain. A fixed wait/threshold waiver was **not** used: the browser assertion polls the native transition. This repairs a real candidate instead of adding a blanket exception. The following local comparison passed **1130 base /1132 current source files;7183→7138 source candidates;0 introduced,7138 retained,45 removed**. The retained candidates are not approved visuals or claimed bugs.

`npm run check:design-policy:no-growth -- --base-root <reviewed-clean-checkout>` now gives a bounded, read-only local summary/exit1 on introduced copies, exit2 on missing/unsafe/syntactically malformed input; its four scoped cases exercise multiset growth despite other removals, existing debt, five-group report cap and failure states. It selects **no** Git commit, writes/approves **no** baseline and does not import app/runtime modules. The [ratchet rollout guide](../design-system/DESIGN-POLICY-RATCHET.md) documents exact event-SHA, baseline-**executed** CI as a later reviewed phase. A read-only remote `main` GitHub contents request returned404 for the scanner during this round: enabling a seemingly green head-executed required job today would let a PR change its own judge. **No CI policy gate was wired** and PR05/G02/G03 remain partially implemented; the scanner does not certify excluded generated files, CSS cascade, semantics, logical spacing, contrast, RTL, rendered routes or deployment. **Final-source ordered local gates passed:** zero-warning lint → TypeScript → seven-entry catalog integrity (explicitly refreshed one HeroCarousel source SHA only) → 16-color portable-token drift check (no change to the selected pilot values) → current-source route inventory (212 leaves/5 layouts) → **829/829 unit/property files, 8288 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (emitted main1.506MB warning remains P08) → local smoke (16074 root characters; two isolated Vercel collectors; SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → actual bilingual carousel/browser including new dark hover, eight-cell notification/promotion and eight-cell FriendsPage groups → the same distinct-checkout source no-growth result (0 introduced/45 removed). The exact13-path runtime resolver found no backend functions/groups/shared-runtime paths or errors and zero overlap with backend PR #341: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Neither a Git-linked Preview nor any protected schema/production state was touched.
### PR06 — real pattern explorer, not a second application

A finite Storybook **10.6.0** pilot now uses exact pinned `storybook`, `@storybook/react-vite` and `@storybook/addon-a11y` MIT development dependencies (lockfile reviewed; local install and clean `npm ci` used `--ignore-scripts`). It has **eight curated stories**: the real `StatePanel` loading/empty/error/partial/permission variants, one **local-only** accessible action (does not fetch/retry or invent a write) and two actual `PCard`/`SectionHeader`/`PageHeader` reading compositions. Story examples import canonical public pattern exports; no duplicate primitive/pattern implementation, app router, profile hooks, Supabase queries, demo logins or invented academic data were introduced. `src/index.css` and initialized EN/AR i18n are reused in one Storybook preview graph. Toolbar controls simulate light/dark, high-contrast, EN/AR/RTL and normal/root20 text **for rendering only**: actual account-owned ThemeProvider, LanguageProvider and AccessibilityPreferencesProvider cannot safely mount without the AuthProvider ownership bridge. This is explicitly documented in [the pilot guide](../design-system/STORYBOOK-PILOT.md), not disguised as persisted user settings.

The local static Storybook build runs with its own empty env directory and STORYBOOK-only Vite env prefix; no root `.env.local` or real VITE Supabase credentials are needed. The ignored artifact `test-results/storybook-pilot/` is **not published/deployed** and does not replace the DSH Web GUI. Real isolated Chromium inspection of that **built** static artifact passed **120/120** scene cells (32 desktop EN/AR × light/dark across all8 stories;24 narrow320px/root20 across5 states+extended copy;8 representative high-contrast cells;56 narrow320px/root32 **200% text** variants across languages/themes/contrast including the local keyboard action). It requires correct localized text and root lang/dir/theme, StatePanel busy/alert/status semantics, heading hierarchy, no document overflow, local keyboard action, no browser errors or outbound traffic. **32 scoped axe WCAG2.1-AA inspections** on representative error/extended reading scenes across languages/themes/standard+high contrast passed zero violations; an addon panel by itself is not an automated CI pass. The first browser case revealed two valid `role=status` nodes, so its test was corrected to target the local-action message without weakening either role assertion. The added 200% text cells also check that visible h1/h2/status bounds and the keyboard-focused action stay within320px with a >=44px target; Arabic extended-reading scenes require a loaded self-hosted Noto Sans Arabic face and matching computed font-family. Font readiness/face status does not prove exact glyph shaping or human visual approval. No screenshots were reviewed/approved, and these real pattern stories do not attest authenticated role routes, every authenticated-route state, actual browser zoom, physical assistive technology or a customer-ready visual design.

`npm run check:storybook:pilot` separately typechecks `.storybook`/story files, `npm run build:storybook:pilot` builds static examples, and `npm run test:storybook:pilot` fails if that artifact or any selected story/state is missing. A secret-free CI **storybook-pilot** job installs with scripts disabled, builds and runs the closed-network Chromium gate; it is a dependency of the existing Build status check, while production Build remains in its separate environment. Scoped AST/CI tests verify exact story exports, MIT pinned dev-only lock entries, no imported app/auth owner, and CI fake-key separation. Product CSS/main-JS emitted hashes will be rechecked at final gates for any story-source leakage. PR06 remains **Partial** until wider reused patterns, reviewed application-owned visual images, actual provider/route states and app-level interaction/contrast coverage pass. Storybook's index/manifest is preview tooling, **not** the Edeviser PR07 stable catalog/manifest or a migration of authorization. Early verification caught three integration failures and did not mask them: TypeScript 6 required the existing `src/vite-env.d.ts` in the separate preview typecheck; zero-warning ESLint scanned generated ignored Storybook manager JS until only `test-results/storybook-pilot/**` was excluded and only preview-config Fast Refresh was exempted (authored stories stay linted); existing CI job-boundary tests caught misleading comment placement/old Build prerequisite assumptions, so the fake-key explorer now sits before the unchanged auth job and Build depends on both. A Storybook browser assertion also distinguished the real partial `role=status` from its separate demo-only interaction status rather than allowing an ambiguous match. The source-derived catalog was regenerated explicitly for the lockfile SHA only; the seven APIs/examples and metadata were not edited by hand. **Final-source ordered local gates passed:** zero-warning lint → TypeScript → catalog → portable-token drift → source route inventory (212 leaves/5 layouts) → Storybook-specific typecheck → **830/830 unit/property files, 8293 passing tests +1 pre-existing skip** (`--maxWorkers=2`) → app build (existing1.506MB emitted main warning P08) → local smoke (16074 root characters/two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → static Storybook build → **64/64** closed-loopback Storybook scenes with16 scoped axe runs → existing carousel, notification/promotion and FriendPage browser groups → clean-checkpoint design-policy comparison (0 introduced/45 removed). The production CSS `index-DfFKUnpc.css` and main JS `index-BQH5NA_f.js` names/content hashes match the preceding frontend snapshot under the same fake environment; no runtime story CSS/source leak was observed. Storybook built JS did not contain either known fake VITE key, and its browser fixture blocked all outbound requests. Exact18-path runtime resolver reports no backend functions/groups/shared paths or errors and zero PR #341 path overlap: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. The new CI job/build dependency is only locally source-verified; hosted exact-head CI, visual review, authenticated five-role scenes and deployment remain pending.
### PR06/G05 follow-up — 200% root text in actual pattern stories

The same real-source Storybook preview now offers **32px root size** alongside16px/20px, explicitly **200% text relative to the base16px**, not a browser/device zoom simulator or a persisted AccessibilityPreferencesProvider setting. Its closed-network built-asset test expanded from64 to **120 passing scenes** across EN/AR, LTR/RTL, light/dark, standard/high-contrast and narrow320px/desktop contexts. The additional56 narrow/root32 cells include all five StatePanel variants, local-only keyboard action and extended PageHeader/SectionHeader/PCard copy; they fail clipped visible h1/h2/status text bounds, document horizontal overflow, actions outside the320px viewport or below44px, wrong computed32px root, incorrect AR/EN lang/dir, browser errors or outbound requests. **32** representative scoped axe WCAG2.1-AA scans (normal/root32, standard/high contrast) passed zero violations. Visible Arabic extended-reading scenes also require an actually `loaded` Noto Sans Arabic FontFace and the heading's computed family to declare that face; `document.fonts.ready` alone is not claimed as glyph proof. No screenshot/image-human review or exact glyph-shaping approval was performed. The [current Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) emphasize text reflow, visible focus, contrast and assistive semantics, but this bounded explorer is **not** an authenticated route, actual browser 200% zoom, parent/admin/teacher/coordinator acceptance or production delivery certificate.

Latest isolated ordered gates passed zero-warning lint → TypeScript → seven-entry catalog check → 16-color portable-token check → source route inventory (212 leaves/5 layouts) → Storybook typecheck → **830/830 unit/property files,8293 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (existing1.506MB emitted main warning P08) → local smoke (16074 root characters, exactly two Vercel collectors isolated, SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → built Storybook/120 real scenes → unchanged carousel, notification/promotion and FriendsPage browser groups → local checkpoint no-growth (0 introduced,45 removed;7138 retained). The app's production CSS `index-DfFKUnpc.css` and main JS `index-BQH5NA_f.js` hashes remain exactly the same as before these story-tooling changes under identical fake environment variables. The exact5-path runtime resolver reports no backend functions/groups/shared runtime paths or errors and no backend PR #341 overlap: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Required hosted exact-head CI and whole-product G05/G08 remain pending; no PR update, merge, deployment or Production mutation occurred.
### D15/G04 — two coordinator coverage observations, neither academic attainment

A **read-only LIVE Supabase pg_proc + pg_get_functiondef SELECT** on the current project confirmed `get_coordinator_workspace()` and `get_coordinator_accreditation_readiness()` are deployed **SECURITY DEFINER** functions with explicit coordinator/admin identity guards, not the `SECURITY INVOKER`/pure-RLS premise in the old aggregate-hook comment. The workspace definition scopes counted active assigned courses to coordinator programs and reports `coverage.totalClos`, `mappedClos` and the rounded percent of CLOs participating in an outcome mapping; zero eligible CLOs returns a numeric0 with `status=insufficientEvidence`. It does **not** count mapped assessments or prove learner attainment. The accreditation-readiness definition scopes all institution courses and reports the rounded share classified documented (a mapped CLO plus course evidence and attainment presence). Its four independent `pack` checklist items are **not the percentage denominator**, and 100% is not regulatory accreditation. With no institution courses the RPC returns numeric0; the hook also returns null on unavailable/error. These claims are about the inspected deployed function *definition*, **not** authenticated role behavior, real result rows, completed accreditation or Preview attestation. No SQL write/DDL or protected migration/type change was made.

`src/lib/coordinatorCoverageView.ts` now admits only finite bounded percentages and positive, consistent CLO/course denominators for this **display**, without recomputing or shifting any institution academic thresholds. The routed CoordinatorDashboardScreen uses the aggregate `coverage` tuple and validated accreditation `courses` result; zero denominators and failed RPCs render **unmeasured** status/“—”, never an announced `aria-valuenow=0`. It composes a feature-owned `CoordinatorCoveragePanels` with actual PCard/SectionHeader/link APIs: two distinct explicitly named EN/AR progressbars only when measured, `bg-primary` fills on `bg-muted` tracks and semantic card/text/action pairs; integer-percent display and mapped/total CLO counts use `Intl` without modifying the backend values. Copy now describes CLO **outcome mapping** rather than linked assessments or PLO attainment and identifies the evidence percentage as **institution-wide documented-course coverage**, not pack completion or certification. Pack labels/states are bilingual; malformed unknown IDs/states show generic unavailable descriptions rather than raw code or a false pending label. Other hardcoded/coordinator sections, backend role execution and program-specific coverage still need separate review.

Pure boundary tests, actual routed-owner integration cases and five isolated-component state cases passed; a closed-loopback real-component/CSS/i18n Chromium fixture passed **80/80 scenes**:5 finite/unknown/no-data scenarios × EN/AR × light/dark × standard/high contrast × 320px/root20 and1280px/root16. It enforces measured role/value/width semantics, zero-versus-unknown, exact localized headings/scope/counts, link focus/keyboard targets >=44px, zero horizontal overflow, no browser errors/outbound requests, computed fill/track >=3:1 and readable text/CTA >=4.5:1 against composited actual surfaces. Its first locator assertion was corrected to match the real component, not to loosen a threshold; an initial component unit assertion caught that an accessible meter label was not visible to sighted readers, so a visible evidence scope caption was added. This is controlled-props component paint, **not a logged-in coordinator route or official evidence decision**. Current `src/**/*.tsx` grep:18 matches = **16 authored production**, one protected `src/components/ui/EdvToggle.tsx` and one test string. D15 remains Open. **Latest ordered local gates passed:** zero-warning lint → TypeScript → seven-entry catalog → 16-color portable-token check → source route inventory (212 leaves/5 layouts) → Storybook typecheck → **832/832 unit/property files, 8314 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (main1.508MB emitted warning P08 remains; CSS `index-CfFvoYZ1.css` 232.26KB vs previous231.98KB, not measured initial transfer) → local smoke (16074 root characters, exactly two Vercel collectors isolated, SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → built Storybook/120 scene pilot → real coordinator80 scene/browser and existing carousel/notification/Friends browser groups → explicit-checkpoint design-policy local comparison (0 introduced/55 removed;7128 retained). This exact12-path frontend resolver returned no backend functions/groups/shared paths/errors: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Backend PR #341 also touches both coordinator locale *paths*, but it changes top-level course-file and separate accreditation copy; this frontend change is confined to dashboard gap/evidence keys, disjoint hunks. PR #341 also changes `useCoordinatorAccreditation.ts` source/error parsing, so that hook was deliberately restored to clean HEAD and is **not** in this frontend commit. Current hook error swallowing remains a separate backend PR handoff, not a frontend claim of repaired service validation. Local component validation cannot attest authenticated or hosted Preview behavior.
### D15/G04/G05 — PLO attainment band palette and honest program filtering

The four active `AdminAnalyticsPage` PLO legend swatches previously used non-emitted `bg-transparent0`; the fifth unmeasured light swatch had only ~1.48:1 source contrast on a white PCard. Their own PLO heatmap uses **85/70/50** visual band cutoffs plus a `-1` no-evidence sentinel. The adjacent *different mastery-distribution metric* uses **90/80/70** elsewhere; neither scale was changed or promoted to an official configurable institution grade scale. A feature-owned actual `PloAttainmentHeatmap` now maps five finite source-declared bands to named `--plo-<band>-surface`/`--plo-<band>-ink` pairs in the existing token owner; the same ink colors each legend marker and its tinted cell labels. The marker is decorative and categories are visibly named in every cell; full PLO names wrap rather than truncate. The pure `classifyPloAttainment` helper centralizes the exact existing85/70/50 source cutoffs for both hooks/pages *before* display rounding; invalid/nonfinite/out-of-range or unsupported DTO categories render neutral “Unmeasured”/“—”, not fabricated0 or Not Yet. Half-open legend labels make the boundaries explicit, and a visible disclaimer keeps native assessment scales/official academic records authoritative. Source token checks, exact-boundary/Arabic/filtered-page cases and a **direct parent-executed** closed-loopback Chromium test now pass. The delegated browser worker failed without a result; its output was not counted. The inspected finite fixture independently built the real component, CSS, i18n and Shadcn Select, and passed **16 EN/AR × light/dark × standard/high-contrast × 320px/root20 or1280px/root16 contexts with five scenarios apiece (80/80 scenario cells)**. It checks same-color category markers/cell text, marker-to-card >=3:1 and cell text-to-tint >=4.5:1, explicit0 versus —, legend half-open ranges, localized labels, actual keyboard Select focus/option change and filtered loading/error/empty, no app horizontal overflow and no external browser traffic. There are no screenshots/approved image baselines, authenticated admin routes or live PLO result rows in that fixture.

The page formerly displayed unfiltered institution PLOs beneath a *selected* program when its filtered query was pending or errored. All-program fallback remains only while `selectedProgram=all`; a selected program now shows localized loading/error/empty states with an actual query retry and no misleading global rows. A routed-page mock integration test checks that transition, while component tests cover raw84.99 versus85, real0 versus sentinel-1, invalid categories, keyboard-labeled program Select and EN/AR/RTL labels. The widget uses paired new keys in `locales/{en,ar}/common.json`, not `admin.json`. The current local Arabic `admin.json` contains **163 of 168 parsed strings with reversible mojibake** (source bytes, not merely a display encoding glitch), and five strings already correct; English admin has five damaged rail labels. Open backend PR #341 rewrites both admin locale files wholesale and adds competency translations, so this frontend branch deliberately does **not** modify those competing paths or claim the rest of the admin route is bilingual. Any admin locale repair requires separate strict per-key roundtrip-verified work coordinated with #341; simple namespace key parity is insufficient proof. No machine translation, runtime decoder, backend function, generated primitive or protected file was introduced. D15, G04 and G05 remain Open for the whole role. **Latest ordered local batch gates passed:** zero-warning lint → TypeScript → seven-entry catalog → 16-color portable-token drift → route source inventory (212 leaves/5 layouts) → Storybook typecheck → **835/835 unit/property files, 8347 passed +1 pre-existing skip** (`--maxWorkers=2`) → production build (main1.510MB emitted warning P08 remains; CSS `index-BNPv1qfW.css` 231.92KB, not measured initial transfer) → local smoke (16074 root characters, two isolated Vercel collectors, SW blocked) → all eight locale namespaces → **3/3** isolated auth-browser cases → built Storybook/120 scene pilot → direct admin PLO browser fixture (80 scenario cells) and prior coordinator, carousel, notification/promotion and FriendsPage groups → local clean-checkpoint no-growth comparison (0 introduced/61 removed;7122 retained source candidates, not adjudicated compliance). Exact15-path runtime resolver found no backend functions/groups/shared paths or errors and **zero backend PR #341 changed-path overlap**: **Deployment Impact: NONE (backend); Runtime features: []; Affected functions: []; Shared runtime changed: NO; Production action required: NO backend action**. Draft frontend PR #342 and exact-head hosted CI were not updated by this local batch yet; no visual baseline, deployment or customer-ready certification is inferred.
## Batch8 evidence — completed local gates, not whole-product certification

The following round10–11 entries record work and pending checks at those checkpoints. Round12 below supersedes their pending Batch8 browser status and verifies the narrow D17 acceptance criteria. Later work and the full application must not be described as certified by these scoped results.

- Parent changed StatePanel to reuse PCard, semantic destructive text, named reduced-motion-safe loading, and actual EN/AR defaults. The existing design-system pattern tests and platform preservation tests passed together: scoped lint and28 tests. The latter now checks one AST-parsed AppToaster mount beneath actual providers rather than preserving obsolete prototype center/rich-color presentation. Query keys, audit logging and XP preservation assertions remain unchanged.
- AppToaster replaces the generated primitive's five important defaults through its public API, without editing the generated file. Parent review repaired centered placement and unbounded content with native direction-aware bottom-end positioning and bounded scrolling. One root `--z-toast:11000` token governs this layer above existing tours10000; other portal layers remain open. The child's Vitest startup was rejected by esbuild spawn EPERM; no approval-disabled escalation or bypass followed. Parent execution then found and recovered four strict test type errors; lint and TypeScript passed. Five actual unit failures exposed Happy DOM computed-variable assumptions and an incorrect native hotkey label: exact inline API assertions now stay explicitly separate from Chromium paint proof, and the source-verified label is alt+T. All13 actual-Sonner unit cases subsequently passed. Chromium then proved long touch scrolling blocked by native touch-action:none (scrollTop0 despite1480px available). Parent implemented an explicit shared touch policy: pan-y/pinch-zoom plus a touch-only pointer-down propagation boundary; visible close/action clicks and native mouse/keyboard behavior remain. Round11 parent scoped lint/type checks passed; a combined60-second command exhausted its wall-clock cap after entering Vitest, without test results. The tests-only background run was collected successfully:14 actual toast cases (including touch-versus-mouse input ownership) plus44 attendance cases,58 total in4.14s. Chromium follow-up has exercised native CDP touch scrolling and action taps successfully; remaining mouse/keyboard/focus checks and final integrated gates are pending. Touch swipe dismissal is intentionally replaced by scrolling plus visible controls, not falsely claimed preserved.
- Parent attendance migration removes invented names/counts, six synthetic periods, four desktop-only demo courses, fixed praise and institution contacts. It distinguishes unavailable/error/loading/empty/real-zero evidence, gates the real rail with the page, localizes both surfaces and retains exact calculation/query/auth contracts. The canonical zero-denominator100% convention remains unchanged; the UI must identify absent measurements rather than display a measured100%. Child reports scoped lint and49 tests across the actual page/rail component test plus unchanged calculation/parity tests. The collected attendance-only Chromium run also passed168 actual page/rail/StatePanel state scenes across both themes, EN/AR and390/1280/320-at-20px contexts. This is component/state proof with query DTO seams, not authenticated parent-route certification. Parent additionally exposed the rail through the existing feature barrel and removed ambiguous global-absence denominator copy. Final integrated review is pending.
- The existing browser regression script is being extended, not replaced. Its next proof must use actual ThemeProvider and LanguageProvider with only auth/preference-persistence boundaries isolated, actual AppToaster/Sonner, and real attendance page/rail with owned query DTOs. All108 older cases and strict contrast/gamut/geometry requirements remain. Attendance screenshots were captured; visual inspection and remaining toast checks are pending. This is not a212-route completion claim.

### Round11 integration and review checkpoint

- Parent ordered lint and TypeScript passed, followed by **803 unit files /7776 passing tests +1 existing skip** (167.08s), then **4490-module production build** (13.07s). The initial command's last stage named a nonexistent smoke script and exited1; this was a command-path error, not an application failure. The actual `scripts/smoke-prod-build.mjs` was then read and run successfully: **22859 root characters**, exactly2 isolated local Vercel collector scripts. No alias/extra script was created to hide the mistake. The migration-ledger missing-input stderr is the expected negative CLI fixture at `runtimeDependencyGovernance.test.ts:309–314`, not an unexamined migration failure.
- The built HTML references JS `dist/assets/index-F-3KMk7g.js` (1475446B,450.43kB gzip, SHA256 `F4BC76987CEC910C758AA616C594DB73E144BF74AF6ED1D9F46C19C7A41A0F1C`) and CSS `dist/assets/index-BV5R3yv5.css` (225826B,35.51kB gzip, SHA256 `206029DC3AD561A0C80F7387FA4C7E18371C5C9A85BB46D2F1E0940BB5E61DDB`). This local hermetic build includes concurrent workspace changes and uses fake loopback Supabase settings; it is NOT a release/deployment artifact or a performance-budget pass.
- Real mouse→Alt+T browser checks exposed a second notification defect: the list became focused but Chromium did not match `:focus-visible`, leaving its front toast without an indicator. Only the negative-tabindex list's selector now uses `:focus`; ordinary toast/button indicators retain `:focus-visible`. Follow-up light-theme notification checks passed; the dark-theme gesture-to-bottom/action-tap case is still under investigation. Sequential keyboard checks no longer substitute programmatic focus/scroll for actual navigation. New target checks must use the existing exact44px minimum, not43.5px.
- Complete112-case browser integration remains pending while its new fixture tests settle; no original finding is newly marked Verified. Screenshots were captured, but parent `read_image` refused them because the configured model does not declare image input. Captured/computed evidence is NOT visual review or approved baseline evidence.
- All8 locale namespaces pass key parity. The16-path Batch8 runtime resolver returns no direct/shared functions, groups or errors and `deploymentRequired:false`; scoped diff hygiene passes. **Deployment Impact: NONE; Runtime feature(s): []; Affected functions: []; Shared runtime changed: NO; Production action required: NO** for the resolver's Supabase scope. A future frontend release and its independent verification remain separate.
- G02 read-only preparation confirms current V09–V12 defects: mismatched RTL reference paths/first-run pass, uncollected and behaviorally unsound cookie-based refresh tests, missing-reference/auth fallbacks,20 prototype-based dashboard comparisons at0.6 tolerance, and capture overwriting references without approval provenance. No baseline, auth fixture, capture policy or Preview data was changed. Source-policy repair must not pretend isolated real sessions, authorized preference changes or reviewed application baselines already exist.

### Round12 — Batch8 browser completion and next bounded work

- Parent collected `pwsh-33`: **112/112 Chromium tests passed, zero failures/skips/cancellations,712.98s**, preserving all108 earlier cases. The frozen script SHA256 is `34F566CC5716B637F40794911AC517A2801D6065173E31710FC411C0144DCDC2`. This follows the ordered Batch8 lint/types/unit/build and corrected smoke recorded above; final script syntax, embedded JSX, scoped lint and diff checks also passed.
- The dark-mode action investigation was a real native-input distinction, not an adapter callback defect: a tap while residual kinetic scrolling remained produced pointer/touch events but no click, stopping the fling. The final fixture uses a native controlled drag with a brief hold before lifting, requires actual latest scrollend plus400ms stable geometry, then verifies exactly one trusted action tap and callback. No extra tap, forced click or post-preparation programmatic scroll masks the result. Both locales reached native scrollend (EN103/103, AR100/100), with no accidental dismissal/stuck swipe. Actual mouse swipe, touch Close, immediate Alt+T indicator, natural LI→button keyboard navigation, exact44px targets and strict4.5/3 contrast all passed.
- **D17 is Verified for its stated adoption/paint/placement criteria**, not for universal notification UX or whole-app visual approval. Actual provider ownership, native descriptions/actions/cancel/status paint, long Arabic wrapping, no-shell and all five shell presentations, viewport/nav clearance and simulated24px safe-area handling are covered. Physical device notches, backend authentication, font delivery, all routes and deployment remain excluded. D08 stays Open for broader partial/permission state adoption. Six parent-run screenshots are under `C:\Users\hp\AppData\Local\Temp\edeviser-batch8-visual-0pn8zV\`; they are captured, not visually reviewed or approved.
- **Batch9/G02 partial V09 implementation:** both Playwright configurations now disallow snapshot updates/ignores and CLI overrides; RTL uses exactly its matcher-resolved snapshot path, with no module-load directory writes or missing-reference first-run pass. It separately asserts actual route, Arabic language and RTL direction. Visual reduced motion uses the supported context API. Parent's first scoped run found a Node filesystem mock-default interop failure (21 cases unexecuted); the isolated mock was repaired without exposing real filesystem writes. Rerun: **51/51 tests in2 files passed** (2.82s). Child scoped ESLint and strict TypeScript passed. Actual Playwright collection/live execution and reviewed capture workflow remain pending; **V09–V12 are not closed**, and prototype capture still needs migration.
- **G05 assets prepared, not consumed:**11 unchanged font binaries in `src/assets/fonts/` total481776B: Source Sans3 Latin/Latin-ext normal+italic176856B, Plus Jakarta Sans Latin/Latin-ext normal49076B, Noto Sans Arabic Arabic-only weight-variable165960B, and optional OpenDyslexic four WOFF faces89884B. Each passed HTTP/MIME/signature/length/published-SHA256 and written-file checks. The canonical `public/font-licenses.txt` contains source/version/hash/Unicode/axis contracts and full notices; it preserves both Adobe's embedded Source reserved-name attribution and OpenDyslexic's embedded CC BY3 attribution, not merely the wrapper-package license. Notice SHA256 `727045D7E78D6A2B11C184ED0C5646DE3143BCC0E972B703C93A856E401897C7`. No package dependency or CSS/provider integration was added. These bytes are neither initial network-transfer measurements nor proof of browser decoding/glyph consumption. The delivery/consumption/settings work remains open.
- Parent reran zero-warning scoped lint and diff hygiene after the mock repair. The18-path policy/font-preparation runtime resolver reports **Deployment Impact: NONE; Runtime feature(s): []; Affected functions: []; Shared runtime changed: NO; Production action required: NO**, with no resolver errors or required Supabase deployment. Font consumption and frontend distribution are not attested by this backend-only result.
- Fresh student progress inspection confirms the fabricated5/12 fallback counts, fixed20-minute AI claim, missing course links despite comments, and summary's disabled dependent-query error/stale-cache problem. A bounded data-owner repair has started separately; page/localization migration and new integrated proof remain pending. No academic calculation or backend policy change is authorized.

### Round13 — truthful student progress and consumed typography

- **Student data owner implemented:** `useStudentProgress.ts` now exposes matching-row `attainmentRecorded` and `recordedCourseCount`; recorded0 with0 samples remains a real measurement. The academic summary owns a canonical `outcomeAttainment` query, refreshing its base with `fetchQuery`/`staleTime:0` inside the query function instead of depending on another React observer. Assignment/deadline read failures propagate. Existing averaging, rounding, bands, strongest/weakest selection and BOTH `scope:student_course` predicates remain unchanged. Eighteen actual QueryClient tests verify errors/retry, fresh-cache bypass, broad invalidation, late-account isolation, absent-ID guards and calculation/query preservation; Supabase DTO responses remain test seams, not live backend proof.
- **Student main screen migrated:** the stable routed facades now lead through `features/student/progress/index.ts` to `StudentProgressScreen.tsx`. It uses shared PageHeader/PCard/SectionCard/StatePanel and neutral definition-list metrics, not a page-specific paint recipe. Removed invented5/12 counts and20-minute AI advice. Missing/invalid measurements are not failing0 scores; incomplete aggregates/band totals are withheld without changing their canonical calculations. Native localized numbers, recorded sample counts, actual course-ID links, the existing tutor destination, unavailable-title labels and long-code/name wrapping are implemented. The lowest outcome is actual numeric evidence, not a progress/growth bar or institutional risk classification. Thirty-four bilingual component tests cover pending/paused/error/undefined/no-actor/empty/zero/unrecorded/partial/invalid/untitled/mismatched-focus states and real callbacks/navigation. A test fixture's own output element created a second implicit status; the assertion was correctly scoped to the actual translated partial-status message, not removed. The separate progress rail, full authenticated journey and large-list performance are NOT certified by this main-screen migration.
- **D08 shared state contract implemented:** optional real action content, partial/polite status and permission/alert join the existing loading/empty/error API. One PCard owns actionable surfaces; actions remain outside live/busy regions, and the standalone loading skeleton remains compatible when no action exists. Twenty-eight actual pattern tests pass, including EN/AR defaults and twelve action/state cases. New narrow/large-text browser proof is still pending, so this finding is Implemented, not newly Verified.
- **G05 font consumption implemented:** one `fonts.css` registry references the eleven licensed originals with exact styles/weight ranges/Unicode subsets and swap display. Body/sans/sidebar/auth consume Source Sans3; headings/brand retain Jakarta; Arabic uses Noto. OpenDyslexic is an optional Latin reading face, with229 eligible codepoints excluding private-use E000; it does not claim Arabic support. The guarded loader validates all four actual declared CSS faces, deduplicates loading, retries failures and rejects stale completion after disabling. Direction management owns dir/lang only. One critical Source Sans3 Latin-normal preload replaces Google font links; no dependency or CSP widening. Existing startup device-preference replay is connected; a new accessibility settings UI/profile persistence implementation is NOT implied.
- **Actual font proof:** `scripts/audit/font-delivery-regression.mjs` passed eight cold built-auth scenes—EN/AR × light/dark × stored reading off/on—on the rebuilt artifact. Real CDP platform-font observations show custom Source Sans3/Jakarta/Noto/OpenDyslexic glyphs at actual intro/headline/brand consumers. ALL observed glyph fonts must be expected custom families, not merely one matching font. Arabic punctuation legitimately uses the Latin fallback. HTTP status, same-origin hashed URL, MIME, binary magic, length and published SHA256 checks pass; preload and CSS resolve to one URL fetched once. Normal EN requests2 fonts, AR3; enabled reading requests exactly4 additional faces, disabled reading0. No external-font request, duplicate optional request or observed preload warning. This is not proof for every glyph/style/route, interactive preferences, mobile typography, warm service workers, total initial transfer, LCP or visual approval. The test preview servers closed after each run.
- **Ordered parent gates collected:** `pwsh-49` first passed lint/types,807 unit files/7908 tests+1 existing skip,4493-module build and smoke, followed by real auth font tests. After the final unavailable-title/short-link refinements, `pwsh-52` repeated the full ordered gates: **807 unit files,7910 passing tests +1 existing skip (168.99s),4493-module production build (13.74s),22859-character local smoke, exactly2 isolated deployment-only Vercel collectors**. The strengthened eight-scene font audit was rerun successfully against this final build. Migration-ledger missing-input stderr remains the known negative CLI test, not a runtime migration failure. No thresholds or skips were added to obtain these results.
- **Artifact identity:** main `dist/assets/index-D0XJsvqw.js`,1480629B/451.81kB gzip, SHA256 `CA2481EF396C4971AD9DCDE555C968FF6CBFBBE6757EE2D4CB5F94A67C27451F`; CSS `dist/assets/index-D1cjQYV6.css`,229020B/36.26kB gzip, SHA256 `AB069DB736DD13CA7F0339F1AFEA33B8E2CDCB2DC9588D022CB395D60A242B27`. Built `font-licenses.txt` is41890B and retains SHA256 `727045D7E78D6A2B11C184ED0C5646DE3143BCC0E972B703C93A856E401897C7`. These are local hermetic artifacts from the existing dirty workspace, not a release or asset-transfer budget pass.
- All8 locale namespaces pass parity (common965, student720 leaves);29-path scoped runtime resolution and diff hygiene pass. **Deployment Impact: NONE; Runtime feature(s): []; Affected functions: []; Shared runtime changed: NO; Production action required: NO** within the resolver's Supabase scope. Frontend release/independent deployed verification remain separate.
- **Browser integration pending:** the extended shell suite targets114 cases, preserving112 earlier cases and replacing only the former weakest-outcome bar probe with actual numeric score/4.5 text-contrast proof. New student/state matrices cover real components with query/auth seams. The first run exposed a fixture-only cross-drive CSS import/rebase problem (C: temporary wrapper importing F: source produced invalid font URLs), not a production-font defect. The fixture now imports the real stylesheet as its original module and injects only its existing source-scanning directive in a scoped build transform; all11 hashed fonts emit without unresolved URLs. The real variable font's internal family alias `Source Sans 3 ExtraLight` must be recognized without allowing system fallback. No114-case success is claimed yet.
- **Subsequent browser diagnostic/source fix:** both updated Round9 groups pass21 scenes/13 fields per theme (twelve real bars plus the actual37% score), and strict custom-font gates pass. At320px/root20, native Tab brought a long course link only to bottom1000.421875 in a1000px viewport; its3px focus frame was also clipped. The exact44px target requirement passed, but viewport arithmetic tolerance was NOT relaxed. Shared `index.css` now gives the actual document scrollport a focus gap/sticky-header padding, focusable controls a block scroll margin, and mobile MAIN content the shell's own nav/safe-area clearance. Fixed navigation controls are deliberately excluded from that extra end margin. Seven shell source-contract tests and scoped lint/diff checks pass. This CSS change postdates `pwsh-52` and its hashes: native zero-clipping/five-point occlusion checks, full browser integration and fresh final gates remain pending.
- **Next accessibility ownership work is source-reviewed, not implemented:** only startup device application is consumed today. The dormant panel/hooks have a shared query key, deferred initial read, late `getUser()` mutation targeting and unacknowledged/unsynchronized optimistic writes. Plan one root owner using existing auth ownership-token identity and one shared `ProfileDropdown` dialog; do not duplicate it in five role pages or institution settings. Keep intentional device fallback distinct from account hydration, do not copy remote account preferences into global device storage, and expose local-save/account-sync/font-load outcomes separately. Simplified view currently has no real consumed target; do not claim effective all-role focus mode or mount its orphan second provider. Motion and contrast still require actual connected behavior/portal verification. No backend/profile schema/auth policy change was made.

### Round13 follow-up — full-matrix failure retained, not waived

- Child's targeted `pwsh-57` passed both student themes (78 actual states each) and both updated Round9 progress groups; five Node tests including the parent passed in172.53s. Native Tab now keeps the complete3px focus ring inside the viewport at zero edge tolerance; exact44px targets, center/four-mid-edge occlusion checks and native touch/navigation pass even for320px/root20, long names/codes, missing labels and one-letter links. The production fix is shared native scrolling clearance, not a forced-focus/scroll test repair. Browser script handoff SHA256: `EDEB257DB355875AEE648F9E2EF3A90554B9F18473A8D35792FFF8D076DF2EA7` (subsequent diagnostic work will create another revision).
- Parent `pwsh-58` re-executed ordered lint/types, **807 files/7911 passing unit tests +1 existing skip (162.04s)**,4493-module build (13.76s), and22859-character local smoke with exactly2 isolated Vercel collectors. The entire **114-case browser run executed with zero skipped/cancelled cases:112 passed,2 failed**,846.70s. Both new student groups, both actual notification/attendance groups, progress/shared-pattern/contrast checks and all80 role-locale-theme-width navigation cases passed. The failing leaf is **Round9 onboarding/dark**; the second failure is its enclosing aggregate test. All20 onboarding interaction scenes and5 guard assertions completed, but the collector recorded a failed same-origin Jakarta font request. That failure remains enforced; the existing collector omitted the browser error reason, so cancellation versus transport failure is not yet asserted. Diagnostics/readiness repair is underway without ignoring aborted/font requests, raising thresholds or claiming the full matrix passed.
- Real Playwright CLI collection now lists all5 intended RTL tests with their correct role/surface names. Six actual CLI negative checks—both configs × `--update-snapshots`, `--ignore-snapshots`, `-u`—reject before execution with the exact policy error and expected exit1. These commands used listing mode/a console reporter, not browser execution, seed setup, authenticated sessions or baseline capture. V09 still lacks the reviewed application capture workflow; collection/override evidence does not close it.
- Remaining guideline review items include `src/features/student/progress/StudentProgressScreen.tsx:87` (unbounded course-list rendering needs large-list verification/strategy) and `index.html:7` (static brand theme-color is not synchronized with light/dark page surfaces). These remain within the existing shared/performance milestones, not silently treated as completed design work.

### Round14 — complete browser recovery; accessibility owner deliberately not mounted

- **`pwsh-67`:114/114 browser tests passed**, zero failures/skips/cancellations,853493.7892ms. This includes156 student scenes across both themes, prior toast/attendance, all Round9 groups, shared patterns/contrast and the80 role/language/theme/width navigation cases. Final browser script SHA256 is `B9633296A3CA7346F954F2B984F0C92C16987D5F849CC22F063335591DFB4F96`. The old failure was reproduced in `pwsh-61`: resizing a departing narrow baseline scene revealed the desktop brand and began a Jakarta request immediately before navigation, producing `net::ERR_ABORTED`. The repair navigates before resizing and waits for actual mounted layout, font readiness and request settlement on arrival/departure. All request failures remain fatal with error reason/type/scene/timestamps; no abort whitelist, fixed sleep, threshold relaxation or reduced case count. Bounded `pwsh-62` passed before the full rerun. These are bounded component/shell scenes, not212 authenticated route journeys or visual approval.
- **Shared accessibility foundation implemented:** `accessibility.css` owns paired light/dark high-contrast semantic surfaces and one destructive-button adoption, without blanket descendant paint overrides. It owns one reduced-motion backstop; the contradictory100ms exception is removed. `portals.css` adopts only existing official dialog/sheet/dropdown/submenu/select/tooltip slots, with1000/1010/1020/1030 layer tokens above existing100–130 chrome. Low-priority dialog/sheet scroll defaults preserve smaller caller constraints. Normal theme colors, fonts, focus-scroll and toast11000 remain intact. Real Tailwind compilation and101 unit contracts pass:90 opaque text pairs≥7,30 boundaries≥3 and specified hover/alpha/icon checks; an initial2.983 icon blend was corrected before freeze, not rounded up. This is not all-consumer AAA or modal behavior certification. Legacy Popover without a slot, uninstantiated families and concurrent tours/nested stacks remain unresolved.
- **Reading/display UI implemented but UNCONNECTED:** one `PreferenceToggle` pattern uses a real44px-minimum Shadcn button with switch semantics and one clickable label area; no generated switch-thumb surgery or role-specific variant. Reworked `AccessibilitySettingsPanel` sends partial intent to the owner and separates account-read, account-save, device-save and actual font-load outcomes. Four genuine preferences are exposed; the unconsumed simplification mechanism is not advertised. `ReadingDisplayDialog` is a controlled sibling-ready dialog with localized header/footer close controls, a bounded inner scroll region and surviving-trigger focus restoration. EN/AR strings are matched; public legacy preference keys are retained. **28 actual panel/dialog tests pass**, including label activation, partial payloads, pending-font OFF, private-error non-disclosure, independent retries, Escape and focus return. A noninteractive tabindex lint failure was repaired by removing that extra stop; native controls already make the scroll region keyboard reachable. Browser-native all-role menu/dialog reachability and full large-text geometry are still pending.
- **Parent combined verification:** scoped lint and **129/129 foundation/panel/dialog tests** passed. Earlier75 scoped data/font/profile-journal tests passed the first owner handoff, but that result does NOT certify the subsequent repair. The provider and its public adapters are intentionally not mounted; `App.tsx`, `AuthProvider` startup replay and `ProfileDropdown` remain unchanged this round. Integration must move—not duplicate—the startup owner, bind dialog opening to exact lifecycle, and connect stored-motion OR OS preference through MotionConfig.
- **Independent review prevented unsafe integration.** The first unmounted owner correctly handled actor tokens/leases, dirty-read guards, serialized same-account writes, storage failure, font cleanup and exact five-known-field acknowledgement. Further review exposed: (1) a partial edit before account data could overwrite untouched known preferences with device defaults; (2) unknown JSON could be sent but a lossy acknowledgement still accepted; (3) an older read/error could discard metadata from a newer acknowledged write; and (4) a same-account return could read before its predecessor write and later overwrite an untouched choice. Repairs/tests are active: field-intent journaling, submitted-JSON preservation checks, confirmation-version fences, and a fresh owned read after entering each serialized write. Public full-object mutation retains replacement semantics. Hydration must never become a device-storage write. No backend/schema/RLS/live-state claim is made; do not mount or describe this data layer as verified until corrected source and causal regressions are reviewed.
- **Final bounded recheck:** UI scoped lint and129 tests passed again after copy/label refinements (2.35s). All8 locale namespaces match, including990 common leaves. The runtime resolver examined the20 changed paths: no affected groups/functions/shared-runtime paths, `deploymentRequired:false`, no errors; scoped `git diff --check` passed. **Deployment Impact:NONE (backend); Runtime features:[]; Affected functions:[]; Shared runtime changed:NO; Backend production action required:NO.** Frontend rebuilding/publishing and final-source integrated proof remain separate and pending.
- **Next:** collect/review the corrected owner tests, run fresh ordered whole-repository gates, then connect the single provider/shared menu control and exercise real browser lifecycle, portal, font, high-contrast and motion states. Font preload/network/artifact and runtime-impact checks must be refreshed against the final rebuilt snapshot. No deployment, cleanup deletion, auth policy change or production data mutation occurred.

### Rounds15–16 — connected shared controls; native-browser and auth repairs

- **Connected, not deployed:** `App.tsx` now places one `AccessibilityPreferencesProvider` below Query/Auth/Language/Theme, around the router and overlays. `AccessibilityMotion` delegates to real MotionConfig with `always` for stored reduction, otherwise `user`; it never disables the platform preference. The old Auth startup accessibility replay was removed in the same integration. Root-ancestry tests and actual MotionConfig context/draft-preservation cases were added. No orphan FocusModeProvider or unsupported simplified-view switch was mounted.
- **Preference owner reviewed before connection:**43 initial corrected owner cases plus45 existing regressions passed in `pwsh-78`; independent source review cleared field-intent rebasing, complete submitted-JSON acknowledgment, metadata fences, settled superseded queries and native per-account mutation ordering. The provider is stable/unkeyed, with a memoized controller, private owner-symbol/lease stamps and distinct lifecycle mutation keys. It does not remount router/forms on auth settlement. A later OS-motion review found that generated CSS animations still depended only on the stored flag: the same rendering owner now listens to live OS changes and applies stored OR OS to the one CSS backstop. OS events do not save preferences, query accounts or reset fonts. `pwsh-83/84`: scoped lint/types and91 tests passed, including46 owner cases and cleanup/late-event/no-write checks.
- **Shared account presentation:** the account menu now uses native Router links, actual theme radio semantics, localized account naming and one reading/display entry. Student level/XP and teacher totals are not invented while missing/failed; genuine zero is locale-formatted. Stock portrait fallbacks, signed-URL rewriting and the redundant tiny header XP gauge were removed without changing their queries/math. Account details move into the menu. One actor-identity gate applies before profile display and role-query enabling. Native profile destinations, institution entry, tour action and auth methods remain.
- **Real browser failures led to shared repairs, not relaxed tests.** `pwsh-82` found composed DialogClose focus at1.929 light/2.367 dark, and a320/root20 header trigger extending to325px (mirrored to−5px in RTL). Radix asChild replaced the button data-slot, bypassing a slot-only focus rule; native control semantics now own the opaque3px outline, while higher-specificity menu/select rules keep their inset3px outline. The old50%-alpha canonical light ring measures1.986:1 on white; opaque measures4.258:1. The true icon-sized account trigger avoids the default button's conditional icon padding. Header height is3.25rem—still52px at root16,65px at root20. `controls.css` owns minimum44px/rem menu/select targets, logical radio indicators and keyboard focus; `portals.css` retains its narrow layer/scroll responsibility. Generated primitives were not edited.
- **Bounded browser proof:** `pwsh-86` passed all six NONFONT groups plus aggregate in172.679s:24 appearance cells (light/dark × normal/HC × EN/AR ×390/1280/320, with root20 at320), ten real regular-layout scenes (five roles × desktop EN/light/normal and mobile AR/dark/HC), and four persistence/ownership scenarios. Actual shared menu/dialog, native keyboard/Select/Escape/return focus,44px targets, full3px rings, ancestor clipping, five-point hit testing, normal-text4.5 contrast and focus3:1 were checked. Actual read/write/storage failures, pending-read partial intent, unknown JSON, subtree continuity and live OS motion were exercised through ordinary DTO/native-storage seams. This is not full AuthProvider/backend verification, all-route coverage, all-consumer AAA or visual approval. Font work was explicitly excluded, not silently passed.
- **Font retry was genuinely broken:** real Chromium retained an errored CSS-connected normal400 OpenDyslexic face after Retry, while the other three were loaded. Native loading/off/warm-on/glyph cases had passed before that failure. `pwsh-88/89` isolated that calling load again or assigning the same source did not retry; replacing the exact failed canonical CSS rule at its original index did. The probe observed four cold requests plus only the failed URL retried. A CSS-owned repair and dedicated native regression are in progress; CSS URL/descriptor ownership, loaded-sibling resource reuse, OFF/late-activation and all11-face behavior must be verified before final acceptance.
- **Exit lifecycle is a separate boundary from data ownership.** Radix can retain outgoing DOM during an exit animation; rebinding that DOM to a new context could let a former actor's visible control dispatch a new actor's handler. Only the ephemeral menu and reading dialog are lifecycle-keyed—not the application subtree. A causal unit test first proves an unkeyed native Radix exit remains connected under a controlled animation-name seam, then requires old switches/theme items to detach and not dispatch. This test caught a first implementation using duplicate sibling keys, which retained menu/body locks; keys are now namespaced `menu:` and `reading:`. The corrected test passed in `pwsh-90`; final browser coverage must also assert one account trigger and complete exit/lock cleanup.
- **Actual AuthProvider review found a mixed-identity window:** an uncached B session could be published while A's profile remained visible. The bounded frontend repair tracks the published profile owner, clears foreign identity and holds loading before B's read, retains same-actor refresh continuity and prevents token refresh from prematurely releasing B's pending load. Eight actual-provider DTO regressions passed in the parent's `pwsh-90`, including stale A/ABA, cached B and missing/error outcomes. This is source/test evidence, not a claim of a live leak. One older language test expected A's profile field before the new owned read settled; it is being reconciled with the stronger absence-during-load and acknowledged-language-after-hydration contract. Existing auth/API/policy/cache/journal/analytics contracts remain protected.
- **Gate status:** `pwsh-80` passed250 focused tests before connection. `pwsh-81` passed whole-repository lint/types, then8111 tests across811 passing files, with3 failed legacy MotionConfig-location assertions in2 files and1 existing skip; build/smoke did not run. Those assertions now follow the adapter and retain OS protection, alongside new real-context/root tests. `pwsh-90` passed67/68 selected cases; its sole remaining failure was the premature language-profile expectation above. No all-green final-source build,114-case rerun, complete seven-group reading run or rebuilt-auth font audit is claimed yet. Original audit counts remain28 Verified/42 unfinished; these additional repairs are milestone work, not whole-product completion.

### Round17 — principal architecture review and agent discoverability

The user requested an honest assessment of scalability, future full redesigns, industry practice and AI-agent usability. The [principal strategy assessment](../architecture/frontend-design-system-strategy.md) records the actual hierarchy, remaining ownership/enforcement gaps and staged proposals. It is not another completion checklist or a claim that Storybook, MCP or DTCG tooling has been installed. Official DTCG repository version metadata, Storybook AI/manifests documentation (explicitly preview), and shadcn registry/RTL documentation were checked directly; the search connector was unavailable.

Immediate documentation repairs: root/nearest AGENTS now route frontend work to the design-system README; README maps the actual font, accessibility, control, portal and preference owners and distinguishes import choices; PARITY explicitly records historical reference rather than overruling current semantic/RTL/accessibility requirements. Seven documentation/link/authority tests, scoped lint and scoped diff checks passed. No generator configuration, new runtime dependency, protected steering file, visual baseline or production state changed. The review found that ordinary ESLint and the separate source-text design scanner do not amount to enforced semantic adoption; no general component explorer/catalog was found in the inspected project surfaces. Actual pattern escape hatches and prototype styles still need migration. Names alone do not prove a duplicate implementation.

Meanwhile, the corrected auth-language and suspended-exit suites passed58/58 in the parent (`Round16 pwsh-3`). The font specialist froze the real CSS-owned retry with22 unit tests and native Chromium proof covering all11 faces, exact unchanged registry text and only the failed font URL re-requested. The first parent full run of this round (`pwsh-6`) stopped at two test narrowing errors; those were corrected and the child's scoped TypeScript passed. The subsequent `pwsh-9` run passed lint/types, then8133 tests with1 existing skip across814 files, but three tests timed out in the full run; build/smoke did not run. All44 cases in those three suites passed an isolated two-worker diagnostic in10.09s with their original deadlines unchanged. This suggests execution contention but does not establish a clean integrated snapshot; a complete bounded-worker rerun and the browser matrices remain necessary. Original verified-finding counts and the nine unfinished milestone boundaries are unchanged by this assessment.

### Round19 — fresh integrated baseline and full bounded browser recovery

- **Parent ordered gates PASS:** `pwsh-1` ran lint → TypeScript → all unit/property tests with `--maxWorkers=2` → production build → smoke → locale parity.814 files passed;8136 tests passed,1 pre-existing skip;309.94s. No deadline, assertion, coverage or browser threshold changed. The4501-module build completed in16.48s. Local smoke rendered22859 root characters and isolated exactly2 Vercel collector scripts; service workers were blocked, so neither deployed telemetry nor offline behavior is attested. All8 namespaces match, including991 common and720 student leaves.
- **114/114 shell/browser matrix PASS:** parent `pwsh-4`,917578.6631ms, zero failures/skips/cancellations. All156 student-state scenes,168 attendance-state scenes, toast scenarios, Round9 fields/onboarding, shared patterns,420 normal14px enabled Button samples and80 role/language/theme/boundary navigation cells remain exercised. Screenshot captures under `C:\Users\hp\AppData\Local\Temp\edeviser-batch8-visual-RoLMye\` are captured/unreviewed—not visual approval. Shell script SHA256 remains `B9633296A3CA7346F954F2B984F0C92C16987D5F849CC22F063335591DFB4F96`.
- **Reading/display all7 groups PASS after fixture synchronization repair:** the parent first attempt (`pwsh-2`) passed actual font retry but failed other groups when a menu item lost focus between a transient focus assertion and geometry measurement. Installed Radix schedules arrow movement asynchronously; the old loop could queue additional arrows before the previous destination settled. The corrected protocol establishes first enabled-item focus and awaits each actual ordinal destination, with owner/route/single-profile traces and an exact arrow-count assertion. No programmatic focus, scroll correction, fixed sleep, tolerance or production-source change. The diagnostic old-loop bounded run did not reproduce the failure, so the historical mechanism remains a source-backed race inference, not a claimed captured reproduction. Child full run `pwsh-9` with CI=true passed8 tests including aggregate,132.001s, zero failures/skips. Final script SHA256 `00A2C4E847A10CCDEEAEC8A232175F742C45A949AF314ABFC7A3F1902ADCB1C9`. It includes24 appearance cells,10 actual five-layout scenes, preference failure/retry/ownership cases, raw portal/lock teardown including ABA/logout, live OS motion without writes and actual font cold/loading-OFF/warm/retry glyph behavior. Auth/DTO seams and the lack of whole-route/visual certification remain explicit.
- **Rebuilt public-auth font scenes PASS:** an initial invocation omitted the mandatory URL and correctly stopped before testing. Parent then served the built app temporarily at `http://127.0.0.1:4199`, ran all8 scenes with the required URL (`pwsh-8`), and stopped preview job `pwsh-6`. EN reading-OFF requests2 fonts, ON6; AR OFF3, ON7; optional faces exactly0/4, warnings none. Actual Source Sans3/Plus Jakarta/Noto/OpenDyslexic glyphs and retained brand faces were observed, with existing byte/MIME/magic/hash/preload checks. This is local built public auth, not production/deployed/immersive/all-route typography attestation.
- **Exact built snapshot:** `dist/index.html`1988B SHA256 `BF3A1FD90EA98A4CE9CEA6C843F82364FB6DBA0F7BDC4AC7123DE7F93DE832FC`; `index-Qv3-wQst.js`1506153B SHA256 `2F3AFA5C649A6F6588E64BCB1DA7863BAD2DA2C3B967504729CA5DBF10ABD994`; `index-DCOzjhLl.css`234501B SHA256 `530E3628D3C526C4765CA610A684FA71C0E03E48E8586A666AA9857E0389E1C7`. The build uses hermetic localhost/fake-key environment values and is not a production-release configuration. Emitted sizes and Vite gzip estimates are not measured initial transfer. The large-main-chunk warning remains an open performance concern. Font loader SHA256 `B1E1351FEB3A5EBBC53EC4B95DA0668BA98D14EE8D2C767CB6F833194590008C`.
- **Deployment review:** resolver evaluated29 changed frontend/tooling/documentation paths, no errors; groups/functions/shared-runtime paths empty. Deployment Impact:NONE (backend); Runtime features:[]; Affected functions:[]; Shared runtime changed:NO; Backend production action required:NO. Frontend publication still requires its separate authorized release. Scoped diff check passed. No schema/RLS/RPC/auth-policy or production-data mutation occurred.
- **Evidence retention:** PowerShell transcripts retained commands/final markers but did not capture all native Node stdout in this environment; they must not be described as complete logs. Collected tool results provide the recorded matrix results above. The built-font JSON observations were additionally persisted through Tee-Object at `C:\Users\hp\AppData\Local\Temp\edeviser-frontend-round19-built-fonts.log`. A previous round's job registry was unavailable on continuation; it was not credited as a pass, and no surviving verification process was found before the fresh ordered run.
- **Approved PR02 work started after the UI baseline:** seven-entry source-derived catalog/examples/generator and17 tests were added without UI/CSS/barrel/package/dependency/CI changes. Parent first execution passed22/24 catalog+documentation cases; two valid temporary fixtures failed because cross-drive TypeScript library paths were misclassified as repository inputs. Repair also covers realpath/junction/write-target boundaries, evidence-file allowlists and appropriate input-digest scope. Catalog acceptance remains pending; its new source/tooling changes require their own fresh gates. Original audit count28/70 is not a claim that the additional approved report work is complete.

### Round20 — continued approved architecture and truthful-role work

PR01's inline entrypoint guidance was also corrected: the primitive and pattern barrel headers no longer instruct agents to treat prototype fidelity as the current design authority. All exports/order/implementations were preserved. Scoped lint, seven documentation/authority tests and scoped diff checks pass. These comment/documentation repairs do not certify remaining pattern implementations.

**PR02 pilot now has scoped verification:** parent execution passes27 catalog cases plus7 documentation cases (34 total), and native `--check` passes all7 entries. Actual TypeScript exports, aliases, props/optionality/JSDoc and typed examples are extracted without executing application modules; native inherited props are explicitly omitted and CVA variants retained. The initial cross-drive fixture failure is fixed. Containment now checks real paths, portable dependency provenance uses `npm:` paths, and both fixed output targets are preflighted against links/junctions/nonfiles before either write. Evidence references are allowlisted/existence-checked but are not falsely treated as execution or API-content inputs. Precise negative tests cover these boundaries. One remaining fixture expectation assumed same-line member JSDoc was exposed by TypeScript; formatting that fixture as ordinary multiline JSDoc preserved the documentation assertion without changing the generator or inventing a fallback. This is a seven-entry pilot, not a complete approved inventory, rendered gallery or CI-wide semantic-policy gate.

**Bounded parent-dashboard truthfulness cut implemented:** unchecked reminder/encouragement inserts and unconditional success handlers were removed. The existing intentions remain visibly unavailable with localized explanations and real notifications/support links; no scheduler, immediate-note substitute or claim of global backend absence was invented. Fabricated database-course and Satisfactory-milestone copy in those two sections was replaced by generic child-named support copy. The help section uses the existing semantic card surface. Aggregate/calculations/child selection/routes are unchanged. Child scoped lint/types and15 actual-component cases pass (12 new bilingual availability cases plus3 existing section cases). This is not whole-dashboard localization, all account-action repair or delivered-reminder functionality. The full parent post-change run passed lint/types/catalog checking, then8174 tests plus1 existing skip across816 files; its only failure was the now-stale cast allowlist entry for the two removed dashboard inserts. Build/smoke did not run. That allowance was subsequently removed and its maximum reduced from150 to148;31 focused cast/dashboard tests pass in the hermetic environment. A fresh full run and actual new-section browser proof remain separate.

### Round21 — portable catalog CI and notification cache isolation

- Removed the stale ParentDashboard insert-cast allowance and reduced the shrinking-only maximum from150 to148 after the two unsafe inserts were removed.31 focused cast/dashboard cases pass with the explicit fake loopback environment. An earlier CI-only invocation printed happy-dom fetch teardown errors: source review confirmed the guard requires both CI and the loopback URL, and some older tests leave a read hook unmocked. The fake-environment rerun was clean; no warning was suppressed and broader non-Supabase HTTP isolation remains G02 work.
- Catalog text hashing/comparison and extracted API documentation now normalize physical CRLF/LF, preserving literal escape sequences.29 catalog cases cover full-fixture line-ending conversion, actual API drift, alias/type/JSDoc extraction, path/target safety and honest partial scope. These are normalized UTF-8 text fingerprints, not font/binary hashes. Named npm check/generate commands were added. The existing CI Type Check workflow invokes only the read-only check, never automatic generation. The unit CI job now always uses fake loopback credentials rather than deployment-secret fallbacks; Preview RLS and build configuration were not changed. YAML parsing and3 new wiring tests passed. Hosted CI/branch-protection execution is not claimed.
- A real frontend cache collision was repaired with one production line: communications now uses `notifications.list({ userId, scope: 'communications' })`. The Bell's limited raw array, communications' full mapped aggregate and the numeric unread count no longer share an incompatible cache entry. Both warm mounting orders failed before the fix;13 real QueryClient regressions plus5 existing Bell tests pass afterward. Deferred/concurrent order, legitimate cache hits/dedupe, actor partition and all existing notification-prefix invalidations were exercised. Query functions, limits, API/mutation payloads and the unrelated unsafe announcement fallback remain unchanged; no backend claim is made.
- **Full ordered parent run PASS (`pwsh-5`):** lint → TypeScript → named catalog check →818 unit/property files,8193 passed tests +1 existing skip (315.09s) →4501-module build (14.64s) →local smoke (22859 characters, exactly2 isolated Vercel collectors, service workers blocked) →all8 locale namespaces, including1002 common leaves. Deadlines/thresholds unchanged. The CSS remains `index-DCOzjhLl.css`; current main entry is `index-6vk7nq9q.js`. Large-chunk/asset performance concerns remain open; emitted sizes are not initial transfer. The durable native-output log is `C:\Users\hp\AppData\Local\Temp\edeviser-frontend-round21-gates.log`.
- Source review/resolver examined22 changed application/catalog/test/workflow paths: no affected backend groups/functions/shared-runtime paths and no errors. Deployment Impact:NONE (backend); Runtime features:[]; Affected functions:[]; Shared runtime changed:NO; Backend production action required:NO. This does not attest hosted CI or a frontend deployment. The new main entry SHA256 is `6A51B2DEE2B618B29A144A040203E1A402261CEBAD78654EEBEA5A1027E342A2`; CSS SHA256 remains `530E3628D3C526C4765CA610A684FA71C0E03E48E8586A666AA9857E0389E1C7`, unchanged from the earlier verified styling snapshot. Scoped diff checking passed.
- Expanded parent browser coverage exposed a genuine long-name wrapping failure at320/root20: a35-character mixed-script first-name token extended beyond its paragraph bounds despite good contrast. Only the two repaired action-section containers gained `min-w-0` and inherited overflow wrapping; no name shortening, type-size reduction, palette change or threshold relaxation.17 scoped parent cases pass. Child full8-group run then passed9 tests including aggregate in150.738s, zero failures/skips, preserving all7 earlier groups and adding12 actual parent-dashboard cells. Actual route facade/regular layout/read hooks use ordinary DTO seams; claims cover the two repaired sections, native links/history and zero writes/false successes—not destination bodies, whole-dashboard localization, visual approval or backend/RLS. Final script SHA256 `48F2468C2FF7301CF4A41BF8615615382DF57FABF8D461040C9D5A84996C1181`.
- A fresh, conversation-independent agent found the current guidance/catalog, chose PCard/SectionHeader/StatePanel/Button instead of compatibility defaults, and created four authorized fixture/test files only. Its caller-owned course-results example preserves null versus zero/native results, EN/AR copy and mixed-script names. Scoped lint/types and19 real-component/keyboard/isolated-axe cases passed; parent combined execution passed109 relevant cases and catalog checking. This is one discovery/source/DOM benchmark, not broad AI reliability or rendered geometry/contrast/font approval.
- A diagnostic production build after the wrapping/benchmark changes passed4501 modules in13.49s but proved an artifact-isolation gap: the benchmark was not runtime-imported, yet one unique flex-basis utility added47 CSS bytes. The emitted CSS became `index-DTKO_Rxi.css`234548B; removing only that emitted rule restores234501B and SHA256 `530e3628d3c526c4765ca610a684fa71c0e03e48e8586a666aa9857e0389e1c7`. This confirms Tailwind source detection is independent of runtime tree-shaking. Production source-boundary isolation remains to be repaired and verified; the earlier full818-file gate snapshot must not be presented as post-benchmark acceptance.

### Research follow-up after the user's web-access update

Two fresh web-search calls still timed out after60 seconds each, so no search-result discovery or broad market-comparison claim is made. Direct primary-source fetching now succeeds, including the DTCG technical-report index and published2025.10 Format/Resolver modules. The strategy document was updated with confirmed stable CG-report status, explicit typing/reference/extension requirements, context-resolution order, Storybook's still-preview manifest curation/dev-server caveats, shadcn's RTL migration exceptions and Tailwind's text-based source detection/exclusion controls. These sharpen existing PR04/PR06/PR08/PR09 requirements; they do not install tools, waive conformance tests or create a new goal. The previously unavailable DTCG website is no longer described as unavailable. Current local catalog implementation is distinguished from DTCG token and Storybook manifest formats.

### Round22 — premium visualization research inside the agreed scope

The user requested senior research on making Edeviser's visualization-heavy frontend more premium, including justified library imports, then returning to the existing goal. The [premium visualization strategy](../architecture/premium-visualization-strategy.md) records the results. The dedicated search tool timed out, but public DuckDuckGo HTML searches successfully returned discovery results; relevant capabilities, release caveats and licensing were checked against official project/W3C sources. This is a selected-source research comparison, not authenticated competitor usability testing, screenshot approval or an Edeviser performance benchmark.

Recommendation: keep Recharts and TanStack Table as defaults, establish shared visualization framing/tokens/formatting/truthful states, and use task-focused role views. Selective virtualization is conditional on measured workload. visx/ECharts are specialist candidates only for demonstrated gaps; AG Grid's advanced commercial capabilities need a concrete workflow and license decision; current TanStack Charts Alpha is a watchlist/isolated experiment, not a default production replacement. No new chart/grid package or commercial license was installed/purchased. Source review confirmed unconsumed chart-token declarations, repeated local chart recipes and localization/sizing/state gaps. Installed Recharts already has OS-aware animation defaults and an accessibility layer; the report does not erase those capabilities or claim that they alone prove application accessibility. The existing server-page/local-sort mismatch remains an application contract to repair with current table capabilities, not a reason to invent new backend policy.

The research extends implementation guidance for the same G03/G04/G05/G06/G08 work. It does not replace the master objective or its evidence requirements. Next implementation resumes with the measured benchmark CSS-isolation gap and remaining governed visualization/component migrations.

### Round23 — benchmark CSS isolation and first visualization implementation

- Added one narrow negative source declaration in `src/index.css` for the development benchmark fixture directory. Source inspection found only its test/own-file references, not runtime consumers. No benchmark files, active prototypes, other test sources or valid utilities were deleted/blanket-disabled.
- Real Tailwind compilation confirms exactly that negative source and leaves automatic runtime detection active; an explicit-candidate positive control still generates the valid utility. The CSS-entry/benchmark suite passes23 cases and scoped lint passes. This protects the fixture for future explicit test/explorer builds while removing its automatic production contribution.
- The production build (`pwsh-40`) passes4501 modules in13.71s and restores **exactly234501B** `index-DCOzjhLl.css`, SHA256 `530E3628D3C526C4765CA610A684FA71C0E03E48E8586A666AA9857E0389E1C7`—the pre-benchmark styling snapshot. This is actual build evidence, not an assumption from lack of runtime imports. It is a bounded isolation/build result, not a new full-route/visual/deployment claim.
- Implementation of the first governed visualization slice is now assigned: a compositional shared VisualizationFrame and migration of the actual StudyTimeChart planner consumer using existing Recharts, semantic style, localized truthful states/data alternatives and stored-plus-OS motion. Existing minute/hour and average rounding, order and domain contracts must be preserved. No new renderer/dependency was approved or installed; source and browser gates remain pending for that separate change.

### Round24 — local unit-network isolation independent of CI flags

The earlier Supabase guard activated only when both CI=true and the fake loopback URL were supplied. The unit/property configuration now owns fake URL/key values in `test.env` for local and CI execution, preserving the existing Qatar timezone. Unit setup always installs a small idempotent exact-origin guard. The separate Preview RLS configuration has no unit setup and was not changed; production build environment selection was not moved into the unit policy.

New tests cover process/import-meta values, installed protection, string/URL/Request inputs, no duplicate wrapper stacking, fake native forwarding and separate RLS wiring. Parent deliberately ran with CI=false and an external reserved `.invalid` URL/key:26 network/parent cases passed, scoped ESLint passed, and a TypeScript program including Vite config, setup, helper/test and transitive declarations reported0 diagnostics. This closes the omitted-CI-flag Supabase path, not general network sandboxing: other HTTP still requires explicit mocks. No external request or live database operation was used as proof. Full integration must be refreshed with the pending visualization changes.

### Round25 — governed visualization connected; full source gates green

The new shared `VisualizationFrame` uses one PCard/named figure, a caller-owned summary, stable disclosure IDs, real data alternative and scoped SVG keyboard-focus recipe. `StudyTimeChart` remains on the existing Recharts engine and is still consumed by WeeklyPlannerPage. It now uses semantic chart/axis/reference/tooltip presentation and relative type, nineteen matched EN/AR labels, validated Gregorian date-only UTC formatting and localized numeric hours. Supplied records/order and the existing minute-to-hour/average rounding remain unchanged; average is not recomputed from rows. Empty, zero and nonfinite/unavailable values are distinguished. Defined courseFilter values are controlled; undefined retains local selection without silently filtering supplied data. Stored motion reduction selects false; otherwise Recharts retains its native auto/OS behavior. Data disclosure and long-label wrapping are real components, not screenshots or mocked chart replacements.

Parent execution passes15 visualization cases plus9 network cases. The first full run passed8225 tests but failed four older planner cases because their isolated page fixture did not provide the new public preference-data seam. That fixture was corrected without replacing StudyTimeChart/VisualizationFrame or weakening the production owner requirement;24 focused planner/chart/frame cases then passed. Catalog artifacts were explicitly regenerated for reviewed source dependencies; the existing seven-entry adoption scope was not automatically expanded to approve the new frame.

**Corrected ordered parent run (`pwsh-56`) PASS:** lint → TypeScript → catalog check →821 files/8229 passed tests +1 existing skip (296.65s) →4502-module build (14.36s) →local smoke22859 root characters/exactly2 isolated collectors →all8 locale namespaces, including1021 common leaves. No deadlines or thresholds changed. The first failed run did not build. The corrected native-output log is `C:\Users\hp\AppData\Local\Temp\edeviser-round25-viz-gates-corrected.log`.

Current built snapshot: `index-CY-H13GV.js`1509582B SHA256 `4664738DA35669074A1B83F954351923B3DFE74788ED2AE0D8B88D7374005B29`; `index-DZnFYGZR.css`235023B SHA256 `096A06A7F9A333E2E09B6A437FB874D0A98AE48A14D6DC4F654058980D3819F6`. These are emitted assets from a local fake-environment build, not initial transfer or a production configuration. The main-chunk warning remains open performance work.

Resolver review of14 visualization/unit-environment/catalog paths returned no errors or affected backend groups/functions/shared-runtime paths: Deployment Impact:NONE (backend), Runtime features:[], Affected functions:[], Shared runtime changed:NO, Backend production action required:NO. No schema/RLS/auth policy or academic classification changed. A ninth bounded real-Recharts browser group is being prepared for chart/SVG/tooltip focus, paint, reflow, data alternatives, controlled filters, actual motion and locale behavior. Those properties and whole-planner/whole-product visual acceptance are not established by the current source/DOM tests.

### Round26 — native chart sizing and semantic page theme color

The first real chart diagnostic passed initial EN/light/390 data/axis/reference/tooltip/focus checks, but strict diagnostics caught Recharts' initial ResponsiveContainer negative-size warning. Installed source confirmed default negative sentinels before effect-based measurement; this was not an observed permanently collapsed chart. Rather than accept warnings or supply guessed dimensions, the chart now uses the installed, documented native responsive BarChart API inside the same CSS-sized parent. Data, axes, calculations and tooltip behavior are unchanged. Unit measurement targets the actual native wrapper; no production dimension mock was added. The bounded native-chart rerun subsequently passed real resize/keyboard/tooltip checks without a warning exception; all sizing warnings remain fatal. The complete nine-group matrix still awaits a fresh combined static gate.

A separate small frontend owner now synchronizes the page's theme-color metadata from the actual root semantic background, including dark/high-contrast changes. It shares a scoped observer across leases, disconnects on final release and restores only metadata it still owns. It neither persists a preference nor defines a second palette. The initial HTML fallback matches the canonical light background and is guarded by a source test. Nine native-DOM/source tests cover palette changes, root overrides, missing CSS, created metadata, shared/duplicate cleanup and later-writer preservation. Combined meta/chart/frame tests pass24 cases with scoped lint. This is metadata/source behavior, not physical browser-toolbar or whole-page visual attestation. The earlier full R25 snapshot predates these changes. Selected-root TypeScript reported0 diagnostics and the resolver found no backend deployment closure for the six changed paths.

Child bounded browser run `pwsh-71` passed the visualization group in45.56s (58.918s including build):12 appearance cells plus EN/AR edge-state, actual motion and resize cases. Essential bars/reference strokes meet3:1, axis/tooltip text4.5, and native SVG focus retains its full3px outline with2px offset. Actual coordinates preserve the supplied99-minute average as1.7h and121/153-minute records as2/2.6h, with input order, zero/missing/invalid/negative cases explicit. Native directional keyboard tooltip movement and controlled filter behavior passed. Frame observations distinguish normal intermediate bar positions from OS/stored reduction; actual metadata matches the computed root background. No sentinel-warning waiver or fabricated dimensions were used.

Four captures remain **CAPTURED_UNREVIEWED** in `C:\Users\hp\AppData\Local\Temp\edeviser-viz-captured-unreviewed-9u7OmG\`. The parent attempted direct image inspection; the tool explicitly rejected it because the current model does not declare image input. Therefore no visual review/approval is claimed from the captures, numeric measurements or source inspection. Full nine-group and post-table combined integration gates remain pending.

### Round27 — source-policy kernel and deeper table/KPI contract review

A pure TypeScript-AST design-policy kernel now analyzes supplied source snapshots and compares finding multisets without filesystem/Git/runtime execution, baseline writes or automatic approval. Parent's first25-case run exposed a malformed CVA test fixture; syntax validation correctly rejected it. After that fixture was repaired,25 tests passed. Imported-helper aliases/shadowing, literal branches/templates/spreads, ordinary-copy/comment exclusions, actual paint/spacing/type candidates and counted additions/removals are exercised. This is not a repository/CI policy yet and explicitly does not resolve opaque dataflow. A five-file source probe showed no supported candidates in PCard/SectionHeader/VisualizationFrame/StudyTimeChart and a fixed-type candidate in the old KPI card; absence of candidates is not semantic approval. The probe also exposed a missed arbitrary-shadow literal because Tailwind encodes spaces with underscores, so bounded decoding/regressions are being added before the kernel is accepted for integration.

The Course/server-table migration remains in progress. Review identified two additional shared-contract hazards to test before acceptance: unknown total counts must not silently switch caller-owned page rows into client pagination, and stable Course row IDs must prevent stateful row actions rebinding to another record after server reordering. Pending rows must not claim requested sorting as already applied, grouped headers need correct leaf-only sort semantics, and pending visual treatment must retain readable contrast. No passing result or full server-table migration is claimed yet.

The KPI audit identified13 canonical caller modules/48 tiles, including5 currently unreferenced DashboardNew modules that must not be deleted by name, plus3 separate active local KPI implementations/10 tiles requiring follow-up. A bounded canonical migration was authorized: finite tone/value-state/surface choices instead of arbitrary paint/class overrides, static semantic PCard composition, real zero/native values preserved, explicit/nonfinite-unavailable presentation, and neutral mastery/coverage metrics rather than copying a generic fixed-threshold classifier into another component. Existing explicit risk/security/outstanding conditions remain; no academic calculation or native result is redefined. The old attainment helper and unreferenced shared KPI file remain outside this phase. Source and tests are still being implemented; catalog promotion and rendered proof must follow.

### Round28 — arbitrary-paint policy regression closed

The pure policy kernel now decodes Tailwind arbitrary-value whitespace for paint inspection while retaining original tokens/fingerprints, escaped underscores, URL contents and variable identifiers. Inline CSS is not subjected to Tailwind decoding. Parent execution passes27 cases, including the formerly missed RGBA-shadow/gradient patterns and semantic-reference negatives. Syntax, scoped lint and strict TypeScript passed in the implementation handoff. The new kernel README documents its input/output contract and limits: it is not a repository scanner, a CI gate, full dataflow analysis, computed contrast or automatic approval of existing debt. The former source-probe shadow recipe is retained as an assembled regression fixture; the canonical KPI source is changing, so that old result is not represented as a current-file finding.

Table review additions are being tested before acceptance: stable entity row IDs, retained row-action identity across reordering, unknown-count caller pagination, readable pending state and correct leaf-header sort semantics. The canonical KPI migration is also in progress under the agreed finite API/caller scope. Neither migration nor a new combined full-source/browser snapshot is marked complete yet.

### Latest direct progress request — reconciled status and dependency permission

The user explicitly permits installing justified dependencies that will actually be used within the agreed redesign direction; this does not authorize indiscriminate library installation or commercial-license purchases. The [master-goal status snapshot](./frontend-master-goal-status.md) lists achievements, all41 unfinished original findings, the additional architecture requirements, current test boundaries and why no reliable completion date is yet available. This ledger remains the sole execution tracker. Reconciliation marks the already-tested V14 unit Supabase boundary Verified, bringing the original count to29/70; it is not a project completion percentage.

A separate read-only repository-policy adapter now has16 passing temporary-repository/argument tests alongside27 kernel tests (43 total), with scoped lint passing. It explicitly reports authored-source exclusions, rejects empty/malformed/link-escaping input, requires distinct comparison checkouts, retains existing debt and never writes a baseline. It is not connected to CI. Course/table implementation has42 scoped passing tests; canonical KPI implementation is on disk but its latest reported ordered check stopped at test TypeScript expectations for absent locale keys, before unit/browser acceptance. The last fully green whole-source snapshot remains R25 until current changes pass a fresh combined run.

## Master goal and nine acceptance milestones — user-requested round10 register

The existing persisted master goal remains active; these are its tracked milestones, not nine competing goals or new report files. Current original-audit counts are29 Verified and41 unfinished; that denominator is not a whole-product completion percentage. The complete route inventory is212 leaf declarations plus5 layouts and20 supplemental ownership rows, not212 rendered/approved pages.

| Milestone | Current state | Completion gate |
|---|---|---|
| goal-01 — Inventory and evidence | Current declared-route inventory complete; maintained as code changes | Every route/alias maps to its actual owner; deeper sections/states and residual findings remain accounted for, not hidden by aggregate counts. |
| goal-02 — Trustworthy verification | In progress | Finish V09–V17 collection/baseline/network/seed/report/performance-gate issues; missing evidence fails closed and no unchecked baseline becomes a pass. |
| goal-03 — Shared design architecture | Foundations verified; migration in progress | Actual consumers use one governed theme/token/primitive/pattern contract; remove page/role/card islands and competing recipes without editing generated primitives. |
| goal-04 — Five-role surface and truthful-state migration | In progress; scoped attendance, shared-state and student-progress evidence recorded | Migrate all admin/coordinator/teacher/student/parent sections and relevant public/auth surfaces; truthful loading/error/empty/unknown/partial/permission states, accurate controls and localized copy. No fabricated KPIs or fake successful writes. |
| goal-05 — Typography and accessibility | Shared fonts/settings connected; bounded glyph/interaction proof recorded; final-source and whole-app proof pending | Source Sans3 body, retained Plus Jakarta display/brand, Noto Arabic and optional Latin-only OpenDyslexic must feed one consumed contract. Require actual glyph/delivery proof, connected needed settings, large-text reflow and purposeful accessible motion. Asset presence is not delivery or usage proof. |
| goal-06 — Navigation, interaction and lifecycle | In progress | Finish navigation aliases/rails, tables/carousels/portals/focus, institution-aware registration, cache/account/child/offline ownership and telemetry lifecycle without weakening auth/domain contracts. |
| goal-07 — Safe retirement | Two unchanged duplicates removed; remaining candidates gated | Preserve modified originals, prove import/export/dynamic/data/CSS/asset/tool dependencies, migrate prototype tooling and reviewed baselines, compare build/behavior, then isolated deletion plus tested rollback. |
| goal-08 — Complete rendered and performance coverage | Scoped matrices passing; whole-product coverage incomplete | Real required routes/states in both themes/ENAR and relevant narrow/desktop/large-text contexts; keyboard/touch/focus/contrast/visual review and measured initial/route performance, not just shell fixtures. |
| goal-09 — Final integrated and release handoff | Pending all preceding gates | Ordered lint→types→unit→build→smoke→browser, locale/runtime-impact/diff checks, reconcile every required finding, exact artifacts and recovery plan. Local redesign completion is separate from authorized deployed verification/customer readiness. |

### Approved principal-report requirements — same master goal, revision33

The user explicitly instructed: **“based on your findings above the whole report add that into goal and fix those all.”** The full [principal assessment](../architecture/frontend-design-system-strategy.md) is now accepted implementation scope within goal-01 through goal-09, not optional background advice. This table extends this ledger rather than creating a second checklist. The original28/70 audit count remains historical scope; it must not be used to imply these additional requirements are completed.

| Report requirement | Milestones | Current state | Required acceptance evidence |
|---|---|---|---|
| PR01 — Current authority and discoverable ownership/imports | 01,03 | Partly implemented;7 scoped documentation checks pass | Current guidance and inline examples agree; canonical versus compatibility imports are explicit; all relevant owners are mapped without treating every shared/Admin/Hawdex name as a duplicate. |
| PR02 — Approved component/recipe catalog | 01,03,07 | Seven-entry pilot verified by29 source/tooling tests and read-only CI wiring; broader classification/deprecation coverage remains | Small validated catalog tied to actual exports, source-derived API details/examples, recommended/compatibility/deprecated status, replacements and precise verification scope; no hand-maintained duplicate prop truth. |
| PR03 — Semantic component APIs and truthful view states | 03,04,05 | Migration in progress | Remove uncontrolled paint/hover/fixed-type islands and misleading unknown-as-zero behavior at actual consumers; use finite semantic choices; domain classification remains outside generic UI. |
| PR04 — Token hierarchy and interoperable build path | 03,09 | CSS authority retained; 16-color Format 2025.10 export-only pilot with read-only CI drift implemented; full hierarchy/Resolver/HC path pending | Reference/semantic/component boundaries, a documented tested DTCG-compatible path for portable tokens, one authoring source and generated-output drift checks; contextual layout expressions remain in the appropriate recipe layer. |
| PR05 — Executable design policy in CI | 02,03 | Catalog/token drift in CI; AST kernel/repository adapter plus explicit-baseline local no-growth summary pass. Event-SHA trusted scanner CI and semantic/import/logical enforcement still pending; no fake required gate. | Semantic/import/logical-CSS checks with meaningful new-violation enforcement, explicit reviewed generated/domain/legacy exceptions and real-repository evidence; no permanently blessed baseline or blanket exclusions masquerading as compliance. |
| PR06 — Real-source component explorer and state examples | 02,05,08 | Eight real-pattern Storybook10.6 stories plus local/CI fake-key static build,120 closed-network scenes and32 scoped axe runs; broader states/actual providers/auth routes/human-reviewed visual baselines pending | Focused reused-pattern pilot, actual CSS/environment providers, required states, interaction/accessibility checks and reviewed visual evidence; expand alongside route migration, not a duplicate implementation gallery. |
| PR07 — AI-readable documentation and demonstrated agent usability | 01,02,08 | Entry links and seven-entry generated catalog implemented; one19-case fresh-agent benchmark passes; broader tasks/rendered proof pending | Source/types/examples feed machine-readable discovery; fresh-agent representative tasks find approved APIs and pass the same gates without hidden instructions. Pin/validate preview integrations and retain a tool-independent fallback. |
| PR08 — RTL/direction and generated-primitive migration | 03,05,06 | Scoped runtime repairs present; generator migration pending | Version-pinned review appropriate to existing new-york configuration; actual direction, popup, keyboard, icon and animation checks; no blind rtl flag or hand-editing generated custody. |
| PR09 — Predictable cascade and responsive composition | 03,05,06,08 | Partial shared adoption | Document and verify layer/adoption ownership including unlayered/important behavior; use container-responsive composition where justified and eliminate role/page override contests. |
| PR10 — Future redesign, versioning and distribution safeguards | 07,09 | Specified | Consumer/deprecation maps, bounded token/component/workflow change scopes, migration/codemod and rollback contracts. Record evidence-based MCP/registry/package adoption or not-applicable decisions; do not add distribution infrastructure without a real need. |

Optional-tool decisions may be explicitly deferred/not applicable with evidence; mandatory architecture and product acceptance work may not be deferred to claim completion. No framework rewrite, microfrontends, per-role libraries, competing styling framework or runtime LLM-generated UI is implied by this approval. Deployment remains separately authorized.

### Retirement queue and timing

- Already removed: `src/components/shared/SectionHeader.tsx` and `src/components/shared/Shimmer.tsx`; canonical patterns retained. Dependency/Tailwind/byte-identical CSS and full Batch7 gates are recorded below.
- Next candidate batch, not yet approved as removable by name: the five shared `AdminDashboardNew.tsx`, `CoordinatorDashboardNew.tsx`, `TeacherDashboardNew.tsx`, `StudentDashboardNew.tsx`, `ParentDashboardNew.tsx` and shared `SeverityIcon.tsx`. Earlier scans found no incoming edges, but these originals contain pre-existing modifications; preserve verified recoverable bytes first, then refreshed CSS/tool/API proof and an isolated removal batch.
- Prototype deletion follows—not precedes—replacement of prototype-dependent capture/maps/baselines, absent-tree artifact-detector tests and generator claim cleanup. Respect the dedicated cleanup gate; do not mix prototype deletion into feature additions.
- Same-name TS/TSX facades, active route wrappers, Hawdex's active Logo/public API and production mascots are not deletion targets merely because names overlap. Hawdex HBadge/HButton/theme-map ownership still needs consolidation proof. Current orphan accessibility components need an explicit connect-or-retire decision against product requirements, not automatic deletion.
- Repository references/archives are not automatically deployed assets. Unimported source can still affect Tailwind output; emitted images are not necessarily initial downloads. No claim of zero production risk: local isolation, measured gates, controlled release and rollback reduce risk. No frontend deployment or production data/schema change is authorized by this queue.

## Architecture and design direction

Canonical ownership stays with `src/router/` for routing/guards, `src/app/RoleAppShell.tsx` for the shell, `src/components/ui/` for accessible primitives, `src/design-system/primitives/` for their facade, `src/design-system/patterns/` for shared compositions, and `src/features/<role>/` for role compositions. Data access remains in hooks; framework-free domain behavior stays in `src/lib/`.

Use shared Precision—Light and Obsidian—Dark foundations with explicit profile-scoped semantic overrides, not five independent component systems. A system theme preference selects one of the two themes; marketplace cosmetic metadata is a different concern. Keep authentication's identity and flow. Edeviser's intelligence surfaces should expose evidence, provenance, suggestion, human review, supported execution state and result; do not invent autonomous writes, confidence values, or new backend capabilities.

Role IA candidates must map every existing route and alias before migration: Student Today/Learn/Progress/Community/Me; Teacher Today/Classes/Planning/Assessment/Learners; Coordinator Overview/Curriculum/Assessment Quality/Improvement/Accreditation; Admin Overview/People/Academics/Operations/Governance; Parent Overview/Learning/Updates/Account with verified child context. `advisor` is a sharing recipient, not a sixth authenticated role.

## A. Verification credibility and reporting

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| V01 | Verified | `scripts/design-lint/check.mjs`: all checks and Main were nested inside an uncalled function's catch block. | Real guarded CLI restored; clean/violation/error exits verified. All 21 new Vitest cases passed in the focused and full suites. Source-policy occurrence triage remains separate. |
| V02 | Verified | Design lint launched `powershell.exe` and used fragile glob/output parsing. | Replaced with sorted `node:fs` traversal, exact token/exemption boundaries and fail-closed I/O behavior. Current Windows tests pass, including POSIX/Windows path forms; another OS runtime has not been claimed. |
| V03 | Verified | Accessibility tests previously substituted string assertions for contrast math and confused 14px with 14pt bold. | Opaque-sRGB math, correct 24px / 56/3px-bold thresholds and known-bad fixtures pass. Existing token-pair coverage retained; normal-text failures are explicit in D13, not mislabeled as rendered contrast compliance. |
| V04 | Verified | Browser findings previously lived in worker-local memory while global teardown flushed another process's buffer. | Fresh setup identity, immutable per-scan files in project output roots, validated current-run merge and explicit zero-scan coverage. Real Chromium/two-worker temporary fixtures and full unit suite passed. |
| V05 | Verified | Axe scans did not fail their specs. | Persist and attach evidence before throwing at the existing Major-or-higher threshold. A real unlabeled-button fixture was rejected and retained in merged evidence; clean fixture and full unit suite passed. Authenticated app accessibility is not attested. |
| V06 | Verified | Static accessibility stage and browser teardown both wrote `a11y-findings.json`; default audit order could overwrite browser evidence. | Static output is now `a11y-static-findings.json`; browser filename unchanged. Tests verify browser bytes survive static reruns and both artifacts aggregate/render exactly once. Full suite passed. |
| V07 | Verified | `scripts/audit/findings.ts` captured output cwd at import; tests change cwd later. | Destination and returned relative path now use cwd at call time. Real two-workspace tests pass; an ESM-safe mock guard prevents a regression from writing into repository reports. |
| V08 | Verified | Student touch-target spec previously logged small controls but only asserted URL. | Strict 44px box/fail-empty enforcement, measurement attachment and documented exclusions. Fractional/zero-size/incorrect-exclusion failures verified in real Chromium; authenticated student-page compliance is not claimed. |
| V09 | Open (approved RTL baselines pending) | The RTL spec now resolves one `testInfo.snapshotPath` for existence and comparison, rejects missing refs and snapshot-update/ignore CLI overrides. Mocked policy cases/collection do not constitute actual authenticated Arabic screenshots or reviewer approval. | Collect authenticated Qatar Preview/fake-data role scenes under governed V15 prerequisites, obtain reviewable baselines and inspect EN/AR paint without auto-approving references. |
| V10 | Implemented (isolated local browser verified; hosted gate pending) | The auth-expiry spec was outside every root project, mutated cookies although this SPA uses Supabase localStorage, and admitted zero refresh/tautological redirects. | The new named `playwright.auth-local.config.ts` collects **exactly three** cases with no Preview setup/teardown, role storage or configured webServer. Its self-owned Vite fixture imports actual Supabase JS, AuthProvider and RouteGuard with fake loopback origin and closed browser network. Three real Chromium cases pass: one expired-session refresh + new token/role, one revoked refresh + storage/route clearing, one empty storage + no refresh. Source/CI wiring tests pass; exact-head hosted Linux execution and actual full AppRouter acceptance remain pending. Root Preview-seeding config is deliberately not run or changed (V15). |
| V11 | Implemented (source policy; real scene acceptance pending) | Five active dashboard prototype comparisons used to skip missing references, accept absent storageState as anonymous, and screenshot redirected/unknown-theme pages; an empty active map gave a green placeholder. | Missing reference/role state, zero active entries, wrong final route, non-EN/LTR/light root or unresolved FontFaceSet readiness now fail before comparison; candidates cannot overwrite historical refs. Four fixture tests and21 collected visual entries prove policy wiring only. Authenticated role scenes, app-owned reviewed baselines, both themes and EN/AR remain unverified. |
| V12 | Open | Exactly five dashboard rows ×4 viewports still compare to historical prototype PNGs with `maxDiffRatio:0.6` (60% mismatched pixels). The capture command now writes exclusive unreviewed candidates outside committed refs; obsolete design-truth/pixel-perfect prose was corrected, not converted to app approval. | Add application-owned reviewed baselines and fail-closed route/state/role/theme/locale matrix; measure paint and auth/data scope. Do not waive mismatch or promote prototype captures automatically. |
| V13 | Investigate | Vitest uses happy-dom and `css:false`; unit tests do not prove layout, focus containment or contrast rendering. | Browser checks remain required; do not report DOM-unit success as visual success. |
| V14 | Verified (scoped unit Supabase boundary) | Unit network guard depended on CI plus a loopback Supabase URL. | Unit config now fixes fake process/import-meta URL/key values locally and in CI; setup installs an idempotent exact-origin guard. Nine regressions, an explicit CI=false/external-env run and the R25 full suite pass. Preview RLS remains separate. Other HTTP still requires mocks; this is not a universal network sandbox. |
| V15 | Implemented (read-only provenance guards; real Preview validation pending) | Root Playwright setup used to overwrite six role storage files with empty states when disabled; opt-in relied on env `preview`/URL labels, and teardown trusted run ID plus those labels. Legacy CI ran login specs against any non-default secrets-based URL. | Legacy-only selection now performs no fixture writes; audit selection without authorization fails before file/browser/seed. Opted-in setup makes a read-only Management API branch lookup requiring `git_branch` + `pr_number` + exact HTTPS Preview ref at `FUNCTIONS_DEPLOYED`, preserves existing role-state bytes and only then can POST seed. Teardown requires setup-owned runId/ref and rechecks the same linked target before POST; errors fail, not warn-and-pass. CI E2E job is explicitly fake-loopback **collection-only** pending governed Preview integration. 18 synthetic guard cases and existing harness contracts passed; no real Preview mutation, migrations/role execution or deployed attestation is claimed. |
| V16 | Implemented (bounded replacement; historical pack and acceptance pending) | The old generator hardcoded “Finalized”, “100% GREEN”, live RPC/RLS, complete audit and “Secrets Redacted: Yes” while recursively bundling broad source into PDFs with insufficient redaction. | Replacement reads only the bounded dated route ledger, refuses implicit/in-repo/existing output and emits three atomic files in a new separate directory. Every inventoried route is explicitly UNVERIFIED; zero executed checks, verified/skipped/failed counts and untested readiness dimensions remain distinct. Legacy PDF/CSV bytes are preserved with an explicit do-not-distribute README and corrected historical Markdown disclaimer. Five synthetic tests and no-args negative pass, but no RLS/live/browser/secret review, safe PDF redistribution or full codebase certification is claimed. |
| V17 | Implemented (fail-closed wiring; hosted/collection verification pending) | Both root `lighthouse` and CI step swallowed LHCI errors with `|| true`; their green job was not performance evidence. | Local follow-up uses one `npm run lighthouse` command in both places, with no `|| true`/`continue-on-error`; error-level accessibility, best-practices, SEO and total-byte assertions retain their exact values, while performance scores/timings remain warn-only. Three source contracts and an executed missing-config negative exit pass. A real local 3-run collection failed after run1 with Chrome temp cleanup EPERM (no assertion/upload pass); verify exact-head Linux CI and accountable reports before marking Verified. |
| V18 | Verified | Executed baseline lint reported 8 errors and 3 warnings: extracted reference source was scanned, two dashboard constants were unused, Hawdex mixed exports triggered warnings, and `.eslintignore` was unsupported. | Migrated the existing one-line reference-only ignore to flat config; removed only the two unused constants; separated Hawdex components/data behind stable extensionless facades. No lint suppressions. Full lint is zero-warning; public export/Logo tests and full type/unit/build gates pass. |
| V19 | Verified | Local bundle smoke failed on Vite HTML fallbacks for the two deployment-provided Vercel collector scripts; service-worker-owned requests also bypassed page routing. | Exact same-loopback-origin GET-script isolation plus explicitly worker-blocked network-bundle scope. Eleven real Chromium scenarios and rebuilt-app smoke passed; ordinary app failures remain enforced. Offline/PWA and deployed telemetry are explicitly not verified. No production telemetry change; I07 remains open. |
| V20 | Verified | Scoped config type-check found seven unsupported `use.reducedMotion` values; student also inherited WebKit despite its documented Chromium matrix. | Same values moved into `contextOptions.reducedMotion`; student explicitly uses Chromium and parent retains WebKit. Nine config contracts and full suite pass; importing config from a unit test now includes it in root TypeScript checking. |

## B. Shell, navigation and responsive accessibility

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| S01 | Verified | `RoleAppShell` and `Sidebar` previously both mounted `MobileTabBar`. | Shell-only ownership; all five roles expose one appropriate navigation landmark in real responsive browser cases. |
| S02 | Verified | Closed mobile sidebar was only translated offscreen, without modal/focus semantics. | Closed content unmounted; Radix modal, keyboard trap/Escape, actual touch dismissal/navigation, background hiding, focus restoration and resize closure pass. No route-guard change. |
| S03 | Verified | `MobileTabBar` comments described obsolete breakpoint/flag behavior. | Actual below-640px, shell-owned contract documented and browser boundary checks pass. |
| S04 | Verified | Header-owned English labels/small targets/RTL transform; real hit-testing additionally found the desktop brand link covered by the sidebar. | Localized header/brand names, minimum targets, one exposed clickable shared brand and English/Arabic 1440px centering pass. Ancillary header widgets are outside this fixture's claim; remaining student stats localization is D11. |
| S05 | Investigate | `navItems`, presentation and mobile maps can silently drop destinations through intersection; missing admin institution settings and repeated labels. | Explicit route-to-navigation contract, preserve aliases, role-by-role destination and label tests. |
| S06 | Open | Student journal rail mapping is unreachable after an earlier rail-free branch. | Resolve intended layout per route without changing onboarding, immersive quiz or focus routing. |
| S07 | Investigate | Router has one broad error boundary; route-loading spinner is unlabeled. | Accessible loading and appropriate page-level failure containment, without guard-policy changes. |
| S08 | Open | `DataTable` pagination needs responsive refinement. | Preserve server/client pagination, sorting, `aria-busy`, empty state and mutation semantics; narrow-width/browser checks. |
| S09 | Open | Dialog portal styling is unscoped, lacks universal height/scroll containment, uses physical alignment and hardcoded Close. | Role token scope through portals, localization, keyboard focus and small-screen scrolling. Respect primitive ownership rules. |
| S10 | Verified | Mobile content clearance was split between shell padding and a body-wide `:has()` rule. | One shell-grid clearance formula shares height/overhang/safe-area values with the bar. Real final-action hit-testing passes, including raised affordances and a synthetic 24px inset; no physical-notch attestation. |
| S11 | Open | HeroCarousel source sets 26px arrows and 4px-high dot targets, unconditional negative-X translation/swipe direction, mounted inactive slides without explicit focus exclusion, and no explicit auto-advance pause control. | Review carousel necessity per role; for retained carousels, preserve content/actions with real touch targets, localized RTL navigation, inactive-slide focus isolation, pause ownership and reduced-motion behavior. Multi-slide rendered keyboard/touch checks required. |

## C. Themes, components and styling ownership

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| D01 | Verified | Later light defaults previously overrode equal-specificity dark heatmap/XP declarations. | Moved eight conflicting defaults before .dark without changing domain colors. Actual HeatmapGrid cells/legends/outlines/hover rings pass both themes and live round trips. XP is verified only as a painted token swatch, not an established application widget. Ordered application gates pass. |
| D02 | Open | `[data-role]` defines `--role-accent` but no consumers were found. | Explicit consumed semantic role overrides; visual isolation across roles and portals. |
| D03 | Verified | Agent ring, risk dot and cursor blink previously ignored native reduced motion. | Scoped animation:none removes the three effects, preserving visible static indicators and ring depth. Browser positive controls prove normal loops still run; initial/live reduced-motion round trips pass. No production widget consumer of these utilities is established, so this verifies the CSS contract rather than a fabricated live widget. |
| D04 | Open | `src/index.css` still needs broader semantic/logical migration. Batch 4 repaired tactile/sidebar bindings, fixed label-color overrides and explicitly removed the student sidebar's light gradient in dark mode; other hardcoded styles and physical properties remain. | Continue incremental semantic/logical migration with actual consumer/browser proof; do not bulk-rewrite existing user work or treat the bounded sidebar repair as completion of this wider finding. |
| D05 | Verified | Tactile controls previously depended on undefined action variables and private fallbacks; browser-native color-scheme was undeclared. | Both scopes define the canonical action contract; tactile consumers have no fallback hex colors. Actual computed light/dark color-scheme and interactive button paint checks pass. |
| D06 | Open | Parallel Hawdex exports: hardcoded HBadge, limited raw HButton API, unused theme map and competing patterns. | Consolidate APIs based on dependency proof. Keep active Hawdex Logo; do not delete its directory wholesale. |
| D07 | Verified | Pattern SectionHeader hardcoded light text/small heading; PCard added arbitrary radii/shadows/lift. | Canonical Card adapter, semantic wrapping headings and shared bounded actions. Browser checks exposed and repaired a shadow-merge conflict. Batch6 ordered application gates and104/104 rendered tests pass, including80 shared-pattern role/theme/locale/width/text-size scenes. This is pattern/shell evidence, not every authenticated page or real-font certification. |
| D08 | Verified (Round14, scoped contract) | StatePanel was English-only and limited to loading/empty/error with hardcoded color. | Localized partial/permission and real action slots within PCard;28 actual pattern tests and ordered unit gates pass. Full114 browser run now passes, including156 student-state scenes with native retry/navigation/touch, exact44px targets, zero-clipped focus rings and actual EN/AR text. This verifies the shared contract and tested consumers, not complete adoption in every page state. |
| D09 | Investigate | Same-basename `.ts` compatibility shims and old `.tsx` implementations need import/API proof. | Batch7 removes only two unchanged shared SectionHeader/Shimmer copies after resolved-import, asset, Tailwind and replacement checks; canonical patterns remain. Same-basename route/shim decisions and other candidates are not certified by that narrow retirement. |
| D10 | Investigate | Legacy `*DashboardNew` shared components are orphan candidates, not proven removable. | Full production/test/export/dependency proof before deletion. |
| D11 | Open | Student dashboard local ring, heat ramp and English weekday labels duplicate shared behavior. StudentHeaderStats also retains an English progress label and default-locale number formatting. | Reuse canonical patterns/tokens and locale-aware labels without changing domain values. |
| D12 | Verified | Design-system README previously said live tokens were not imported and mixed current/target paths. | Current single-graph wiring, existing entrypoints, action contract and migration boundaries are documented. Five tests validate local links and reject missing/empty coverage; barrel exports remain unchanged. |
| D13 | Verified | Before Batch 4, corrected math measured light primary/card at 4.2581:1 and destructive foreground/fill at 4.1256:1: both failed normal-text AA. | Repaired semantic pairs preserve brand anchors. Twenty-five source checks and 420 normal14px samples of seven actual Button variants pass across role/theme/surface/interaction combinations. Real cascade/alpha composition is checked; no lowered threshold or normal-as-large reclassification. Custom gradient/hardcoded consumers remain in the broader styling/role matrix scope. |
| D14 | Verified | Real shell browser checks found transparent backgrounds; the previous real app `dist/assets/*.css` also lacked `.bg-background{`. `main.tsx` imported tokens and utilities as separate CSS entries, so Tailwind could not resolve semantic utility theme names across graphs. | Import canonical tokens within the index.css compilation graph; use one JS CSS entry. Verify generated semantic classes and actual light/dark computed styling, not just token declaration presence. |
| D15 | Open (selected notification, promotion, presence, coordinator and admin PLO slices; remaining debt) | Four admin PLO legend fills plus the weak unmeasured marker now share named academic-visual band tokens with actual tinted cells; **12 other authored production occurrences plus 1 protected generated-UI occurrence** remain (one test assertion excluded). | Scoped actual-component browser matrices include the PLO legend/filtered states; no authenticated five-role route, app-owned visual baseline or native academic-scale certification. Other progress, leaderboard legends, status, tutor, dormant controls and generated UI adoption need meaning-specific source/rendered verification. Preserve PLO85/70/50 versus distinct mastery90/80/70 cutoffs. |
| D16 | Open | Expanded source audit: directionManager assigns html font; dyslexia preferences also target ancestors, but body/headings/sidebar declare their own fonts. Font preference variables are not consistently consumed; fixed pixel type also bypasses root-only size preferences. | One locale/accessibility font contract at actual consumers. Preserve Arabic shaping, user preference priority and legibility; verify loaded/computed fonts, live changes and large-text reflow rather than root-class presence. |
| D17 | Verified (Batch8, round12) | Sonner's semantic normal-color variables compete with important fixed slate/white utilities; descriptions/actions retain ambient-theme colors. Its system theme default is not explicit application theme ownership. | Use one paired toast surface and explicit theme integration through supported primitive adoption. Verify description/action/cancel/error/success paint, long Arabic text, portal placement and safe areas. Do not hand-fork generated primitives. |

## D. Frontend lifecycle, identity and authentication

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| I01 | Open | Embedded LoginPage registration omits institutionId, which AuthProvider requires. | Reuse/adapt existing institution-aware signup flow; never weaken institution validation or join policy. |
| I02 | Verified | Theme hydration was not identity-scoped and pending writes survived account changes. | Matching-profile hydration, fresh-over-cache updates until a local choice, and lifecycle-owned debounced mutations. Account switch/logout/unmount/retained-callback/queued-write/same-account re-login regressions and full gates pass. Already-dispatched server writes cannot be rolled back by this guard. |
| I03 | Verified | LanguageProvider had the analogous pending-write lifecycle gap. | Same lifecycle guard with explicit mutation owner; storage/i18n/direction synchronize through the provider API. The unsafe update cast was removed and its allowance shrank. Regression and full gates pass. Other language control/read-policy unification is tracked separately in I09. |
| I04 | Investigate | Ordinary logout does not clear singleton QueryClient; some query keys lack identity. | Prove account-switch ownership with tests, cancel stale requests and prevent stale data reuse. Source risk is not a verified live leak. |
| I05 | Investigate | Offline activity queue uses shared storage and replay may attribute entries to the current session. | Test account-switch ownership; scope/discard/quarantine appropriately without changing event/domain meaning. |
| I06 | Open | Student page-view duration is recorded on next navigation, not final-page cleanup. | Correct lifecycle including unmount/final-page behavior and duplicate-event prevention. |
| I07 | Investigate | Consent revocation behavior, Vercel telemetry outside consent wrapper, and raw invitation-token paths need review. | Preserve supported event contracts, enforce applicable consent, and redact sensitive route parameters; no token logging. |
| I08 | Investigate | Parent child context, tenant scope and role styling are distinct isolation contracts. | Cross-account, cross-tenant, role and verified-child tests; do not confuse CSS scope with authorization. |
| I09 | Verified | LanguageSwitcher/AuthProvider/provider paths previously disagreed on language ownership and legacy/preferred reads. Successful writes also left a fresh cached profile stale on remount. Both field names appear in local generated types; this is not a live schema claim. | One provider choice/read policy; confirmed matching-owner cache/context patches preserve metadata and TTL. Request-start generations protect against older responses without permanently overriding later server reads. Real React/menu tests cover save/remount, late/fresh responses, batched account transitions and StrictMode; full local gates pass. No backend-column, API or auth-policy change; live persistence/deployment is not attested. |

## E. Domain-sensitive analytics review (not a styling rewrite)

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| A01 | Investigate | Admin analytics fallback mastery and PLO thresholds differ. | Identify authoritative policy/contract and add tests; document any required domain decision before changing calculations. |
| A02 | Investigate | Fallback ignores selected date range. | Trace query/fallback contract and test requested periods without changing backend APIs. |
| A03 | Investigate | Retention denominator includes unmeasured records. | Establish policy and fixture-based expected results before any correction. |
| A04 | Investigate | Small-cohort percentage can remain visible. | Confirm suppression policy and test frontend presentation/privacy behavior. |
| A05 | Open | Department mastery/trend fallback values are hardcoded and errors ignored. | Replace unsupported certainty with honest data/partial/error states while preserving domain contracts. |

## F. Prototype retirement, routes and remaining release gates

| ID | Status | Finding / evidence | Required repair and proof |
|---|---|---|---|
| P01 | Open | Disposable `prototype/` remains referenced by visual capture config/server and documentation. | Migrate active references first, then dedicated deletion with Git recovery evidence; no in-repository replacement archive. |
| P02 | Open | Prototype artifact scanner derives some forbidden paths from the live prototype tree. | Preserve forbidden-artifact protection after the directory is absent; regression for absent-tree detection. |
| P03 | Open | Cleanup gate intentionally rejects prototype deletions mixed with production additions. | Respect isolated deletion sequencing, CI artifact scan and rollback evidence. |
| P04 | Investigate | Service-worker registration is not proof of safe offline/PWA behavior. | Test caching/update/offline states and privacy; do not claim native-app support from registration alone. |
| P05 | Open | Complete route and role coverage needs an executable matrix. | Preserve the 217 literal Route declarations, aliases, critical builders, query parameters and all five roles; declaration counts are not screen counts. |
| P06 | Open | Responsive/theme/locale/a11y/performance matrix is incomplete. | Verify 320/375/390/430/640/768/1024/1280/1440/1536+ widths as relevant, both themes, English/LTR and Arabic/RTL, keyboard/touch/zoom and degraded states. |
| P07 | Open | Duplicate cleanup and frontend deployment readiness cannot be inferred from old PDFs or local source. | Executed gates, explicit residual findings, and independent deployment attestation before any production claim. |
| P08 | Open | Executed Vite build warns on a 1,459.39 kB main JS chunk (445.92 kB gzip) and emits many mascot PNGs around 1–3.7 MB each. | Measure initial/route delivery, prune eager dependencies and optimize production-owned image delivery while preserving identity/quality. Do not raise the warning threshold or restore the previously broken manualChunks setup to hide the issue. |

### Known retain/protect decisions

- `CurriculumMatrixPage.tsx` is a live facade over `CurriculumMatrixPageNew`, not a deletion-ready duplicate.
- `ConsistencyScoreRing` has an active HabitAnalytics consumer.
- Hawdex Logo remains active through RoleBrandLink in GlobalHeader and Sidebar; retain its public facade.
- Student marketplace theme/avatar metadata is not an extra global theme to remove.
- Authentication landing assets/styles and bilingual/RTL identity are protected during refinement.
- Critical teacher assignment/grading and student assignment/XP route builders retain paths and parameter encoding.
- Production-owned mascots/assets are not disposable merely because a prototype once used them.

## Execution log

### Batch 0 — resume and baseline

Executed successfully: repository location, Git status, HEAD, Node `v26.5.0`, npm `11.17.0`.

The large dirty working tree is pre-existing at this repair baseline. Initial `npm run lint` failed with 8 errors and 3 warnings (V18); initial TypeScript failed on the two unused dashboard constants. Both are now passing after the scoped batch below. No application verification is claimed from the earlier harness smoke tests.

### Batch 1 — executed repairs and evidence

Implemented and locally verified V01, V02, V06, V07 and V18. Broader D06 design-system consolidation is still open; only its mixed-export hygiene was addressed here.

Source changes:

- `scripts/design-lint/check.mjs` now runs and exposes typed testable scanner/runner APIs; `check.d.mts` and `src/__tests__/unit/designLint.test.ts` provide contract/regression coverage.
- `scripts/audit/findings.ts` resolves cwd per write; `a11y-stage.ts` preserves browser artifacts using a separate static filename. New/strengthened findings and accessibility-stage tests cover actual I/O and aggregation.
- Migrated the user's unsupported `.eslintignore` (only `_hawdex_analysis/`) into `eslint.config.js`, then removed the obsolete file. The excluded directory is a separately packaged Figma extraction, Git-ignored by `/_*`, with no imports/references found in production `src` or scripts. The actual `src/design-system/hawdex` remains linted.
- Removed only unused `BRAND_SURFACE` declarations from Admin/Coordinator dashboard screens; existing unrelated edits retained.
- Split Hawdex exports into stable `gamification.ts` / `primitives.ts` facades plus `HexBadge.tsx`, `gamification-data.tsx`, and `primitive-components.tsx`. The old `.tsx` facade paths were relocated, not duplicated. Public extensionless imports, all exported symbols and component markup are preserved. `hawdexPublicExports.test.ts` verifies public binding identity, palette/data/cx compatibility and Logo geometry/gradient.

Execution history and final results:

1. Initial child/main Vitest attempts failed before collection on esbuild `spawn EPERM`. The main-session exact-command escalation was approved while approval policy was `ask`; no application permission logic was changed. The user later changed standing harness policy to `danger-full-access` / approvals `never`; subsequent commands omitted escalation fields.
2. The first executable focused tests exposed real test-harness issues: Node CLI imported under browser transforms and an attempted spy on frozen native ESM exports. These were fixed with a file-scoped Node environment and a narrow hoisted filesystem mock retaining real writes behind the safety guard.
3. Focused result: **6 suites / 59 tests passed**. A full TypeScript pass then caught an ES2021-only `replaceAll` in a new fixture; it was replaced with an ES2020-compatible regex without changing compiler configuration.
4. Final ordered command: `npm run lint` → `npx tsc --noEmit` → hermetic `npm test -- --maxWorkers=4`. Overall exit **0**. ESLint **zero warnings**; TypeScript **passed**; **775 test files passed, 7,287 tests passed, 1 skipped** (156.93s unit duration).
5. The pre-existing skip is `deepseekProvider.test.ts:155`, request-timeout classification. It was not unskipped or counted as passing. The migration-ledger error printed during tests is an intentional negative fixture: `runtimeDependencyGovernance.test.ts:309–314` asserts that invoking the ledger command without a path throws.
6. `npm run build` with temporary loopback Supabase URL and a fake smoke key **passed** (Vite 6.4.3, 4,481 modules; no `.env.local` edit). These are local verification artifacts, not production-configured deployment output. Size warning is recorded in P08.
7. `npm run smoke:build` **failed**, despite rendering 22,848 root characters, on two uncaught syntax errors. An independent browser diagnostic identified both HTML responses at local Vercel collector script URLs (V19). The smoke gate was not bypassed or declared passing. Its preview/browser and the diagnostic preview/browser were closed.
8. First restored design CLI scan: **1,922 files, 498 source-text occurrences** (412 icon backgrounds, 7 legacy gradients, 18 physical spacing, 61 raw brand gradients), exit **1**. This historical count predates the Hawdex file split. These are not 498 confirmed visual bugs: comments, fixtures and canonical implementations require semantic triage. No broad exemptions were added to hide them.

Unit tests used `CI=true`, `VITE_SUPABASE_URL=http://localhost:54321` and a fake key. RLS/preview mutation tests, Supabase seed/reset, production deployment and live-state assertions were not performed.

Batch 1 follow-up (superseded by Batch 2 below): fix and negatively test the local collector boundary, finish remaining fail-open accessibility/visual gates, then advance shell and lifecycle repairs.

### Batch 2 — browser evidence and preference ownership

Verified V04/V05/V19/V20/I02/I03. At Batch 2 closure the ledger tracked **64 findings/risks, 11 verified** across both batches. This is incremental progress, not completion of the broad frontend objective.

- Browser accessibility evidence now uses a dedicated fresh `A11Y_RUN_ID` established by a pure first global setup. The Preview seed's identity and guards are unchanged. Each scan writes a UUID-named file with exclusive creation, project/test/worker/retry provenance, and findings, then attaches it before enforcing the original Major-or-higher severity threshold.
- Teardown merges only the current run, validates required and optional fields with named type guards, rejects malformed/duplicate evidence, and preserves separate static evidence. A run without scans emits an explicit unverified-coverage notice instead of a misleading clean result. Existing guarded Preview cleanup remains in `finally`.
- Scoped config verification found and fixed the reduced-motion property location and student browser inheritance (V20). A config contract test now imports `playwright.config.ts`, bringing it into root TypeScript checking without running fixture setup.
- Local bundle smoke isolates only the two exact deployment-provided script paths when requested as same-loopback-origin GET scripts. A rebuilt-app diagnostic additionally proved both requests were service-worker-owned (`fromServiceWorker: true`), bypassing page routing. The local **network-delivered bundle** context now blocks service workers and explicitly disclaims offline/PWA and deployed-telemetry verification. Application script errors, HTML/MIME fallbacks, missing assets, empty root and uncaught errors still fail. No application/service-worker/telemetry source was changed; browser/preview cleanup runs on exceptions.
- `useDebouncedProfilePreference` centralizes TanStack mutations with explicit owner variables, per-owner serialization and lifecycle-identity checks at scheduling and queued execution. Review added generation/reference identity so an old callback or queued write cannot become valid again after A → logout/B → A. The corresponding same-account re-login regressions pass. Both providers cancel unsent timers at identity change/unmount and reject retained callbacks. Matching profile hydration is reset per account; fresh profile updates apply unless a newer local choice exists. Already-dispatched server writes are not claimed to be rolled back.
- The LanguageProvider unsafe update cast was removed. Its obsolete entry was removed from `supabaseCastAllowlist.json`, and the shrinking-only maximum decreased **151 → 150**; the guard was not weakened. The remaining language ownership paths are tracked in I09.

Executed evidence so far:

1. Initial focused preference/smoke unit run: **3 files / 62 tests passed**. After lifecycle-generation and service-worker-boundary review, the changed preference/smoke suites passed **2 files / 54 tests**, including 16 lifecycle cases and 38 smoke cases.
2. Final real Chromium smoke boundary: **11 scenarios plus the outer Node test passed (12 total)**. This includes the demonstrated service-worker bypass, explicit worker-blocking scope, deliberately broken application scripts, and a real navigation failure with browser cleanup. The revised local smoke then passed against the rebuilt app: 22,848 root characters, both collector entrypoints explicitly isolated.
3. Added `scripts/audit/axe-browser.regression.mjs`: an isolated temporary Playwright config runs **two real Chromium tests in two workers**, using real axe scans and the actual setup/merge helpers. The accessible fixture passed; the unlabeled-button fixture threw as expected and retained its evidence. Two unique scan files merged with one current run identity. This Node regression **passed**. Synthetic evidence was written only under the owned temp workspace and removed afterward; no application test config or Preview seed ran.
4. The first combined gate exposed two real test maintenance defects: a Node crypto mock needed a file-scoped Node test environment, and the removed unsafe cast left a stale allowlist entry. Both were corrected. Focused recheck: **2 files / 38 tests passed**, including forced collision/exclusive-create protection and the tightened cast guard.
5. `npm run i18n:check` passed across all eight namespaces. `npm run check:runtime-dependencies` passed manifest validation. An explicit resolver call for this batch's provider/hook and verification paths returned no affected backend groups/functions, no deployment requirement and no errors.
6. Final ordered lint → TypeScript → full hermetic unit suite → local test build → local browser smoke: **exit 0**. ESLint zero warnings; TypeScript passed; **780 test files passed, 7,384 tests passed, 1 existing skip** (155.57s unit duration). Vite built **4,482 modules in 12.66s**. Local smoke rendered **22,848 root characters**, isolated exactly two collector scripts, and passed with no application page/asset errors. Both service-worker/PWA and deployed-telemetry exclusions were printed. Main bundle remains **1,460.25 kB / 446.17 kB gzip**, with large PNG assets; P08 is still open. The known migration-ledger negative fixture and Node loader deprecation messages are not test failures.
7. Scoped `git diff --check` passed (Git printed only LF/CRLF normalization notices). All relevant background jobs were collected; interrupted verification attempts were deliberately stopped before source review corrections, not treated as successful checks.

No auth policy, Supabase schema/RLS/RPC, analytics event contract or domain calculation changed. Actual authenticated role/Preview coverage is still pending; passing controlled browser fixtures is not presented as production accessibility attestation.

Batch 2 follow-up (advanced in Batch 3 below): contrast/touch-target gates, shell/navigation/drawer fixes and frontend language ownership; visual-baseline/route-collection and cache/offline repairs remain pending.

### Batch 3 — real navigation checks, language ownership and CSS compilation

Current inventory: **66 findings/risks, 19 Verified**. This batch verifies V03/V08/S01/S02/S03/S04/S10 and the newly discovered D14 (eight additional closures). I09's language-control work is Implemented but stays open for save/cache/remount consistency. D13 records the newly measured normal-text contrast failure rather than hiding it behind large-text checks. The broader objective is not complete.

- V03 now uses opaque-sRGB luminance/contrast math with unrounded thresholds, scoped source-token lookup and negative fixtures. Large text means 24px normal or 56/3px at weight 700+, not 14px bold. This is source-token evidence, not compositing/cascade or rendered-text certification. The existing light primary/card and destructive pairs fail AA normal text; D13 remains open.
- V08 now measures real rendered control boxes and throws below the project's 44px minimum, including fractional failures. Nonrendered, disabled, inert and fully clipped keyboard-only controls have recorded exclusions; no blanket link/icon/offscreen/aria-hidden waiver. Empty coverage fails. The student spec attaches measurements before enforcing them. This strict per-control box policy does not infer larger associated-label targets, and the authenticated student page is not claimed to pass.
- RoleAppShell is the single MobileTabBar owner. Sidebar unmounts closed mobile content and composes Radix modal primitives for focus/scroll isolation, dismissal and background hiding; it closes on link/history navigation and desktop resize. Role scope travels with its portal. Navigation closes focus main content; dismissals restore the opener where visible. Generated shadcn primitives were not edited.
- GlobalHeader-owned labels and trigger semantics are localized; header button/brand targets have 44px minima and search centering no longer uses a direction-sensitive translation. Real hit-testing also found the desktop sidebar brand span intercepting the header dashboard link. Shared `RoleBrandLink` now supplies exactly one exposed clickable brand: sidebar on desktop, header on mobile, preserving the intended overlay order, active Hawdex Logo and all five original dashboard roots. Its domain prop is `userRole`, not the ARIA `role` prop. Bottom-navigation height, raised affordance and safe area share shell-scoped variables, with clearance applied once by the grid. The body-wide `:has()` padding rule and obsolete flag/lg comments are removed.
- LanguageProvider is now the sole production language-change/hydration path. Valid `preferred_language` wins; valid legacy `language_preference` is a fallback; absent/invalid values retain supported device/current language. AuthProvider still publishes profiles but no longer independently overwrites i18n. Signup metadata and authentication/cache/session policy are unchanged. LanguageSwitcher uses the guarded provider through actual Radix selection. Existing account-transition ownership policy and the 16 preference lifecycle regressions are preserved.
- I09 remains Implemented, not Verified. Read-only follow-up confirms the fresh profile cache window is 30 seconds and successful preference writes do not synchronize it. Next regression must cover real menu save → successful mocked response → full provider-tree remount without signout/storage clearing, plus an older revalidation response arriving after save. A narrow cache patch must validate both owner IDs, whitelist preference leaves, preserve original cachedAt/role/tenant/legacy metadata, and ignore failed/pending/stale writes. Do not reuse `readCachedProfile(A)` for a late A completion: it deletes mismatched entries and could clear B's cache. Do not use `writeCachedProfile` to patch preferences: it resets the timestamp. Storage-only repair must also account for still-mounted AuthContext and late pre-save responses before I09 can close. No such cache policy/helper changes were made in this batch.
- Real browser verification exposed D14: independent token/utility CSS imports did not generate semantic utilities. The previous local application build also lacked `.bg-background{`. Tokens now enter index.css's Tailwind graph; main.tsx loads that single stylesheet. Token palette values themselves have not been changed. Source-contract tests and emitted-CSS/browser checks guard this ownership.

Executed evidence and correction trail:

1. Initial focused shell/contrast/touch/language/provider regressions: **9 files / 130 tests passed**. New shell ownership/modal-state tests: **10 passed** after a failing assertion led to explicit `aria-modal=true` on the actual modal. Six brand/destination cases and three CSS-entry contracts were added afterward. DOM/source tests are explicitly not geometry proof.
2. `node --test scripts/touch-targets.regression.mjs`: **9 real small-viewport Chromium scenarios plus the outer test passed (10 total)**, including fractional-size failures, transformed dimensions, unsupported exclusions and empty coverage. Isolated synthetic pages only; no network/auth/Preview/report mutation.
3. First ordered full attempt passed lint but TypeScript found four implicit parameter types in the new media-query test double; explicit unknown/string types corrected them. A later full run found two obsolete source-spelling assertions (old search translation and CSS-only sidebar breakpoint); tests now reflect direction-neutral centering and a conditionally mounted desktop sidebar, backed by real browser evidence. Touched header property checks also fail on missing/empty source instead of silently skipping. The new brand component's custom `role` prop triggered ARIA lint; it was renamed `userRole`. No suppressions or weaker compiler/lint thresholds were added.
4. First real shell run rejected transparent computed backgrounds, leading to D14's application CSS-graph repair. Preflight then needed to recognize grouped CSS selectors rather than demand a class immediately before `{`; exact-name and negative lookalike checks remain. Another run passed 60 narrow-view mouse cases but failed all 22 desktop/wide brand hit tests, identifying the intercepted link. The shared-brand repair preserves desktop layering instead of hiding the failure.
5. Final-source `node --test scripts/shell-navigation.regression.mjs`: **82 scenarios plus the outer test passed (83 total)**. Matrix: five roles × English/Arabic × light/dark × 320/390/639/640px (80), plus two 1440px header-centering cases. Mobile contexts use actual touch taps for open/outside/close/link paths; keyboard trapping/Escape, programmatic navigation, resize closure, focus restoration, one exposed nav/brand, RTL placement, role-scoped portal and raised-tab/synthetic-safe-area clearance all pass. Emitted semantic CSS preflight and light/dark shell computed-background checks pass. Temporary fixture/server/browser output is cleaned.
6. Final ordered application command **passed, exit 0**: `npm run lint` → `npx tsc --noEmit` → hermetic `npm test -- --maxWorkers=4` → `npm run build` → `npm run smoke:build`. ESLint zero warnings; TypeScript passed; **787 files / 7,458 tests passed, one existing skip** (152.36s unit duration). Build: 4,482 modules, 13.47s; main JS 1,460.72 kB / 446.33 kB gzip, main CSS 215.29 kB / 33.86 kB gzip. The large-chunk/image warning remains P08, not waived. Rebuilt actual CSS now contains `.bg-background{`. Smoke rendered 22,848 root characters and isolated exactly two collector scripts, within its documented local-bundle scope.
7. English/Arabic parity passes in all eight namespaces (common: 920 keys). Runtime manifest validation, a final exact resolver call covering all **32 Batch 3 deliverable paths**, and scoped Git whitespace checks pass. Resolver: no affected backend groups/functions/shared runtime, no deployment requirement, no errors. Git LF/CRLF notices and Node26 loader deprecations are informational.
8. Current source design-policy scan: **1,940 files / 498 source-text occurrences**, exit **1**, unchanged in count from the earlier baseline (412 icon-background matches, 7 legacy gradients, 18 physical-spacing matches, 61 raw brand-gradient uses). Inspected output includes fixtures and semantic status colors; these are not 498 confirmed visual bugs. The scan is still failing and needs semantic triage and actual component repairs—no broad exemptions were added. Full design/a11y/visual conformance is not claimed.

The standalone browser fixture builds actual shell/header/navigation/Radix/buttons/nav data/locales/CSS with exact aliases for auth/data and ancillary widgets. Its synthetic 24px safe-area input is not physical-notch emulation. Placeholder profile/notification/search internals, actual authenticated roles/guards/data, webfont loading, offline/PWA and deployment behavior remain outside that fixture's claims.

Next concrete work: complete I09 save/cache/remount ownership (including late-response ordering), fix D13 normal-text action contrast and D04's student dark-gradient leak, then continue V09–V12 visual/collection gates and S05–S09 navigation/page/portal details alongside remaining auth/cache/offline lifecycle issues. Keep prototype deletion gated and the broader goal active.

### Batch 4 — semantic contrast and preference read-after-write ownership (local gates complete)

Authority update: the current [launch contract](../product/EDEVISER-LAUNCH-CONTRACT.md) was read in full when workspace instructions changed. Its configurable K–12 launch target supersedes conflicting historical product direction. This batch changes shared frontend behavior only; local fixture/unit verification is not Connected, Deployed or Customer-ready launch evidence. Root instructions and the new product contract were changed externally, not by this batch.

Verified repairs: D05, D12, D13 and I09. D04 received the bounded sidebar/tactile improvements below but remains open for its broader styling scope:

- D13: a new normal-text source suite first failed 11 of 17 checks, exposing light primary/accent/destructive and dark fill/foreground failures. Semantic light primary/text blue is now #026699 (distinct from preserved brand blue #0382BD); light destructive is #B91C1C. Bright dark fills pair with #0A1628 ink. Muted text is darkened in light mode. Brand anchors, palette ramps, domain colors and assets remain unchanged. The suite now covers 25 checks, including hover/active token pairs, muted surfaces and preserved identity anchors. Existing large-size tests were strengthened with normal14px checks rather than removing their coverage.
- D05: canonical action variables are defined in both theme scopes. Tactile CSS consumes them without fallback hex colors. Native color-scheme follows light/dark; actual computed declarations are checked in Chromium. Generated shadcn files were not edited.
- D04 partial: both desktop and modal student sidebars remove their opaque light gradient in dark mode. Selected navigation uses semantic sidebar colors. Browser testing then exposed index.css's fixed label overrides (3.37–3.81:1 in dark); those bindings now resolve semantic colors at the consuming sidebar. Other hardcoded/physical styling remains open.
- D12: the design-system README now maps existing source entrypoints and explains the active single CSS graph, shared action contract, live facades and incomplete migration. Obsolete cutover/93.65-degree comments in the barrel were corrected without changing exports. Five documentation tests validate every local file link, fail on missing/empty coverage and distinguish launch targets from verification.
- I09: a narrow matching-owner cache patch runs only after actual successful preference writes, preserves cachedAt/role/tenant/legacy metadata and leaves absent/foreign/invalid cache entries unchanged. A per-AuthProvider journal captures exact lifecycle identity and per-leaf revisions at request start; publication reconciles only newer confirmations, rejects inactive lifecycle responses and retains later-started server authority. The internal context independently publishes owner generations so batched A→B→A retires old callbacks without disabling the new setter. Live AuthContext updates also survive a language-only provider remount. Final focused execution passed 98 tests across eight files, including genuine menu save → full remount, late pre-save revalidation, later authoritative refetch, StrictMode replay, blocked storage getters, retained/current setters and late error suppression. The original 16 lifecycle regressions remain unchanged. Already-dispatched server writes cannot be rolled back, and storage remains best-effort. No backend schema/API/role/tenant/TTL policy change was made.

Executed browser evidence:

1. Expanded actual-Button checks exercise seven variants × canvas/card × default/hover/held-active = 42 normal14px samples per role/theme; 420 enabled samples overall. Disabled controls are recorded separately, not counted as enabled contrast passes.
2. A targeted run passed every light sample and most dark samples; six dark outline samples per role were rejected because serialized near-white OKLab converted marginally above sRGB1. Quantified excursion was 0.000129911 (0.03313 of one8-bit step). Parser clamping is limited to 1/(16×255) near channel boundaries, with exact regressions and an independent Chromium canvas color/alpha comparison. Truly out-of-gamut/unknown paint still fails; the 4.5 contrast threshold is unchanged.
3. After that parser correction all 420 Button samples passed, but 40 dark navigation cases failed on the fixed sidebar-label cascade, proving that safe source tokens alone were insufficient. The label bindings were corrected, not the assertions.
4. Final shared-UI source run `node --test scripts/shell-navigation.regression.mjs` **passed all 93 scenarios plus outer test (94 total)** in 264.41s: original82 navigation/header cases + ten role/theme Button cases + parser/canvas sanity/negative checks. Exact temporary fixture build assets were `index-BINwZ6_k.css` (245.19kB) and `index-BLueOECU.js` (968.19kB), built from the current dirty working tree; they are not a deployment or immutable commit claim. Fixture/server/browser outputs were cleaned. Auth/data/ancillary widget internals, full role journeys, physical notches, webfonts, backend and launch readiness remain excluded.
5. Parent's six focused theme/sidebar/documentation files **passed 94 unit/property checks**. The first expanded cache red phase reproduced four intended lifecycle failures; three collateral failures were unconsumed one-shot mock queues, corrected through fixture reset rather than production changes. The final eight-file cache run passed 98 tests. Global validation then caught three unnecessary callback dependencies and strict indexed-access narrowing in the new documentation test; those were corrected without suppressions. Final ordered lint → TypeScript → unit → build/smoke **passed**: zero-warning lint, root TypeScript, **791 files /7,519 tests passed /1 existing skip**, then a 4,484-module build and local smoke (#root22,848 chars; exactly two deployment collectors isolated). Unit duration97.85s, Vite15.48s. The known skip and intentional missing-ledger negative-fixture stderr remain distinct from failures; no tests were disabled for this batch.
6. i18n parity passed all eight namespaces (common920); touch-policy browser regressions passed all10; critical E2E route-contract guard passed. The source design scanner remains **exit1: 1946 files /498 raw matches** (412 icon-background,7 legacy-gradient,18 physical-CSS,61 raw-brand-gradient). These include fixtures/semantic statuses and need triage, not bulk replacement or a waiver.
7. Exact19-path runtime resolution and manifest validation passed: affectedGroups=[], functions=[], sharedRuntimePaths=[], deploymentRequired=false, errors=[]. Deployment Impact: **NONE**; Runtime feature(s): **none selected by resolver**; Affected functions: **none**; Shared runtime changed: **NO**; Production action required: **NO backend action identified**. This does not deploy or attest the changed frontend. No Preview/Production mutation was performed.

Final hermetic application build identity (fake loopback Supabase configuration, not release output):

- `dist/assets/index-B5d8rC3a.js`: 1,463.32kB /447.19kB gzip; SHA-256 `31A75690A0F64FA7451901E1035ED0E799782B04AA4B39B0C7C3DCC89B9024E1`.
- `dist/assets/index-CEyp738o.css`: 219.32kB /34.32kB gzip; SHA-256 `C9BE71EDAC8DE8D0AEDDB18FBD2F05BB175F5D04F5B8181F181975BBFC05A0AC`.
- P08 remains open: the >500kB chunk warning and large production-owned mascot images were not hidden or solved by this batch. Baseline HEAD remains `b09535e5262b5a006a68be15bdf54abe381a28ae`; the worktree is dirty, not a new immutable commit.

### Expanded product-wide redesign — current human authorization

The requested outcome is now a visibly redesigned, coherent Precision Light / Obsidian Dark product across all roles/pages/sections on web and mobile, with dependency-proven consolidation and thorough override inspection. The chosen direction and referenced public research live in the existing `src/design-system/README.md`; no second design system or parallel completion checklist was created. Official Toddle module pages, Linear's published redesign and Notion's view/filter reference inform the direction; the unavailable search connector and lack of authenticated competitor review are explicit limitations.

Current work is not a claim that this redesign is delivered everywhere:

- Two read-only audits completed the sampled cross-role override/dependency review. They found D15's malformed utility, fixed-light hero scopes mixing dark-resolving descendants, and typography preferences set on html being defeated by body/heading/sidebar fonts. They do not certify all217 route declarations. The current built CSS contains no `bg-transparent0` match; occurrence-by-occurrence paint/semantic classification remains in progress.
- Do not indiscriminately move the tactile recipe into a CSS layer: its current unlayered semantic foreground intentionally wins over the generated variant's `text-white`. A layer-only move can regress verified dark contrast. Fix ownership and the full dependent recipe together, backed by rendered checks.
- Executed TypeScript/Vite resolution inspection confirms several same-basename `.ts` files are active facades rather than duplicates; removing them would change the resolved implementation. SectionHeader/Shimmer implementations also differ in markup/accessibility behavior. No deletion has been authorized merely by a matching name.
- D01/D03 now have nine additional regressions in the existing token test file. Before the CSS repair, **all nine failed** while the prior24 passed. Actual grid/motion browser coverage is being added to the existing isolated fixture; no new production runtime file or override stylesheet has been added.

#### Expanded audit handoff (source/resolution evidence, not rendered certification)

- **D04/D07/D13 consumer scope:** `patterns/SectionHeader.tsx` fixes headings to slate-900/13px; `AdminPrimitives` and parent heading styles repeat fixed dark ink over dark-resolving cards. Admin/coordinator/teacher/student hero callers force light chrome/glass while descendants inherit dark tokens. Parent help/rail gradients have the same surface-pair problem. `ParentButton`, selected-child controls, coordinator action chips and student gradient CTAs bypass some or all of the proven Button recipe. Keep D13's existing verified scope; extend actual consumer tests before claiming these pass.
- **S09:** generated dialog/select/popover portals lack general role propagation. Default overlay/content z50 can overlap beneath header z100/sidebar z110. Establish an adoption-layer scope/stack contract with nested modal→select→popover tests, focus/scroll restoration and viewport containment; do not patch every page's z-index or hand-fork generated files.
- **D16:** `lib/directionManager.ts`, `lib/accessibilityPreferences.ts`, `lib/fontPreferences.ts`, token base typography and sidebar recipes are competing font owners. Test Arabic and dyslexia preference combinations at body/headings/sidebar and actual font metrics. **D04 accessibility extension:** high contrast only changes a partial token set; smooth-scroll calls and the blanket data-state animation exception need combination testing.
- **S11:** `patterns/HeroCarousel.tsx` owns undersized targets, direction-sensitive translations, inactive content and auto-advance. Decide which roles still need a carousel before carrying its behavior into the redesign.
- **D17:** `components/ui/sonner.tsx` important paint utilities defeat semantic variables; `App.tsx` mounts this host. Adopt a paired surface using supported integration, preserving toast behavior and localization.
- **S08/D11:** DataTable pagination does not wrap; student borders/positioning, EdvToggle translation and stat chips retain physical or private style/locale ownership. Five rails repeat layout/link recipes and independently disappear below xl; preserve accessible in-flow destinations for useful mobile actions.
- **Evidence cleanup:** index.css contains a stale contrast table (including 2.90 described as passing3:1); role/generator comments claim prototype parity/finalization without executed current proof. Correct provenance comments during owning changes; do not use them as acceptance evidence.

**Dependency audit:** TypeScript and Vite resolution plus an AST scan across2,124 code files found zero incoming import/reexport/literal dynamic-import edges to shared SectionHeader/Shimmer/SeverityIcon and the five shared DashboardNew implementations. This proves the scanned JavaScript relationship, not safe removal or no CSS impact. Same-basename `.ts` wins over `.tsx`; removing a facade first can reactivate old code. Keep canonical patterns, actual page dashboard facades, the active CurriculumMatrixPageNew target, and Hawdex's active Logo/public namespace contracts. PCard and generated Card differ in layout/padding/API; an alias replacement is unsafe.

**Cleanup gate:** before deleting candidates, inventory unique emitted Tailwind selectors and active computed/data-provided class producers, compare exact-build CSS before/after and verify affected consumers. Automatic Tailwind scanning includes orphan source independently of JS reachability. Five old dashboards and old SeverityIcon.tsx have existing modifications; preserve those working copies before retirement. The eight Hawdex files are untracked and especially must not be treated as Git-recoverable duplicates. Update real references in CANONICAL-COMPONENT-REGISTRY and FRONTEND-MASTER-MAP; retain synthetic scanner fixtures rather than mistake their file names for real imports.

**Prototype/assets gate:** the production-reference scanner returned[] but visual config/screen-map/capture still uses prototype URLs. The artifact gate derives forbidden filenames from the current prototype tree and needs absent-tree negative tests first. The codebase-review-pack generator also emits prototype paths and an unsupported Finalized narrative. Retire only after application-owned reviewed baselines and tooling migration. Eager mascot URL imports cause emission, not necessarily initial image-byte fetching; preserve catalog/marketplace IDs and measure cold/route transfers before optimization claims. No files were deleted by these audits.

#### D15 next repair map (read-only classification; not implemented)

A complete src search found **45 matches:41 production-source occurrences in33 files plus4 test selectors**. None is an ordinary section/card icon wrapper. Historical diff `5c2cd7af55ba2bece1c9caccb855068fde32031f` (#338) shows valid `*-500` fills changing to `bg-transparent0`; this defect already exists in baseline HEAD, not this batch's frozen changes. The unsafe-prefix-replacement mechanism is a supported hypothesis, not proof of the exact script used.

| Family | Exact owning files / occurrences | Repair constraints |
| --- | --- | --- |
| Generic blue progress (9) | shared BadgeCollection, BadgeSpotlightCard, UploadProgress; admin dashboard department magnitude; coordinator dashboard coverage/readiness (2); admin MarketplaceAnalyticsPage; student CompleteProfilePage and ProfileSummaryStep | Existing primary is a candidate. Preserve all widths/values: self-efficacy is not academic mastery, and magnitude is not a new status classification. BadgeSpotlightCard is an orphan candidate, not a verified live screen. |
| Green progress (2) | shared HabitDifficultyIndicator; admin AdminAnalyticsPage acceptance rate | Original green/emerald meaning; measure existing success pair against actual track. HabitDifficultyIndicator has no proved production consumer. |
| Teal progress (2) | StudentLearningProfileRail; teacher CLOListPage readiness | Existing text-teal is a candidate; preserve profile completion vs curriculum readiness semantics. |
| Other progress (2) | student StudentProgressNew weakest-CLO bar (rose); XPHistoryNew contribution share (amber) | Weakest does not imply Not Yet; no new threshold. Preserve rose attention as a bounded compatibility exception if needed. XP uses existing amber --xp, not the separate teal --xp-fill. |
| Chart keys (7) | StudyTimeChart (1); AdminAnalyticsPage attainment legend (4); LeaderboardPage current-week/personal-best keys (2) | Update plot and legend together or retain exact historical blue/yellow as bounded compatibility. Admin uses existing attainment categories; never borrow a Bloom/rank token merely because its hex matches. |
| Status markers (8) | NotificationCenter and NotificationsFeedPage unread (2); TeamLeaderboardView live (1); StudentDashboardScreen friend and StudentFriendsPage avatar/Online-now (3); StudentCoursesNew due-today (1); LearningPath available/submitted nongraded node (1) | Preserve blue attention, green online, amber urgency and teal in-progress. Fixed green-50/teal-50 islands cannot safely consume arbitrary dark foregrounds; pair local surfaces. NotificationCenter/TeamLeaderboardView are orphan candidates. |
| Red text badges (2) | NotificationBell unread count; SaleBadge | Pair fill/foreground and measure normal10–12px text. Shared red emphasis does not make a sale an error or unread badge a destructive operation. |
| Checked controls (6) | ui EdvToggle checked track; onboarding BaselineTestStep, LearningStyleStep, PersonalityStep, SelfEfficacyStep, StudyStrategyStep radio dots | EdvToggle is a custom-named Radix wrapper under ui; respect adoption/regeneration ownership. Pair checked track/thumb and selected radio surface/border/text, including RTL. Do not mix bright dark dots with fixed light selected backgrounds. |
| Removal controls (2) | Tutor ChatPanel image/document attachment buttons | Restore paired destructive action paint without changing attachment gates/callbacks; separately fix20px hover-only interaction as part of route UX. |
| Decoration (1) | LevelUpOverlay blue20% glow | Pure decoration, not a meaningful contrast marker. Preserve effect/motion scope; no fabricated3:1 requirement for a glow. |

`errorStateUploadProgress.test.tsx` has the four invalid selectors (historical lines104,118,191,204): keep progress width/clamp and success/error absence assertions but select semantic behavior, not the broken color class. Connected shared targets are BadgeCollection, LevelUpOverlay, NotificationBell, SaleBadge, StudyTimeChart and UploadProgress; their mere imports are not rendered journey evidence. StudentCoursesNew, StudentProgressNew and XPHistoryNew are active targets behind page facades, not deletion candidates.

Before closing D15: no malformed utility in production class contracts; emitted/computed paint in both themes; meaningful indicators versus actual adjacent track/surface at3:1, normal text at4.5:1; chart/key agreement; original values/status/action behavior; explicit conditional/unreachable fixture scope. Theme semantic tokens not registered in @theme require supported arbitrary-variable utility syntax, not invented `bg-success` classes. No catch-all replacement or extra override sheet.

### Batch 5 — D01/D03 cascade and motion verification (local gates complete)

The four new browser cases failed before repair (all four plus outer test), independently exposing26 dark-paint mismatches and continued animations under reduced motion. The scoped source repair moves only the eight conflicting light defaults before .dark, preserves domain/brand color values, leaves the shared XP fill unchanged, and disables only the three named loops under reduced motion with static ring depth. Nine source regressions previously failed. Ordered lint → TypeScript → unit → build passed: zero-warning lint, **791 files /7,528 tests passed /1 existing skip**, then a4,484-module build (final repeat13.27s; unit82.44s). The intentional missing-ledger negative fixture and Node DEP0205 stderr are not gate failures. Local bundle smoke passed with22,848 root characters and exactly two deployment collectors isolated; offline/PWA and deployed telemetry remain unverified. The final expanded browser matrix **passed98/98 in324.79s**, retaining the previous shell/normal-Button proof and adding four theme×motion cases. After the CSS fix, an intermediate run passed95 and failed two restored-theme checks plus the outer test: an already-running box-shadow transition retained its old duration after the media declaration shortened. The fixture now waits for actual remaining CSSTransition timing (bounded, after pending timelines settle), not just newly computed duration. Exact colors remain enforced. Shadow checks also reject shifted10px/-10px, wrong-spread and non-inset layers instead of accepting zero-offset substrings. Full ordered application gates were repeated after the fixture repair; no thresholds were relaxed.

Hermetic fake-config build identity (not a release/deployment):

- `dist/assets/index-BrckXptA.js`:1,463.32kB /447.20kB gzip; SHA-256 `F1AB8320C6F58525C1857370634B623A7C96E29FBF83E79965478B46046EA6BD`.
- `dist/assets/index-DRltKNu-.css`:219.44kB /34.34kB gzip; SHA-256 `A09B343129E4B9C85DDB4F4E0B5EC670FB58FDA114A3D781C11276FC11CA415F`.
- The large chunk/asset warnings remain P08; no thresholds were raised. Current source still contains the newly inventoried design defects. No duplicate/prototype files have been deleted.
- Translation parity passes all eight namespaces. The runtime resolver validated the exact five changed source/docs/test paths: affectedGroups/functions/sharedRuntimePaths/errors all empty; deploymentRequired false.

```text
Deployment Impact: NONE
Runtime feature(s): [] (manifest-derived backend groups)
Affected functions: []
Shared runtime changed: NO
Production action required: NO (no backend closure selected)
```

No backend operation or frontend deployment was performed. The application source/build and isolated browser fixture remain local evidence.

### Prior D01/D03 inspection handoff (historical pre-repair source positions)

- D01: later unlayered `:root` heatmap/XP defaults override earlier `.dark` values on html.dark. Move only the conflicting defaults before `.dark`, preserving values and unrelated domain defaults. Actual HeatmapGrid consumes fill/outline/hover tokens; no runtime XP-token consumer was found. `--xp-fill` is intentionally shared. Browser proof should use the actual grid plus explicitly labeled XP token-contract swatches, not claim a repaired application XP bar.
- D03: `.agent-ring`, `.risk-dot` and `.cursor-blink` define unbounded CSS loops without reduced-motion coverage; no literal production consumers were found. Add scoped static behavior and positive/negative browser controls, including live media changes. Do not count an unconsumed keyframe definition as a running widget, or globally disable all animations. Four dedicated theme/motion cases can exercise these contracts without multiplying the navigation matrix.

### Batch 6 — shared patterns and generic-blue progress (local gates complete, 20 September 2026)

Implementation is limited to the existing owners; no parallel component files or stylesheets were created:

- `PCard` now composes the generated Card without editing that primitive. Block/no-padding/no-gap defaults, semantic border/foreground, radius and `--depth-2` replace universal hover lift. Caller layout, radius, shadow, style, event and ARIA overrides remain supported. Existing reduced-motion translation/transition guards remain because active callers supply their own motion.
- Canonical `SectionHeader` preserves h2/h3 semantics, wraps full titles/descriptions, and uses a transparent decorative icon. `PageHeader` retains h1 and separates narrow actions. One `HeadingActions` implementation in the existing SectionHeader file governs direct Button/link wrapping and 44px minimum targets; nested action groups still own their layout. No public barrel expansion or new localized production strings.
- Nine `bg-transparent0` fills became `bg-primary` across BadgeCollection, BadgeSpotlightCard, UploadProgress, AdminDashboardScreen, CoordinatorDashboardScreen (two), MarketplaceAnalyticsPage, CompleteProfilePage and ProfileSummaryStep. Existing `bg-muted` tracks and width/domain expressions were preserved. UploadProgress uses React useId plus the supplied visible filename for progressbar naming and exposes its existing clamped range. Four tests formerly selecting the broken class now use semantic progressbar queries. Remaining production matches:32, not zero.

Executed evidence and failures:

1. Initial focused pattern tests reproduced three failures before implementation. Final scoped lint and four-file unit run passed **69 tests** (patterns13 + upload/badge families56). No raw/generated primitive fork. The broader current tree includes other concurrent work; this batch does not take credit for those changes.
2. Six browser cases were added to the existing98-test fixture: four theme/locale pattern cases, each five actual role-shell scopes ×320/1024px ×16/20px root text (80 scenes total), plus two progress-family cases. Business/ancillary seams remain explicit aliases; these are not authenticated route journeys or webfont certification. Parent strengthened the fixture by removing helpful Button wrapping overrides, supplying longer EN/AR action labels, requiring44px targets and checking rendered label bounds. Thus the source heading must provide the behavior.
3. The first complete six-case execution passed both progress families and the parser but failed all four pattern cases solely on depth paint. `tailwind-merge` retained generated `shadow-sm` beside ambiguous `shadow-[var(--depth-2)]`; actual cards painted the default shadow. The implementation now uses `shadow-(--depth-2)`, which removes the conflicting class. Unit assertions reject leftover `shadow-sm` and preserve caller `shadow-none`. Independent inline-token shadow comparison was retained, not weakened.
4. After repair, all six new cases passed inside the subsequent full run, including80 pattern scenes,16 actual progress fields across two themes, actual fill/track contrast ≥3:1, normal text contrast ≥4.5:1, static-card hover geometry and CSS individual transform checks. However, that **full run did not pass**: after normal early timings it reported multi-minute/hour gaps, exceeded its outer deadline, and continued scheduling invalid later cases. It was stopped and collected. No performance conclusion or complete104-test pass is inferred from it. A cancellation guard now rejects scheduling before/after an aborted parent case and closes the browser on suite abort; regression controls cover pre-abort, during-case abort and normal scheduling. No per-case time or contrast threshold was relaxed. The outer budget is660s because the new80 scenes independently add roughly160s to the prior matrix.
5. Two attempted integrated gates remain recorded as failures, not waived: one passed lint/TypeScript then failed an upload-owner test and a portfolio timeout (790 other files passed); the then-current upload helper already contained a later UUID guard when inspected. Both affected files passed48 focused tests with two workers, without parent edits to that upload work or timeout inflation. A later run passed lint but caught strict indexed access in canonicalPolicyResolver and nullable accreditation data. Accreditation source changed concurrently before inspection; it was preserved. The remaining policy-loop typing issue was repaired using a typed predecessor in the same sorted adjacent-band comparison—no grade boundaries, precedence, values, APIs or authoritative academic behavior changed. Existing policy boundary/overlap tests are included in revalidation.
6. A fresh ordered attempt passed lint but exposed additional strictness in concurrently added assessment-contract and competency-tree fixtures. The test-only repair retains the known scale bands as a checked literal tuple, explicitly rejects a missing captured provider/system message, and uses installed i18next's `initAsync:false` option instead of removed `initImmediate`. No boundary values, expected results or assertions were dropped. Scoped lint, full TypeScript and both files'42 tests passed afterward.
7. The final uninterrupted ordered run passed lint → TypeScript → **800 files /7,680 tests /1 existing skip** with four workers (270.62s) → build (4,488 modules,27.72s) → local bundle smoke (22,848 root characters; exactly two deployment collectors isolated) → **104/104 browser tests in454.28s**, zero failures/cancellations/skips. This includes all prior98 cases, the six new cases and the cancellation negative controls. Expected negative migration-ledger stderr and Node DEP0205 did not fail gates. Chunk/asset warnings remain P08; no performance threshold was raised. No frontend/backend deployment or production mutation.
8. Exact runtime resolver validation covers all18 Batch6 source/test/script/doc paths: affectedGroups/functions/sharedRuntimePaths/errors empty, deploymentRequired false. Diff hygiene passes; all eight locale namespaces are in parity. This is local dependency-closure evidence, not live Supabase/deployment attestation.

Hermetic fake-config Batch6 build identity (local, not a release):

- `dist/assets/index-CRrEpu6I.js`:1,469,891 bytes /449.21kB gzip; SHA-256 `18B739BE3F120ED147CC4C9E5569C1AD63767021094DD878F0CC91ABF2FB6039`.
- `dist/assets/index-CJSabeVk.css`:220,891 bytes /34.57kB gzip; SHA-256 `2DF86142E74BCCE9AD422EDE408ECB17FE59F0D225100D75196574E88E7FC2E6`.
- Build differences include concurrently authored source, not only this batch. Emitted mascot files do not establish initial network transfer; active brand assets remain intact.

```text
Deployment Impact: NONE
Runtime feature(s): [] (manifest-derived backend groups)
Affected functions: []
Shared runtime changed: NO
Production action required: NO (no backend closure selected; frontend remains local)
```

Interaction caveats retained: the active ChallengeListPage whole-card callback navigates to the existing challenge route and should eventually become a semantic Link; TeacherStudents filter buttons need explicit focus treatment. Existing Link-wrapped cards must not acquire nested interactive elements. Explicit assignment/marketplace hover effects remain caller-owned; radius/focus geometry and all routed consumers still require wider migration coverage.

### Batch 7 — paired progress/selections and two proven-unused copies (local gates complete)

Implemented11 additional malformed-state repairs in their existing owners, without new components/stylesheets or changes to math, answer indices, gates, mutations or backend contracts. Browser recovery adds one canonical attention-color token pair rather than retaining a page-local palette utility:

- Teal learning-profile and curriculum-readiness fills use `--text-teal` on muted. The CLO readiness wrapper alone becomes card-backed so its semantic labels do not remain on a fixed pale dark-mode island; the separate white SelectTrigger remains outside this repair.
- Habit and admin **draft**-acceptance fills use `--success-foreground` on muted. Habit remains an unconnected shared-component contract; draft acceptance is not academic attainment. The admin chart legend is unchanged.
- Weakest-CLO attention now has a canonical opaque-sRGB `--progress-attention` pair (light#E11D48, dark#FB7185), not a page-local rose utility or error/not-yet classification. The attention card's title/body labels use foreground; its icon, percentage and fill share the attention token. An unused local color field was removed. XP category bars retain amber `--xp` with a local `--tertiary-900` navy track: a deliberate paired-paint decision, not a global XP/muted token change. All width and percentage expressions remain unchanged.
- Five onboarding rows now pair accent/accent-foreground with primary selected borders/dots and a muted-foreground unselected ring. This repairs the whole local selected pair, not a bright token on a fixed pale island. Existing raw radio structure, unselected row/hover, intro gates, baseline timer, one-based versus zero-based answer conventions, save payloads and disabled navigation are preserved. Full radio keyboard/localization work remains separate.
- A fresh source query finds21 remaining malformed occurrences (down from32): seven chart keys plus14 notification/toggle/status/decorative/dormant-control entries. This is a source count; the actual-field fixtures and final local gates below cover the new scoped repairs, not those21 remaining entries or every routed state.

Focused checks passed: progress tests20/20 across two existing files; onboarding42/42 across three existing files, including five real selection/reselection and exact payload cases; canonical pattern/retirement14/14. Scope lint passed without warnings. These did not substitute for the final ordered application and actual-field browser gates; the collected parent `pwsh-128` result below now completes the local Batch7 gates.

#### Rendered recovery history and local Batch7 completion

The child could validate script/embedded-JSX syntax, real locale DTO shapes and lint, but its fixed execution scope rejected Node test-runner spawn before any browser setup (`pwsh-121`, EPERM). Parent independently ran the scoped matrix under the session's explicitly authorized full-access runtime (`pwsh-123`, collected): both onboarding theme cases passed (40 real EN/AR selection/save scenes plus10 guards); both progress cases reached12/13 field checks and failed only because installed Tailwind rose500 computed to out-of-sRGB OKLCH. The strict parser correctly rejected red channel1.0033566 beyond its existing tolerance. This is an unsupported-color proof gap, not an established contrast failure. No parser tolerance, gamut clipping or contrast requirement was weakened. The implementation instead uses a shared, intentional sRGB attention pair and adds real attention-card text measurements and source-token tests. Follow-up `pwsh-126` is collected and passed: scoped lint,29 token tests, and both actual progress theme cases (42 scenes/26 fields total), including attention-card text. The first ordered integrated attempt, `pwsh-127`, subsequently failed after lint and TypeScript passed: two providerConsolidation tests encountered ENOENT because their `git ls-files --cached` inventory still enumerated the deliberately deleted unchanged shared SectionHeader. The other800 test files passed;7,713 tests passed and one existing test remained skipped across801 files. This failure is retained, not relabeled as success. Parent repaired only the test inventory to exclude **Git-confirmed tracked deletions**, without swallowing read errors or changing security patterns/assertions, and added a negative inventory regression. Focused recovery passed6/6.

Collected parent evidence: **`pwsh-128` passed the complete ordered local gate run**: `npm run lint` → TypeScript → full hermetic unit suite (four workers: **801 files,7,716 tests passed, one existing skip;217.91s**) → build (**4,488 modules;13.89s**) → local bundle smoke (**22,848 root characters; exactly two Vercel collector scripts isolated**) → **108/108 browser cases passed in570.067s**. Locale parity passed all eight namespaces. The exact23-path runtime resolver closure returned empty `directFunctions`, `sharedRuntimePaths`, `affectedGroups`, `functions` and `errors`, with `deploymentRequired=false`; scoped Git whitespace checks passed. These are collected parent-run results, not extra tests executed by the documentation worker.

Local build artifact provenance supplied by the parent:

| Artifact | Bytes / reported gzip | SHA-256 |
|---|---|---|
| `dist/assets/index-DTcEI6SV.js` | 1,469,891 bytes /449.21kB gzip | `DA4E87BCE125018EB2FBE7BBCB5F39B6A0D690F3629E3BCBFAAFB1A5212BBE57` |
| `dist/assets/index-CNn1sJLz.css` | 221,427 bytes /34.71kB gzip | `0CA6A6CD4338E0614DA59B3E63311D4FABC69CF6A98A238B6DF60876D313B53F` |

These builds include concurrent user changes and remain local verification artifacts, not production deployment outputs. Local Batch7 gates are complete; **D15 stays Open,21 malformed entries remain, and26/70 original findings remain Verified**. No all-route/component-depth, authenticated-role, live-data, offline/PWA, deployed-telemetry or whole-redesign certification is implied by the108-case controlled matrix.

#### Expanded all-role source inventory (not rendered certification)

The repeated product-wide completion request is now explicit in the design-system README acceptance standard. Fresh router AST/facade inventories account for202 non-layout route declarations: admin52, coordinator27, teacher54, student56 (including standalone focus), parent13. These include six Navigate-only redirects and three course-data-dependent redirect adapters; aliases/create-edit variants remain distinct declarations. They are **source-inventoried only**, not202 verified pages. The two role scans inspected62 admin/coordinator direct modules and105 teacher/student/parent owner/layout/nested modules; these sets overlap and are not an additive unique-owner count. Deeper component, rail, hook, portal and state dependencies remain to be inventoried and rendered.

Important routing conditions: incomplete student onboarding substitutes the wizard before the normal outlet; adaptive quizzes are immersive; focus is outside StudentLayout. Teacher routes always receive TeacherDashboardRail. Parent notifications and communications share ParentCommunicationsPage, not the four-role shared feed. Coordinator profile/outcome-chain dispatch to role-specific current owners; admin outcome-chain uses Legacy. Current dashboard/course/progress/portfolio/journal facades are unconditional despite stale feature-flag comments. A declared student material-id route does not establish material selection: CourseDetailScreen currently reads only courseId.

Prioritized source findings require more than recoloring: StudentProgressNew substitutes zero course/evidence counts with5/12 and has no summary error/loading branch; ParentAttendance substitutes absent totals with97%/120 and unconditional strong-attendance language; LearningPath exposes fixed improvement/time claims. CoordinatorDashboardRail can call absent evidence meeting target; dashboard unknown readiness has aria-valuenow0; XPEconomist can show undefined:1 and collapse missing data to0; CurriculumMatrix term state is unused by its query/export. Parent reminder/encouragement/profile flows can announce success without checked mutation outcomes. These are source findings, not deployed-data claims; hook/enum/authorization contracts must be inspected before repair. Preserve valid unknown dashes and documented domain defaults rather than blanket-replacing all numbers.

High-leverage remaining owners include repeated admin/coordinator CARD strings, teacher/parent raw panel recipes, GradientCardHeader, ParentButton/ParentSectionIcon, StatePanel/default copy, domain-specific status maps, and shared controls/portals. AST color/text/native-heading counts are triage signals, not defect totals or proof of translation failure. The existing70-finding ledger and small rendered fixtures must not be mislabeled exhaustive all-route completion.

#### Dependency-proven retirement

Removed only unchanged tracked `src/components/shared/SectionHeader.tsx` and `src/components/shared/Shimmer.tsx`. Their canonical pattern implementations remain active; the retained loading pattern has its decorative aria-hidden contract. No API adapter, duplicate archive, brand asset or route facade was added/deleted.

- An executed TypeScript AST/resolution audit covered2,135 source/config/script/test files and9,471 resolved edges, with explicit resolution canaries against the real tsconfig.json. Both candidates had zero incoming static/type/re-export/require/reference edges and no unresolved candidate-name edges.
- Reviewed the two nonliteral imports: browser-image-compression behind an existing type assertion, and a schema-checker URL in an audit test. Neither targets these files. Both import.meta.glob calls are explicit PNG asset patterns; neither can select TSX candidates.
- Script/e2e/test/prototype reference checks found only a synthetic Shimmer path written into a test-owned temporary workspace. That detector fixture remains intact; it is not a dependency on the deleted source. The two candidates themselves import only shared utilities/types, not images/assets.
- Installed Oxide scanned a captured1,984-file src snapshot. Independent before/after candidate scans (excluding precisely the two candidates) each produced27,129 candidates. Separate installed Tailwind compilers using actual index.css imports generated **identical267,552-byte CSS**, SHA-256 `60479efa1a26d9dfa731dbbab00765133424f71f5edcdc2228209ba6bf54de10`. This addresses Tailwind's orphan-source contribution; it is not the application bundle hash or a visual certification.
- Immediately before deletion, Git confirmed no candidate changes from HEAD, and SHA-256 guards matched inspected bytes: old SectionHeader `0183a984514dacc131f93d2b4722be1242b531ddb04d27c5ad763a4c4ab7de27`; old Shimmer `fac1239c8ac1ebfbefc884e859e807d471652e9303750d073d9c71a4b1a2f81f`. Recovery remains in the existing baseline commit; no duplicate in-repository archive.
- The existing designSystemPatterns suite now rejects reintroduced copies and renders the retained Shimmer. All14 tests pass. The five old dashboards and SeverityIcon contain existing user modifications and remain untouched despite earlier zero-edge findings. Prototype retirement and same-basename route/facade cleanup are still pending.

### D15 progress/legend map (pre-Batch7 inspection)

This historical handoff guided Batch7's six progress implementations; the seven chart-legend entries remain unimplemented. Source-value contrast calculations are not rendered proof. Preserve every metric, threshold and width expression.

| Family / owners | Next bounded proposal | Important boundary |
| --- | --- | --- |
| Teal: StudentLearningProfileRail, CLOListPage | Existing `--text-teal` on unchanged muted track (source ratios3.354/5.924 light/dark) | Completeness/readiness is not attainment; rail is xl-only in its routed layout. |
| Green: HabitDifficultyIndicator, AdminAnalyticsPage acceptance bar | Existing `--success-foreground` on muted (6.883/12.383); plain success fails light | Habit indicator has no proven production importer; admin acceptance is not learner mastery. |
| Rose: StudentProgressNew weakest CLO | Bounded same-hue restoration, measured against current Tailwind palette | Weakest does not imply a failing attainment band; do not assign not-yet/error semantics. |
| Amber: XPHistoryNew categories | Exact `--xp` fails light muted contrast (1.924); consider a deliberate local navy track (5.755) or documented paired amber ink | Do not silently recolor XP teal or globally change muted/heatmap tokens. Design/rendered decision still needed. |
| StudyTimeChart legend | One local palette shared by actual blue gradient and its key; same neutral stroke for average line/key | Historical blue utility may differ from literal SVG colors in Tailwind4; average gray lacks light contrast. Preserve unique gradient IDs and data/filter semantics. |
| Leaderboard personal-best legend | Shared current/best/previous palette for Cell and keys, including the non-malformed previous-week key | Preserve best-before-current priority and flags. Historical yellow/gray fail light3:1; paired outlines or deliberate paired recoloring need proof. |
| Admin PLO category legend | Connect category accent markers/keys while retaining tinted cells, labels and unmeasured state | Four academic bands are not generic chart-N colors. Existing developing accent narrowly fails3:1 on white; never alter85/70/50 thresholds to solve paint. |

Seven malformed legend keys remain across the three chart owners; the separate admin acceptance fill is not part of that count. Existing chart mocks do not establish SVG/key paint agreement. Real populated/empty/average/best-priority/category fixtures are needed before closing these entries.

### D15 selected/notification/status map (pre-Batch7 inspection)

These19 occurrences plus the13 progress/legend occurrences accounted for32 matches after Batch6. Batch7 implements the five onboarding selected states and six progress fields; the remaining21 entries are still open. This table records the pre-implementation inspection, **not rendered passes**. Repair paired surrounding paint when necessary, not just the broken dot.

| Owners / occurrences | Bounded next repair and preserved behavior | Separate limitations / test boundary |
| --- | --- | --- |
| Custom EdvToggle checked track (1) | Teal `--text-teal` against card thumb; consider muted-foreground unchecked track and canonical focus pair. Preserve all Radix props/ref, noTick and checked/disabled callbacks. | Custom composition lives under governed ui ownership; do not hand-edit generated Switch or duplicate it. ParentProfile's four consumers lack programmatic labels; tick tests its own absent data-state, thumb uses physical movement, and root is44×26 rather than44×44. These need explicit adoption/accessibility work. |
| Five onboarding radio dots (5) | Repair selected row to accent/accent-foreground, ring/dot to primary together. Fixed blue50 islands otherwise fail dark primary contrast (~2.88:1); paired source candidate is5.58/4.57. Preserve 1–5 psychometric answers versus zero-based LearningStyle/Baseline indices and all gates/timers/save payloads. | Active Wizard branches, not dormant. Four intro gates require real Begin interaction. Existing raw radio buttons lack roving/arrow-key behavior; several labels are English-only. Wizard tests mock these steps. Do not invent disabled conditions or count dot diameter as touch target. |
| NotificationsFeedPage unread dot (1) | Primary fits existing card/accent hover; preserve unread-only mark action, separate delete, user/pending guard and empty/error states. | Actual admin/coordinator/teacher/student routes, not parent's separate feed. Fixed gray title and24px delete target are separate defects. Generic span aria-label alone is weak announcement evidence. |
| NotificationCenter unread dot (1) | Pair primary with accent unread row and semantic title; measure hover over declared card backing. Preserve grouping, unread-member mutations, delete propagation and Ask Tutor guards. | Orphan candidate, not Bell's implementation. Outer raw button contains nested action buttons; cannot claim valid interactions from paint repair. No production route should be added for testing. |
| NotificationBell count (1) | Preserve red attention paint with BOTH foreground and background paired; existing red pair gives source6.47/6.55 text contrast. Retaining white on dark red fails2.77. Preserve zero-hidden,99+ display, uncapped accessible count, polite live region, realtime and open-versus-mark-read semantics. | Unread is not an error/destructive operation. Red paint reuse must not change role/action semantics. English unread label,32px trigger and380px popover need separate locale/mobile work. Actual GlobalHeader consumer; count tests are not paint proof. |
| StudentDashboard and StudentFriends presence (3) | Positive recent-activity markers only; pair strong green with a compatible separator instead of retaining the fixed white ring. Preserve conditional f.online and Online now count. | Hook infers activity within5min, not verified network connectivity; absent/invalid/stale values stay unmarked, not falsely Offline. Add positive localized accessible status if repairing color-only semantics; dashboard/all-friends markers currently lack it. |
| Shared TeamLeaderboardView live dot (1) | Entire fixed-green badge needs a paired surface/foreground; currentColor dot can share label ink. Preserve isLive/empty branches and sorting. | Component has test-only usage; same-named hook TYPE in the routed page is not an import. Reduced-motion pulse and actual isLive tests remain. |
| StudentCoursesNew Due Today marker (1) | Warning-foreground for category dot/title on card; preserve date/submission grouping and counts. | Active course route. Marker exists even at zero and is not proof of urgency; separate hardcoded `DUE5:00PM · IN4H` content is unresolved. Do not invent deadline calculations during color repair. |
| LearningPath StageDetail dot (1) | CurrentColor within existing fixed teal700/teal50 pair is a bounded restoration, or migrate the entire pair deliberately. Preserve graded check/locked lock and available/submitted in-progress branches. | Active renderer is exported and testable; existing misleadingly named render test only imports it. Dark token alone on fixed pale island is unsafe. Not attainment or readiness. |
| SaleBadge (1) | Paired readable red emphasis, not destructive action semantics; retain discount pass-through, nonpositive hidden state and pricing eligibility. | Actual ItemCard/marketplace. Locked-card opacity affects composite contrast; Sale/Owned top-end badges can overlap. Tag is decorative; `% OFF` still needs localization. Price-only tests do not prove the badge. |
| ChatPanel draft-removal controls (2) | Future dormant source recipe should use canonical paired destructive Button paint,44px visible keyboard/touch controls and preserved remove/revoke callbacks. | **Currently unreachable:** supportsTutorAttachments() returns false and send rejects unsupported attachments. Never enable or spoof this policy for visual proof. Preserve negative/source gate tests; source/recipe samples are not runtime attachment support. |
| LevelUpOverlay glow (1) | Restore bounded blue/info20% decoration; preserve thresholds, four-second dismissal, reward queue and confetti/motion guards. | Event-driven actual host; pure glow has no3:1 requirement. Existing host tests mock the overlay. Click-anywhere dialog lacks evident keyboard/focus lifecycle; paint alone cannot close that limitation. |

### D16 delivery and ownership handoff (read-only evidence)

The delegated audit found three independent delivery failures in addition to cascade conflicts:

- Production settings/bootstrap call `applyAccessibilityPreferences`, which only toggles a root class; the only importer of `applyDyslexiaFont` is its unit test, so the FontFace loader is not wired into production.
- Certificate-validated public HEAD: the current [pinned WOFF2 URL](https://cdn.jsdelivr.net/npm/open-dyslexic@1.0.3/open-dyslexic-regular.woff2) returns404. The package's [actual regular WOFF](https://cdn.jsdelivr.net/npm/open-dyslexic@1.0.3/woff/OpenDyslexic-Regular.woff) returns200, font/woff,16,636bytes. No font binary was installed/downloaded for this audit.
- Checked-in vercel.json font-src permits self/gstatic, not jsDelivr. This is source-policy evidence, **not deployed-header verification**. Merely correcting the external URL would not resolve that declared policy.

The exact [package metadata](https://registry.npmjs.org/open-dyslexic/1.0.3) points to [Bitstream Vera terms in its pinned README](https://cdn.jsdelivr.net/npm/open-dyslexic@1.0.3/README.md), not an assumed MIT/SIL-OFL license. A same-origin pinned asset must retain applicable notices; Arabic glyph coverage and medical benefits are not established. No CSP broadening or new asset has been implemented.

Next ownership repair: direction applies lang/dir only; locale/accessibility variables are consumed at body/headings/sidebar/auth/portal text, not defeated by descendant or inline families. One loading promise must handle failure/retry and disable-while-pending without reapplying an obsolete preference. Review remote preference hydration/identity because current tests only prove class/variable/storage state. A separate real-font fixture must verify actual response/load plus Chromium rendered-font glyph evidence; computed font-family or fonts.check alone can certify fallback incorrectly. Keep the existing geometry fixture's no-webfonts boundary explicit.

### D17 toast-adoption handoff (read-only, no implementation)

Installed Sonner2.0.7 injects unlayered CSS. The generated wrapper's five classNames entries can each be replaced through supported toastOptions; className/theme/style can also replace its defaults. Preserve that generated file. Current App passes richColors but not resolved theme, direction or translated region/close labels. The vendor owns independent description/status paint,24px action/cancel targets,20px floating close, nowrap/nonshrinking content, faint focus rings and a fixed font stack (another D16 consumer). Root overflow-wrap alone does not resolve intrinsic widths.

- Proposed owner: one internal AppToaster beneath the existing App providers, not a second host/domain XP host. Replace every important default rather than append important overrides. Use one scoped unlayered adoption block with sufficient bounded specificity, neutral popover/foreground surface, muted descriptions, restrained semantic status icons, canonical action pairs, opaque focus rings and wrapping44px controls. Contain the close target inside the frame with reserved space; preserve Sonner's stacking/swipe/height transforms. Disabling richColors is a presentation choice, not a change to toast type/API. Reduced motion must cover the nested generated spinner too.
- Use the actual theme/direction providers and common notifications/close translations; preserve Alt+T and the polite region. Pass-through style must retain the semantic normal variables; fontFamily inheritance can defeat vendor font ownership without a new font stack.
- Prefer locale-aware bottom-end placement with the actual sidebar width only when a shell exists; remove unconditional132px margin from auth/public pages. Bottom-center is also possible but requires explicit shell-only mirrored compensation. Do not infer role from URL or add auth guards for geometry.
- Mobile navigation clearance currently lives only in `.role-app-shell`. Establish a shared variable owner before using it in the global Toaster, applying nav clearance only when actual mobile navigation exists. **Sonner changes offsets at600px, the app at640px:** both offset.bottom and mobileOffset.bottom need the same ownership-aware expression. Re-evaluate composed custom properties at the scope where overrides apply; blindly hoisting a resolved clearance makes child safe-area overrides ineffective. Existing24px shell-only simulation and new global-toast tests must follow the chosen real shared owner, not be disabled or confused with physical-notch proof.
- Actual-adapter tests must cover opposite OS/application themes, live EN/AR and theme changes, status/loading transitions, actions/cancel/close/focus, long/unbroken labels, all relevant4.5:1 text/3:1 control states, reduced motion, stacking, shell/no-shell and live route transitions at320/600/601/639/640/1024px plus20px root. Importing App's internal adapter pulls startup/offlineQueue side effects; a named export with narrow ancillary seams or an explicitly justified shared adapter module must test the actual adoption, not copied props. No new module is created by this plan.
- Vendor z-index999999999 is not an adopted layer policy. Toast-versus-dialog/drawer ownership remains an explicit decision; geometry alone does not certify modal overlap or authenticated journeys.

### Planned batch order

1. Verification credibility and trustworthy evidence paths.
2. Shared shell, navigation and accessibility.
3. Identity/preferences/auth compatibility with targeted regressions.
4. Canonical tokens, states, shared components and portal isolation.
5. Safe prototype retirement and proven-unused cleanup.
6. Role migration in Admin → Coordinator → Teacher → Student → Parent order, plus protected auth refinement.
7. Full relevant gates, browser matrix, performance and final evidence report.

Pre-commit/application completion gate order: lint → TypeScript → unit suite, followed by relevant build/smoke/design/i18n/route/browser gates. A failed gate is recorded and investigated, not bypassed or relabeled as success.

## Runtime/deployment record

For the selected documentation, verification, shell, CSS-entry and frontend-preference changes (executed exact resolver calls covered all 20 Batch 2 paths and all 32 Batch 3 deliverable paths):

```text
Deployment Impact: NONE
Runtime feature(s): none in the backend resolver's affectedGroups
Affected functions: none
Shared runtime changed: NO
Production action required: NO
```

This is the backend dependency/deployment record, not a claim that the frontend was deployed. The generated `dist/` uses loopback/fake verification configuration and is not production deployment output.

Future runtime-affecting batches must use the repository runtime dependency resolver rather than inventing an Edge Function closure. No Supabase or production action is authorized by this ledger.

## Complete declared-route migration inventory — source only

This appended inventory does not alter the original70 finding statuses or certify any route, component, query state, permission, locale or rendered theme. **Every inventory row is SOURCE_INVENTORIED only.** The parent owns integrated gates and recovery: failed `pwsh-127` and passing `pwsh-128` are recorded separately in Batch7. Its local gate completion does not upgrade this declaration inventory to rendered-route verification.

Evidence: TypeScript AST traversal of [AppRouter](../../src/router/AppRouter.tsx) (1,133 lines at inspection), actual [critical route constants](../../src/lib/criticalRoutes.ts), tsconfig-aware module resolution and recursive default re-export inspection. Each JSX Route declaration remains a separate row, including aliases, create/edit modes and redirects. Source order supplies stable `route-<group>-NNN` identifiers; retain existing IDs and allocate new IDs rather than renumbering when declarations change. `L` refers to the opening Route line in this AppRouter snapshot, not a durable source anchor.

- **202 role leaf declarations:** admin52, coordinator27, teacher54, student56, parent13. These include six Navigate-only redirects and three course-data-dependent CourseToolRedirect adapters. Student56 includes the standalone focus route outside StudentLayout.
- **10 other leaf declarations:** eight public/auth/legal/portfolio pages, the root redirect and the catch-all. Thus212 total non-layout Route declarations; seven Navigate-only redirects overall.
- Five role-layout declarations and non-route loading/error/global surfaces are inventoried separately below, not added to the212 leaf count. Alias rows do not imply separate components, and shared modules across roles are not additive unique-owner counts.
- `→` follows an actual facade to its implementation. A single module means no default re-export facade was found; it does **not** mean its deeper component tree has been inspected. Current facade re-exports are unconditional despite stale feature-flag comments in some implementations.
- For compact, unambiguous source paths in tables: `A/` = `src/pages/admin/`; `C/` = `src/pages/coordinator/`; `T/` = `src/pages/teacher/`; `ST/` = `src/pages/student/`; `P/` = `src/pages/parent/`; `SH/` = `src/pages/shared/`; `F/` = `src/features/`; `H/` = `src/components/shared/`. Unprefixed `src/` paths are literal. These are source-path abbreviations, not import aliases.
- Paths are absolute application URL patterns. Index rows show the role root; aliases remain routes, not redirects unless explicitly identified. Dynamic IDs establish a declared URL contract only, not a successful data lookup or authorization proof.

### Admin — 52 declarations

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-admin-001 | `/admin` (L659) | `Navigate` → `/admin/dashboard` | Index; replace redirect. | SOURCE_INVENTORIED |
| route-admin-002 | `/admin/dashboard` (L660) | `AdminDashboard` → `A/AdminDashboard.tsx` → `F/admin/dashboard/AdminDashboardScreen.tsx` | Unconditional facade; admin rail. | SOURCE_INVENTORIED |
| route-admin-003 | `/admin/analytics` (L661) | `AdminAnalyticsPage` → `A/analytics/AdminAnalyticsPage.tsx` | Admin rail. | SOURCE_INVENTORIED |
| route-admin-004 | `/admin/accreditation-reports` (L662) | `AdminAccreditationReportsPage` → `A/reports/AdminAccreditationReportsPage.tsx` | Admin rail; distinct from reports. | SOURCE_INVENTORIED |
| route-admin-005 | `/admin/notifications` (L666) | `NotificationsFeedPage` → `F/shared/notifications/NotificationsFeedPage.tsx` | Shared four-role feed. | SOURCE_INVENTORIED |
| route-admin-006 | `/admin/announcements` (L667) | `AdminAnnouncementsPage` → `A/announcements/AdminAnnouncementsPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-007 | `/admin/users` (L668) | `UserListPage` → `A/users/UserListPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-008 | `/admin/users/new` (L669) | `UserForm` → `A/users/UserForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-admin-009 | `/admin/users/import` (L670) | `BulkImportPage` → `A/users/BulkImportPage.tsx` | Not institution data-import. | SOURCE_INVENTORIED |
| route-admin-010 | `/admin/users/invite` (L671) | `InviteUsersPage` → `A/users/InviteUsersPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-011 | `/admin/users/invite-parent` (L672) | `ParentInvitePage` → `A/users/ParentInvitePage.tsx` | Admin-owned invitation. | SOURCE_INVENTORIED |
| route-admin-012 | `/admin/users/:id/edit` (L673) | `UserForm` → `A/users/UserForm.tsx` | Same owner as008; edit. | SOURCE_INVENTORIED |
| route-admin-013 | `/admin/programs` (L674) | `ProgramListPage` → `A/programs/ProgramListPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-014 | `/admin/programs/new` (L675) | `ProgramForm` → `A/programs/ProgramForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-admin-015 | `/admin/programs/:id/edit` (L676) | `ProgramForm` → `A/programs/ProgramForm.tsx` | Same owner as014; edit. | SOURCE_INVENTORIED |
| route-admin-016 | `/admin/outcomes` (L677) | `ILOListPage` → `A/outcomes/ILOListPage.tsx` | ILO owner, not coordinator PLO list. | SOURCE_INVENTORIED |
| route-admin-017 | `/admin/outcomes/new` (L678) | `ILOForm` → `A/outcomes/ILOForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-admin-018 | `/admin/outcomes/:id/edit` (L679) | `ILOForm` → `A/outcomes/ILOForm.tsx` | Same owner as017; edit. | SOURCE_INVENTORIED |
| route-admin-019 | `/admin/audit-log` (L680) | `AuditLogPage` → `A/AuditLogPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-020 | `/admin/governance` (L681) | `AIGovernancePage` → `A/governance/AIGovernancePage.tsx` | No supported-execution claim. | SOURCE_INVENTORIED |
| route-admin-021 | `/admin/security` (L682) | `AdminSecurityPage` → `F/admin/security/AdminSecurityPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-022 | `/admin/bonus-events` (L683) | `BonusXPEventManager` → `A/BonusXPEventManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-023 | `/admin/courses` (L684) | `CourseListPage` → `A/courses/CourseListPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-024 | `/admin/courses/new` (L685) | `CourseForm` → `A/courses/CourseForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-admin-025 | `/admin/courses/:id/edit` (L686) | `CourseForm` → `A/courses/CourseForm.tsx` | Same owner as024; edit. | SOURCE_INVENTORIED |
| route-admin-026 | `/admin/courses/:courseId/enrollment` (L687) | `CourseEnrollmentPage` → `A/courses/CourseEnrollmentPage.tsx` | Course-scoped. | SOURCE_INVENTORIED |
| route-admin-027 | `/admin/semesters` (L691) | `SemesterManager` → `A/semesters/SemesterManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-028 | `/admin/departments` (L692) | `DepartmentManager` → `A/departments/DepartmentManager.tsx` | Also institution/structure aliases. | SOURCE_INVENTORIED |
| route-admin-029 | `/admin/onboarding/pending` (L693) | `PendingOnboardingPage` → `A/onboarding/PendingOnboardingPage.tsx` | Not student wizard. | SOURCE_INVENTORIED |
| route-admin-030 | `/admin/reports` (L697) | `ReportGeneratorPage` → `A/reports/ReportGeneratorPage.tsx` | Separate from004. | SOURCE_INVENTORIED |
| route-admin-031 | `/admin/calendar` (L698) | `AcademicCalendarManager` → `A/calendar/AcademicCalendarManager.tsx` | Not shared CalendarView. | SOURCE_INVENTORIED |
| route-admin-032 | `/admin/timetable` (L699) | `TimetableManager` → `A/timetable/TimetableManager.tsx` | Shared coordinator owner. | SOURCE_INVENTORIED |
| route-admin-033 | `/admin/fees` (L700) | `FeeManager` → `A/fees/FeeManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-034 | `/admin/import` (L701) | `DataImportPage` → `A/import/DataImportPage.tsx` | Distinct from009. | SOURCE_INVENTORIED |
| route-admin-035 | `/admin/surveys` (L702) | `SurveyManager` → `A/surveys/SurveyManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-036 | `/admin/surveys/results` (L703) | `SurveyResultsPage` → `A/surveys/SurveyResultsPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-037 | `/admin/graduate-attributes` (L704) | `GraduateAttributeManager` → `A/graduate-attributes/GraduateAttributeManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-038 | `/admin/competency-frameworks` (L708) | `CompetencyFrameworkManager` → `A/competency-frameworks/CompetencyFrameworkManager.tsx` | Shared coordinator owner. | SOURCE_INVENTORIED |
| route-admin-039 | `/admin/historical-evidence` (L712) | `HistoricalEvidenceDashboard` → `A/historical-evidence/HistoricalEvidenceDashboard.tsx` | — | SOURCE_INVENTORIED |
| route-admin-040 | `/admin/outcome-chain` (L716) | `OutcomeChainView` → `SH/OutcomeChainView.tsx` → local `OutcomeChainViewLegacy` | Admin branch; coordinator differs. | SOURCE_INVENTORIED |
| route-admin-041 | `/admin/badges` (L717) | `BadgeDefinitionsPage` → `A/badges/BadgeDefinitionsPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-042 | `/admin/badges/spotlight` (L718) | `BadgeSpotlightManager` → `A/badges/BadgeSpotlightManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-043 | `/admin/marketplace` (L722) | `MarketplaceManagementPage` → `A/marketplace/MarketplaceManagementPage.tsx` | — | SOURCE_INVENTORIED |
| route-admin-044 | `/admin/marketplace/sales` (L723) | `SaleEventManager` → `A/marketplace/SaleEventManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-045 | `/admin/marketplace/analytics` (L724) | `MarketplaceAnalyticsPage` → `A/marketplace/MarketplaceAnalyticsPage.tsx` | Distinct from003. | SOURCE_INVENTORIED |
| route-admin-046 | `/admin/marketplace/quests` (L728) | `KnowledgeQuestManager` → `A/marketplace/KnowledgeQuestManager.tsx` | — | SOURCE_INVENTORIED |
| route-admin-047 | `/admin/marketplace/economist` (L732) | `XPEconomistDashboard` → `A/marketplace/XPEconomistDashboard.tsx` | — | SOURCE_INVENTORIED |
| route-admin-048 | `/admin/settings/profile` (L736) | `AdminProfilePage` → `A/settings/AdminProfilePage.tsx` | Not shared ProfilePage. | SOURCE_INVENTORIED |
| route-admin-049 | `/admin/profile` (L737) | `AdminProfilePage` → `A/settings/AdminProfilePage.tsx` | Alias of048. | SOURCE_INVENTORIED |
| route-admin-050 | `/admin/settings/institution` (L738) | `DepartmentManager` → `A/departments/DepartmentManager.tsx` | Alias of028; not InstitutionSettings. | SOURCE_INVENTORIED |
| route-admin-051 | `/admin/structure` (L742) | `DepartmentManager` → `A/departments/DepartmentManager.tsx` | Alias of028. | SOURCE_INVENTORIED |
| route-admin-052 | `/admin/settings/configuration` (L743) | `InstitutionSettingsPage` → `A/settings/InstitutionSettings.tsx` | Distinct from050. | SOURCE_INVENTORIED |

### Coordinator — 27 declarations

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-coordinator-001 | `/coordinator` (L758) | `Navigate` → `/coordinator/dashboard` | Index; replace redirect. | SOURCE_INVENTORIED |
| route-coordinator-002 | `/coordinator/dashboard` (L762) | `CoordinatorDashboard` → `C/CoordinatorDashboard.tsx` → `F/coordinator/dashboard/CoordinatorDashboardScreen.tsx` | Unconditional facade; program rail. | SOURCE_INVENTORIED |
| route-coordinator-003 | `/coordinator/outcomes` (L765) | `PLOListPage` → `C/plos/PLOListPage.tsx` | Alias with005. | SOURCE_INVENTORIED |
| route-coordinator-004 | `/coordinator/notifications` (L766) | `NotificationsFeedPage` → `F/shared/notifications/NotificationsFeedPage.tsx` | Shared four-role feed. | SOURCE_INVENTORIED |
| route-coordinator-005 | `/coordinator/plos` (L767) | `PLOListPage` → `C/plos/PLOListPage.tsx` | Alias of003. | SOURCE_INVENTORIED |
| route-coordinator-006 | `/coordinator/plos/new` (L768) | `PLOForm` → `C/plos/PLOForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-coordinator-007 | `/coordinator/plos/:id/edit` (L769) | `PLOForm` → `C/plos/PLOForm.tsx` | Same owner as006; edit. | SOURCE_INVENTORIED |
| route-coordinator-008 | `/coordinator/matrix` (L770) | `CurriculumMatrixPage` → `C/CurriculumMatrixPage.tsx` → `C/CurriculumMatrixPageNew.tsx` | Embedded `H/CurriculumMatrix.tsx`, `H/CellDetailSheet.tsx`; no current facade flag. | SOURCE_INVENTORIED |
| route-coordinator-009 | `/coordinator/cqi` (L771) | `CQIManager` → `C/cqi/CQIManager.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-010 | `/coordinator/course-file` (L772) | `CourseFileGenerator` → `C/course-file/CourseFileGenerator.tsx` → `C/course-file/CoordinatorAccreditationNew.tsx` | Unconditional facade; alias with011. | SOURCE_INVENTORIED |
| route-coordinator-011 | `/coordinator/accreditation` (L773) | `CourseFileGenerator` → `C/course-file/CourseFileGenerator.tsx` → `C/course-file/CoordinatorAccreditationNew.tsx` | Alias of010. | SOURCE_INVENTORIED |
| route-coordinator-012 | `/coordinator/team-health` (L774) | `TeamHealthReportPage` → `T/teams/TeamHealthReportPage.tsx` | Explicit `courseScope="all"`. | SOURCE_INVENTORIED |
| route-coordinator-013 | `/coordinator/competencies` (L778) | `CompetencyFrameworkManager` → `A/competency-frameworks/CompetencyFrameworkManager.tsx` | Shared admin owner. | SOURCE_INVENTORIED |
| route-coordinator-014 | `/coordinator/discussions` (L782) | `CourseToolRedirect` → `T/CourseToolRedirect.tsx` | First-course adapter; coordinator/discussions props; loading/empty branches. | SOURCE_INVENTORIED |
| route-coordinator-015 | `/coordinator/courses/:courseId/discussions` (L788) | `DiscussionModeration` → `T/discussions/DiscussionModeration.tsx` | Explicit `basePath="/coordinator"`. | SOURCE_INVENTORIED |
| route-coordinator-016 | `/coordinator/courses/:courseId/discussions/:threadId` (L792) | `ThreadDetail` → `ST/discussions/ThreadDetail.tsx` | Student-named shared module. | SOURCE_INVENTORIED |
| route-coordinator-017 | `/coordinator/sankey` (L796) | `SankeyDiagramView` → `C/sankey/SankeyDiagramView.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-018 | `/coordinator/gap-analysis` (L797) | `GapAnalysisView` → `C/gap-analysis/GapAnalysisView.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-019 | `/coordinator/coverage-heatmap` (L798) | `CoverageHeatmapView` → `C/coverage-heatmap/CoverageHeatmapView.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-020 | `/coordinator/unit-close/:courseId` (L799) | `UnitCloseReviewPage` → `C/unit-close/UnitCloseReviewPage.tsx` | Course-scoped. | SOURCE_INVENTORIED |
| route-coordinator-021 | `/coordinator/trends` (L803) | `SemesterTrendView` → `C/trends/SemesterTrendView.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-022 | `/coordinator/cohort-comparison` (L804) | `CohortComparisonView` → `C/cohort-comparison/CohortComparisonView.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-023 | `/coordinator/outcome-chain` (L808) | `OutcomeChainView` → `SH/OutcomeChainView.tsx` → `H/CoordinatorOutcomeAttainmentNew.tsx` | Coordinator role; otherwise local Legacy. | SOURCE_INVENTORIED |
| route-coordinator-024 | `/coordinator/timetable` (L809) | `TimetableManager` → `A/timetable/TimetableManager.tsx` | Shared admin owner. | SOURCE_INVENTORIED |
| route-coordinator-025 | `/coordinator/sessions` (L810) | `SessionManagement` → `SH/SessionManagement.tsx` | — | SOURCE_INVENTORIED |
| route-coordinator-026 | `/coordinator/settings/profile` (L811) | `ProfilePage` → `SH/ProfilePage.tsx` → `H/CoordinatorProfileNew.tsx` | Coordinator role; otherwise local ProfilePageLegacy. | SOURCE_INVENTORIED |
| route-coordinator-027 | `/coordinator/profile` (L812) | `ProfilePage` → `SH/ProfilePage.tsx` → `H/CoordinatorProfileNew.tsx` | Alias of026; same role dispatch. | SOURCE_INVENTORIED |

### Teacher — 54 declarations

Every teacher outlet currently receives TeacherDashboardRail through TeacherLayout. Critical path constants below were resolved from source, not inferred from names.

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-teacher-001 | `/teacher` (L824) | `Navigate` → `/teacher/dashboard` | Index; criticalRoutes constant; replace redirect. | SOURCE_INVENTORIED |
| route-teacher-002 | `/teacher/dashboard` (L830) | `TeacherDashboard` → `T/TeacherDashboard.tsx` → `F/teacher/dashboard/TeacherDashboardScreen.tsx` | Unconditional facade; critical segment. | SOURCE_INVENTORIED |
| route-teacher-003 | `/teacher/students` (L834) | `TeacherStudentsPage` → `T/students/TeacherStudentsPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-004 | `/teacher/notifications` (L835) | `NotificationsFeedPage` → `F/shared/notifications/NotificationsFeedPage.tsx` | Shared four-role feed. | SOURCE_INVENTORIED |
| route-teacher-005 | `/teacher/clos` (L836) | `CLOListPage` → `T/clos/CLOListPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-006 | `/teacher/clos/new` (L837) | `CLOForm` → `T/clos/CLOForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-007 | `/teacher/clos/:id/edit` (L838) | `CLOForm` → `T/clos/CLOForm.tsx` | Same owner as006; edit. | SOURCE_INVENTORIED |
| route-teacher-008 | `/teacher/clos/:id` (L839) | `CLODetailPage` → `T/clos/CLODetailPage.tsx` | Detail, not edit. | SOURCE_INVENTORIED |
| route-teacher-009 | `/teacher/rubrics` (L840) | `RubricListPage` → `T/rubrics/RubricListPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-010 | `/teacher/rubrics/new` (L841) | `RubricBuilder` → `T/rubrics/RubricBuilder.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-011 | `/teacher/rubrics/:id/edit` (L842) | `RubricBuilder` → `T/rubrics/RubricBuilder.tsx` | Same owner as010; edit. | SOURCE_INVENTORIED |
| route-teacher-012 | `/teacher/assignments` (L843) | `AssignmentListPage` → `T/assignments/AssignmentListPage.tsx` | Critical segment. | SOURCE_INVENTORIED |
| route-teacher-013 | `/teacher/assignments/new` (L847) | `AssignmentForm` → `T/assignments/AssignmentForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-014 | `/teacher/assignments/:id/edit` (L848) | `AssignmentForm` → `T/assignments/AssignmentForm.tsx` | Same owner as013; edit. | SOURCE_INVENTORIED |
| route-teacher-015 | `/teacher/grading` (L849) | `GradingQueuePage` → `T/grading/GradingQueuePage.tsx` | Critical gradingQueue segment. | SOURCE_INVENTORIED |
| route-teacher-016 | `/teacher/grading/:submissionId` (L853) | `GradingInterface` → `T/grading/GradingInterface.tsx` | Critical gradingSubmission segment. | SOURCE_INVENTORIED |
| route-teacher-017 | `/teacher/baseline/:courseId` (L857) | `BaselineResultsPage` → `T/baseline/BaselineResultsPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-018 | `/teacher/baseline/:courseId/config` (L861) | `BaselineConfigPage` → `T/baseline/BaselineConfigPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-019 | `/teacher/baseline/:courseId/questions/new` (L865) | `BaselineQuestionForm` → `T/baseline/BaselineQuestionForm.tsx` | Create question. | SOURCE_INVENTORIED |
| route-teacher-020 | `/teacher/baseline` (L869) | `BaselineCoursesListPage` → `T/baseline/BaselineCoursesListPage.tsx` | Distinct from course results017. | SOURCE_INVENTORIED |
| route-teacher-021 | `/teacher/courses/:courseId/generate-questions` (L870) | `GenerateQuestionsPage` → `T/quiz-generation/GenerateQuestionsPage.tsx` | Course-scoped. | SOURCE_INVENTORIED |
| route-teacher-022 | `/teacher/courses/:courseId/review-queue` (L874) | `ReviewQueuePage` → `T/quiz-generation/ReviewQueuePage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-023 | `/teacher/courses/:courseId/question-bank` (L878) | `QuestionBankPage` → `T/quiz-generation/QuestionBankPage.tsx` | Target of024 after course resolution. | SOURCE_INVENTORIED |
| route-teacher-024 | `/teacher/questions` (L882) | `CourseToolRedirect` → `T/CourseToolRedirect.tsx` | Teacher/question-bank props; first visible teacher-owned course or loading/empty. | SOURCE_INVENTORIED |
| route-teacher-025 | `/teacher/courses/:courseId/question-analytics` (L888) | `QuestionAnalyticsDashboard` → `T/quiz-analytics/QuestionAnalyticsDashboard.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-026 | `/teacher/courses/:courseId/quiz-clo-correlation/:quizId` (L892) | `QuizCLOCorrelationPage` → `T/quiz-analytics/QuizCLOCorrelationPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-027 | `/teacher/courses/:courseId/quizzes/new` (L896) | `QuizForm` → `T/quizzes/QuizForm.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-028 | `/teacher/courses/:courseId/quizzes/:id/edit` (L900) | `QuizForm` → `T/quizzes/QuizForm.tsx` | Same owner as027; edit. | SOURCE_INVENTORIED |
| route-teacher-029 | `/teacher/courses/:courseId/explanation-review` (L904) | `ExplanationReviewPage` → `T/quiz-generation/ExplanationReviewPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-030 | `/teacher/announcements` (L908) | `AnnouncementEditor` → `T/announcements/AnnouncementEditor.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-031 | `/teacher/modules` (L909) | `ModuleManager` → `T/courses/ModuleManager.tsx` | Destination of legacy content redirect050. | SOURCE_INVENTORIED |
| route-teacher-032 | `/teacher/courses/:courseId/discussions` (L910) | `DiscussionModeration` → `T/discussions/DiscussionModeration.tsx` | Default basePath; coordinator supplies override. | SOURCE_INVENTORIED |
| route-teacher-033 | `/teacher/discussions` (L914) | `CourseToolRedirect` → `T/CourseToolRedirect.tsx` | Teacher/discussions props; data-dependent redirect. | SOURCE_INVENTORIED |
| route-teacher-034 | `/teacher/courses/:courseId/discussions/:threadId` (L920) | `ThreadDetail` → `ST/discussions/ThreadDetail.tsx` | Student-named shared owner. | SOURCE_INVENTORIED |
| route-teacher-035 | `/teacher/attendance` (L924) | `AttendanceMarker` → `T/attendance/AttendanceMarker.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-036 | `/teacher/attendance/report` (L925) | `AttendanceReport` → `T/attendance/AttendanceReport.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-037 | `/teacher/gradebook` (L926) | `GradebookView` → `T/gradebook/GradebookView.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-038 | `/teacher/teams` (L927) | `TeamManagementPage` → `T/teams/TeamManagementPage.tsx` | Distinct from039. | SOURCE_INVENTORIED |
| route-teacher-039 | `/teacher/teams/manage` (L928) | `TeamManager` → `T/teams/TeamManager.tsx` | Not an alias of038. | SOURCE_INVENTORIED |
| route-teacher-040 | `/teacher/teams/new` (L929) | `TeamFormPage` → `T/teams/TeamFormPage.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-041 | `/teacher/teams/:id/edit` (L930) | `TeamFormPage` → `T/teams/TeamFormPage.tsx` | Same owner as040; edit. | SOURCE_INVENTORIED |
| route-teacher-042 | `/teacher/challenges` (L931) | `TeacherChallengeListPage` → `T/challenges/TeacherChallengeListPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-043 | `/teacher/challenges/new` (L932) | `ChallengeFormPage` → `T/challenges/ChallengeFormPage.tsx` | Create. | SOURCE_INVENTORIED |
| route-teacher-044 | `/teacher/challenges/:id/edit` (L933) | `ChallengeFormPage` → `T/challenges/ChallengeFormPage.tsx` | Same owner as043; edit. | SOURCE_INVENTORIED |
| route-teacher-045 | `/teacher/team-health` (L934) | `TeamHealthReportPage` → `T/teams/TeamHealthReportPage.tsx` | Default scope; coordinator explicitly uses all. | SOURCE_INVENTORIED |
| route-teacher-046 | `/teacher/clos/:cloId/sub-clos` (L935) | `SubCLOManager` → `T/outcomes/SubCLOManager.tsx` | Scoped variant of shared owner047. | SOURCE_INVENTORIED |
| route-teacher-047 | `/teacher/outcomes/sub-clos` (L936) | `SubCLOManager` → `T/outcomes/SubCLOManager.tsx` | No URL cloId; not identical parameter state. | SOURCE_INVENTORIED |
| route-teacher-048 | `/teacher/tutor-analytics` (L937) | `TutorAnalyticsPage` → `T/tutor-analytics/TutorAnalyticsPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-049 | `/teacher/content-review` (L938) | `ContentReviewPage` → `T/content/ContentReviewPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-050 | `/teacher/content` (L940) | `Navigate` → `/teacher/modules` | Legacy alias implemented as replace redirect. | SOURCE_INVENTORIED |
| route-teacher-051 | `/teacher/tutor-handoffs` (L944) | `TeacherHandoffPage` → `T/tutor-analytics/TeacherHandoffPage.tsx` | — | SOURCE_INVENTORIED |
| route-teacher-052 | `/teacher/calendar` (L945) | `CalendarView` → `SH/CalendarView.tsx` | Shared student owner; teacher shell rail still mounted. | SOURCE_INVENTORIED |
| route-teacher-053 | `/teacher/timetable` (L946) | `TimetableView` → `SH/TimetableView.tsx` | Read view, not admin/coordinator manager. | SOURCE_INVENTORIED |
| route-teacher-054 | `/teacher/settings/profile` (L947) | `TeacherProfilePage` → `T/settings/TeacherProfilePage.tsx` | — | SOURCE_INVENTORIED |

### Student — 56 declarations, including standalone focus

Rows001–055 are declared beneath StudentLayout but can be replaced by the Day1 onboarding wizard before their outlet renders. Row015 is immersive after that gate. Row056 is a separately guarded top-level route and bypasses StudentLayout; its presence in the role count must not imply the layout onboarding substitution applies to it.

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-student-001 | `/student` (L959) | `Navigate` → `/student/dashboard` | Index; criticalRoutes constant; replace redirect. | SOURCE_INVENTORIED |
| route-student-002 | `/student/dashboard` (L965) | `StudentDashboard` → `ST/StudentDashboard.tsx` → `F/student/dashboard/StudentDashboardScreen.tsx` | Unconditional facade; critical segment. | SOURCE_INVENTORIED |
| route-student-003 | `/student/assignments` (L969) | `StudentAssignmentListPage` → `ST/assignments/AssignmentListPage.tsx` → `F/student/assignments/AssignmentListScreen.tsx` | Critical segment. | SOURCE_INVENTORIED |
| route-student-004 | `/student/assignments/:id` (L973) | `StudentAssignmentDetailPage` → `ST/assignments/AssignmentDetailPage.tsx` → `F/student/assignments/AssignmentDetailScreen.tsx` | Critical segment; assignment rail. | SOURCE_INVENTORIED |
| route-student-005 | `/student/leaderboard` (L977) | `LeaderboardPage` → `ST/leaderboard/LeaderboardPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-006 | `/student/friends` (L978) | `StudentFriendsPage` → `F/student/friends/StudentFriendsPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-007 | `/student/onboarding/complete-profile` (L979) | `CompleteProfilePage` → `ST/onboarding/CompleteProfilePage.tsx` | Layout may substitute wizard first. | SOURCE_INVENTORIED |
| route-student-008 | `/student/onboarding` (L983) | `OnboardingWizardPage` → `ST/onboarding/OnboardingWizard.tsx` | Same module also mounted by layout with isDay1. | SOURCE_INVENTORIED |
| route-student-009 | `/student/habits` (L984) | `HabitHeatmapPage` → `ST/habits/HabitHeatmapPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-010 | `/student/habits/analytics` (L985) | `HabitAnalyticsPage` → `ST/habits/HabitAnalyticsPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-011 | `/student/planner` (L986) | `WeeklyPlannerPage` → `ST/planner/WeeklyPlannerPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-012 | `/student/planner/starter-week` (L987) | `StarterWeekPlanPage` → `ST/planner/StarterWeekPlanPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-013 | `/student/today` (L991) | `TodayViewPage` → `ST/planner/TodayViewPage.tsx` | Not dashboard alias. | SOURCE_INVENTORIED |
| route-student-014 | `/student/settings/reassessment` (L992) | `ReassessmentPage` → `ST/settings/ReassessmentPage.tsx` | Settings rail. | SOURCE_INVENTORIED |
| route-student-015 | `/student/quizzes/:quizId/adaptive` (L996) | `AdaptiveQuizSession` → `ST/quiz/AdaptiveQuizSession.tsx` | Immersive outlet, no RoleAppShell; onboarding precedes branch. | SOURCE_INVENTORIED |
| route-student-016 | `/student/quizzes/:quizId/review/:attemptId` (L1000) | `PostQuizReview` → `ST/quiz/PostQuizReview.tsx` → `F/student/quizzes/PostQuizReviewScreen.tsx` | Not adaptive immersive-path match. | SOURCE_INVENTORIED |
| route-student-017 | `/student/courses/:courseId/recovery/:cloId` (L1004) | `MasteryRecoveryPage` → `ST/recovery/MasteryRecoveryPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-018 | `/student/settings/profile` (L1008) | `ProfileSettingsPage` → `ST/settings/ProfileSettingsPage.tsx` | Not public-facing student profile033. | SOURCE_INVENTORIED |
| route-student-019 | `/student/xp-history` (L1009) | `XPHistory` → `ST/progress/XPHistory.tsx` → `ST/progress/XPHistoryNew.tsx` | Unconditional facade; critical segment. | SOURCE_INVENTORIED |
| route-student-020 | `/student/portfolio` (L1013) | `StudentPortfolio` → `ST/portfolio/StudentPortfolio.tsx` → `ST/portfolio/StudentPortfolioNew.tsx` | Not public portfolio route. | SOURCE_INVENTORIED |
| route-student-021 | `/student/calendar` (L1014) | `CalendarView` → `SH/CalendarView.tsx` | Internal calendar rail; no app-shell rail. | SOURCE_INVENTORIED |
| route-student-022 | `/student/timetable` (L1015) | `TimetableView` → `SH/TimetableView.tsx` | Shared teacher owner. | SOURCE_INVENTORIED |
| route-student-023 | `/student/notification-preferences` (L1016) | `NotificationPreferences` → `SH/NotificationPreferences.tsx` | — | SOURCE_INVENTORIED |
| route-student-024 | `/student/sessions` (L1020) | `SessionManagement` → `SH/SessionManagement.tsx` | Shared coordinator owner. | SOURCE_INVENTORIED |
| route-student-025 | `/student/surveys` (L1021) | `SurveyResponsePage` → `ST/surveys/SurveyResponsePage.tsx` | — | SOURCE_INVENTORIED |
| route-student-026 | `/student/announcements/:announcementId` (L1022) | `StudentAnnouncementDetail` → `ST/announcements/AnnouncementDetail.tsx` | — | SOURCE_INVENTORIED |
| route-student-027 | `/student/courses` (L1026) | `StudentCoursesListPage` → `ST/courses/StudentCoursesPage.tsx` → `ST/courses/StudentCoursesNew.tsx` | Unconditional facade; learn rail. | SOURCE_INVENTORIED |
| route-student-028 | `/student/courses/:courseId` (L1027) | `StudentCourseDetail` → `ST/courses/CourseDetail.tsx` → `F/student/courses/CourseDetailScreen.tsx` | Learn rail; shared with041. | SOURCE_INVENTORIED |
| route-student-029 | `/student/progress` (L1028) | `StudentProgressPage` → `ST/progress/StudentProgressPage.tsx` → `ST/progress/StudentProgressNew.tsx` → `F/student/progress/index.ts` → `StudentProgressScreen.tsx` | Stable route/facades; feature now owns the main screen. Separate progress rail unchanged. | SOURCE_INVENTORIED |
| route-student-030 | `/student/progress/clos` (L1029) | `CLOProgress` → `ST/progress/CLOProgress.tsx` | Distinct owner, not progress alias. | SOURCE_INVENTORIED |
| route-student-031 | `/student/learning-path` (L1030) | `LearningPathPage` → `ST/progress/LearningPathPage.tsx` | Embeds `ST/progress/LearningPath.tsx`; no shell rail. | SOURCE_INVENTORIED |
| route-student-032 | `/student/badges` (L1031) | `StudentBadgesPage` → `ST/badges/StudentBadgesPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-033 | `/student/profile` (L1032) | `StudentProfilePage` → `F/student/profile/StudentProfilePage.tsx` | Profile rail; not settings018. | SOURCE_INVENTORIED |
| route-student-034 | `/student/learning-profile` (L1033) | `LearningProfilePage` → `ST/profile/LearningProfilePage.tsx` | Learning-profile rail. | SOURCE_INVENTORIED |
| route-student-035 | `/student/journal` (L1034) | `StudentJournalPage` → `ST/journal/StudentJournalPage.tsx` → `ST/journal/StudentJournalNew.tsx` | Unconditional facade; early rail-free branch. | SOURCE_INVENTORIED |
| route-student-036 | `/student/journal/new` (L1035) | `StudentJournalEditorPage` → `ST/journal/JournalEditor.tsx` | Create. | SOURCE_INVENTORIED |
| route-student-037 | `/student/journal/:id` (L1036) | `StudentJournalEditorPage` → `ST/journal/JournalEditor.tsx` | Same owner as036; existing-entry mode. | SOURCE_INVENTORIED |
| route-student-038 | `/student/transcript` (L1037) | `StudentTranscriptPage` → `F/student/transcript/StudentTranscriptPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-039 | `/student/fees` (L1038) | `StudentFeesPage` → `F/student/fees/StudentFeesPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-040 | `/student/notifications` (L1039) | `NotificationsFeedPage` → `F/shared/notifications/NotificationsFeedPage.tsx` | Shared four-role feed. | SOURCE_INVENTORIED |
| route-student-041 | `/student/courses/:courseId/materials/:materialId` (L1040) | `StudentCourseDetail` → `ST/courses/CourseDetail.tsx` → `F/student/courses/CourseDetailScreen.tsx` | Declared material deep link; current screen reads courseId, not proof material selected; fallback rail. | SOURCE_INVENTORIED |
| route-student-042 | `/student/courses/:courseId/discussions` (L1044) | `DiscussionForum` → `ST/discussions/DiscussionForum.tsx` | Not moderation owner. | SOURCE_INVENTORIED |
| route-student-043 | `/student/courses/:courseId/discussions/:threadId` (L1048) | `ThreadDetail` → `ST/discussions/ThreadDetail.tsx` | Shared teacher/coordinator owner. | SOURCE_INVENTORIED |
| route-student-044 | `/student/challenges` (L1052) | `ChallengeListView` → `ST/challenges/ChallengeListView.tsx` | Distinct from045. | SOURCE_INVENTORIED |
| route-student-045 | `/student/challenges/list` (L1053) | `ChallengeListPage` → `ST/challenges/ChallengeListPage.tsx` | Not merely an alias of044. | SOURCE_INVENTORIED |
| route-student-046 | `/student/challenges/:id` (L1054) | `ChallengeDetailPage` → `ST/challenges/ChallengeDetailPage.tsx` | Detail. | SOURCE_INVENTORIED |
| route-student-047 | `/student/tutor` (L1055) | `TutorPage` → `ST/tutor/TutorPage.tsx` | No conversationId supplied by URL. | SOURCE_INVENTORIED |
| route-student-048 | `/student/tutor/:conversationId` (L1056) | `TutorPage` → `ST/tutor/TutorPage.tsx` | Same owner as047; conversation-scoped. | SOURCE_INVENTORIED |
| route-student-049 | `/student/team` (L1057) | `StudentTeamPage` → `ST/team/StudentTeamPage.tsx` | Distinct from team profile050. | SOURCE_INVENTORIED |
| route-student-050 | `/student/teams/:teamId` (L1058) | `TeamProfilePage` → `ST/teams/TeamProfilePage.tsx` | — | SOURCE_INVENTORIED |
| route-student-051 | `/student/teams/new` (L1059) | `CreateTeamPage` → `ST/teams/CreateTeamPage.tsx` | Static create route, not teamId=new detail. | SOURCE_INVENTORIED |
| route-student-052 | `/student/marketplace` (L1060) | `StudentMarketplacePage` → `ST/marketplace/MarketplacePage.tsx` | — | SOURCE_INVENTORIED |
| route-student-053 | `/student/marketplace/my-items` (L1061) | `StudentMyItemsPage` → `ST/marketplace/MyItemsPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-054 | `/student/marketplace/history` (L1065) | `StudentTransactionHistoryPage` → `ST/marketplace/TransactionHistoryPage.tsx` | — | SOURCE_INVENTORIED |
| route-student-055 | `/student/content` (L1069) | `StudentContentPage` → `ST/content/StudentContentPage.tsx` | Not teacher content redirect. | SOURCE_INVENTORIED |
| route-student-056 | `/student/focus/:sessionId` (L1073) | `FocusModePage` → `ST/planner/FocusModePage.tsx` | Standalone RouteGuard; outside StudentLayout and its onboarding/rail flow. | SOURCE_INVENTORIED |

### Parent — 13 declarations

Parent child/tenant context and verified-child authorization are not established by this route list. In particular, notifications and communications share the parent-specific owner, not the four-role NotificationsFeedPage.

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-parent-001 | `/parent` (L1091) | `Navigate` → `/parent/dashboard` | Index; replace redirect. | SOURCE_INVENTORIED |
| route-parent-002 | `/parent/dashboard` (L1095) | `ParentDashboard` → `P/ParentDashboard.tsx` → `F/parent/dashboard/ParentDashboardScreen.tsx` | Unconditional facade; dashboard rail. | SOURCE_INVENTORIED |
| route-parent-003 | `/parent/notifications` (L1096) | `ParentCommunicationsPage` → `P/communications/ParentCommunicationsPage.tsx` | Same owner as004; no global rail. | SOURCE_INVENTORIED |
| route-parent-004 | `/parent/communications` (L1100) | `ParentCommunicationsPage` → `P/communications/ParentCommunicationsPage.tsx` | Alias with003; no global rail. | SOURCE_INVENTORIED |
| route-parent-005 | `/parent/children` (L1104) | `ParentChildrenPage` → `P/ParentChildrenPage.tsx` | — | SOURCE_INVENTORIED |
| route-parent-006 | `/parent/support` (L1105) | `ParentSupportPage` → `P/support/ParentSupportPage.tsx` | Dashboard rail. | SOURCE_INVENTORIED |
| route-parent-007 | `/parent/progress` (L1106) | `ParentProgressPage` → `P/ParentProgressPage.tsx` | Dashboard rail. | SOURCE_INVENTORIED |
| route-parent-008 | `/parent/attendance` (L1107) | `ParentAttendancePage` → `P/ParentAttendancePage.tsx` | — | SOURCE_INVENTORIED |
| route-parent-009 | `/parent/fees` (L1108) | `ParentFeesPage` → `F/parent/fees/ParentFeesPage.tsx` | No global rail. | SOURCE_INVENTORIED |
| route-parent-010 | `/parent/planner` (L1109) | `ParentPlannerView` → `P/planner/ParentPlannerView.tsx` | No studentId in URL. | SOURCE_INVENTORIED |
| route-parent-011 | `/parent/planner/:studentId` (L1110) | `ParentPlannerView` → `P/planner/ParentPlannerView.tsx` | Same owner as010; explicit child parameter. | SOURCE_INVENTORIED |
| route-parent-012 | `/parent/profile` (L1111) | `ParentProfilePage` → `P/settings/ParentProfilePage.tsx` | No global rail. | SOURCE_INVENTORIED |
| route-parent-013 | `/parent/settings/profile` (L1112) | `ParentProfilePage` → `P/settings/ParentProfilePage.tsx` | Alias of012. | SOURCE_INVENTORIED |

### Public, authentication, legal, root and catch-all — 10 declarations

These routes are outside the five role-layout branches. Except the root Navigate, each is wrapped by the local `PublicMain` in AppRouter (L574–578). PublicMain is a main landmark, not a role shell or an authorization guarantee. Authentication screens may have their own internal modes and redirects; those states are not extra JSX Route declarations.

| Inventory ID | Path (source line) | Route target → resolved source owner | Caveat | Status |
|---|---|---|---|---|
| route-public-001 | `/login` (L586) | `LoginPage` → `src/pages/LoginPage.tsx` | Includes auth landing visuals and internal form modes; no role shell. | SOURCE_INVENTORIED |
| route-public-002 | `/signup` (L594) | `SignUpPage` → `src/pages/auth/SignUpPage.tsx` | Distinct from LoginPage embedded registration mode. | SOURCE_INVENTORIED |
| route-public-003 | `/accept-invite/:token` (L602) | `AcceptInvitePage` → `src/pages/auth/AcceptInvitePage.tsx` | Token is sensitive; no token values recorded by this inventory. | SOURCE_INVENTORIED |
| route-public-004 | `/reset-password` (L610) | `ResetPasswordPage` → `src/pages/ResetPasswordPage.tsx` | Auth flow states unverified. | SOURCE_INVENTORIED |
| route-public-005 | `/update-password` (L618) | `UpdatePasswordPage` → `src/pages/UpdatePasswordPage.tsx` | Recovery/session states unverified. | SOURCE_INVENTORIED |
| route-public-006 | `/portfolio/:student_id` (L626) | `PublicPortfolio` → `src/pages/public/PublicPortfolio.tsx` | Public owner differs from authenticated student portfolio. | SOURCE_INVENTORIED |
| route-public-007 | `/terms` (L634) | `TermsPage` → `src/pages/public/TermsPage.tsx` | Public legal page. | SOURCE_INVENTORIED |
| route-public-008 | `/privacy` (L642) | `PrivacyPage` → `src/pages/public/PrivacyPage.tsx` | Public legal page. | SOURCE_INVENTORIED |
| route-public-009 | `/` (L1116) | `Navigate` → `/login` | Root replace redirect; seventh Navigate-only leaf overall. | SOURCE_INVENTORIED |
| route-public-010 | `*` (L1119) | `NotFoundPage` → `src/pages/NotFoundPage.tsx` | Global catch-all declaration; nested unmatched-role behavior requires runtime proof. | SOURCE_INVENTORIED |

### Supplemental layout, guard, fallback and global-surface ownership

These rows are **not** additional leaf routes. The five layout rows reconcile the212 leaf declarations with217 JSX Route declarations overall. Guard redirects, Suspense fallbacks, portals, overlays and child components can replace or surround route bodies without adding Route declarations.

| Inventory ID | Scope / source | Owner chain and condition | Status |
|---|---|---|---|
| route-layout-admin | `/admin/*`, AppRouter L651 | `src/router/RouteGuard.tsx` (admin) → `A/AdminLayout.tsx` → `src/app/RoleAppShell.tsx` + Outlet. AdminDashboardRail only for exact `/admin`, `/admin/dashboard`, `/admin/analytics`, `/admin/accreditation-reports`. | SOURCE_INVENTORIED |
| route-layout-coordinator | `/coordinator/*`, AppRouter L750 | RouteGuard (coordinator) → `C/CoordinatorLayout.tsx` → RoleAppShell + Outlet. CoordinatorDashboardRail only for exact root/dashboard. | SOURCE_INVENTORIED |
| route-layout-teacher | `/teacher/*`, AppRouter L816 | RouteGuard (teacher) → `T/TeacherLayout.tsx` → RoleAppShell + Outlet; `F/teacher/dashboard/TeacherDashboardRail.tsx` passed for every teacher route. | SOURCE_INVENTORIED |
| route-layout-student | `/student/*`, AppRouter L951 | RouteGuard (student) → `ST/StudentLayout.tsx`; onboarding substitution and immersive/rail-free/rail-selected branches below precede ordinary Outlet composition. | SOURCE_INVENTORIED |
| route-layout-parent | `/parent/*`, AppRouter L1083 | RouteGuard (parent) → `P/ParentLayout.tsx` → RoleAppShell + Outlet. `F/parent/dashboard/ParentDashboardRail.tsx` when pathname includes parent dashboard/progress/support; other declared parent routes omit it. | SOURCE_INVENTORIED |
| route-surface-guard | All five layouts and standalone focus | `src/router/RouteGuard.tsx`: loading spinner (L22–27); unauthenticated replace redirect to login (L30–31); missing/wrong role redirect to login or that role's mapped dashboard (L34–36). No independent access-denied Route. These conditions were read, not exercised. | SOURCE_INVENTORIED |
| route-surface-onboarding | StudentLayout L46–54 | If student profile has onboarding_completed=false, lazy `ST/onboarding/OnboardingWizard.tsx` with isDay1 replaces the outlet, under a fixed white Suspense fallback. Step owners/gates require separate inventory; standalone focus bypasses this layout. | SOURCE_INVENTORIED |
| route-surface-immersive | StudentLayout L60–71 | Adaptive-quiz pathname renders immersive wrapper + Outlet without RoleAppShell. Layout also contains a focus regex, but the declared focus leaf is outside this layout. Onboarding is checked first. | SOURCE_INVENTORIED |
| route-surface-student-rails | StudentLayout L25–41,74–114 | Learning-path, calendar and journal early branches omit the shell rail. Otherwise select dashboard/learn/progress/assignment/profile/learning-profile/settings rails or StudentFallbackRail. StudentJournalRail's map entry is bypassed by the early journal branch; do not count it as mounted coverage. | SOURCE_INVENTORIED |
| route-surface-course-adapters | Coordinator014; teacher024/033 | `T/CourseToolRedirect.tsx`: loading Shimmer; no course NoData; otherwise Navigate replace to first role-visible course's requested tool. Teacher filters teacher_id; coordinator uses hook-visible courses. Error and authorization behavior not certified. | SOURCE_INVENTORIED |
| route-surface-role-dispatch | Coordinator023/026/027; admin040 | `SH/OutcomeChainView.tsx` L407–412 dispatches coordinator to `H/CoordinatorOutcomeAttainmentNew.tsx`, otherwise Legacy. `SH/ProfilePage.tsx` L412–417 dispatches coordinator to `H/CoordinatorProfileNew.tsx`, otherwise ProfilePageLegacy. Profile hydration/error branches need real tests. | SOURCE_INVENTORIED |
| route-surface-shell | Normal role-layout branches | `src/app/RoleAppShell.tsx`: `H/GlobalHeader.tsx`, `H/Sidebar.tsx`, `H/SidebarContext.tsx`, `H/MobileTabBar.tsx`, `H/EmailVerificationBanner.tsx`, `H/GuidedTour.tsx`; optional rail; main landmark and responsive grid. Internal controls/menus/portals are not covered by route inventory. | SOURCE_INVENTORIED |
| route-surface-intelligence | RoleAppShell L13–16,77–81 | Lazy `H/EDeviserIntelligencePanel.tsx` rendered only when isAiSurfaceEnabled from `src/ai/lib/featureGate.ts`; null Suspense fallback. This is a real source condition, not proof the feature is enabled at runtime. | SOURCE_INVENTORIED |
| route-surface-public-main | Public/auth/legal/catch-all | Local PublicMain in `src/router/AppRouter.tsx` L574–578 wraps main-content; no role navigation. Auth-specific `F/auth/landing/AuthLandingVisuals.tsx` and `F/auth/landing/AuthLanding.css` remain deeper dependencies, not separate routes. | SOURCE_INVENTORIED |
| route-surface-route-loading | Entire Routes tree | Local LoadingFallback in `src/router/AppRouter.tsx` L562–572 is supplied to Suspense atL583. Distinct from RouteGuard spinner and student onboarding fallback. | SOURCE_INVENTORIED |
| route-surface-route-error | Entire Routes tree | `H/ErrorBoundary.tsx` with local PageErrorFallback in `src/router/AppRouter.tsx` (rendered atL582); page-failed text and reload action, not a dedicated error URL. Inner page boundaries remain separate. | SOURCE_INVENTORIED |
| route-surface-app-error | Outside BrowserRouter | `src/App.tsx` L32–60 uses an outer `H/ErrorBoundary.tsx`; its default fallback and recovery need separate tests from the router's explicit fallback. | SOURCE_INVENTORIED |
| route-surface-app-providers | Global, including public routes | `src/App.tsx`: BrowserRouter, NuqsAdapter, QueryClientProvider, MotionConfig, `src/providers/AuthProvider.tsx`, `src/providers/LanguageProvider.tsx`, `src/providers/ThemeProvider.tsx`. Identity, locale, reduced motion and portal inheritance are not implied by route status. | SOURCE_INVENTORIED |
| route-surface-app-overlays | Global App children outside AppRouter | `H/SkipToMain.tsx`, `H/GamificationFeedbackHost.tsx`, `H/CookieConsentBanner.tsx`, `src/components/ui/sonner.tsx` Toaster. Reward queue can mount XP/badge/level-up overlays; internal event conditions and portal paint need separate coverage. | SOURCE_INVENTORIED |
| route-surface-app-external | App L20–24,50–59 | ReactQueryDevtools lazy mount only under import.meta.env.DEV; Vercel SpeedInsights and Analytics vendor components outside BrowserRouter. No standalone routes; deployed telemetry/consent behavior not verified. | SOURCE_INVENTORIED |

### Coverage boundary and next matrix expansion

This inventory accounts for **declarations and resolved facade ownership**, not all pages' rendered states or all components. No application test, build, browser job, live Supabase call or deployment was performed for this documentation addition. Prior batch evidence remains scoped to the owners/states it actually exercised.

Next durable work must attach component/state/rendered evidence to these IDs without upgrading their statuses merely because a route renders once:

1. Inventory each route's query loading, empty, error, partial, permission, unknown, stale and populated states; create/edit/detail parameter variants; redirects; internal tabs/dialogs; mutation success/failure and protected approval boundaries. Preserve native assessment/domain contracts and distinguish configured values from evidence-backed results.
2. Expand the shared shell/rails before page-specific overrides. Additional rail owners are `F/admin/dashboard/AdminDashboardRail.tsx`, `F/coordinator/dashboard/CoordinatorDashboardRail.tsx`, `F/teacher/dashboard/TeacherDashboardRail.tsx`, `F/parent/dashboard/ParentDashboardRail.tsx`, `F/student/dashboard/StudentDashboardRail.tsx`, and `F/student/rails/StudentLearnRail.tsx`, `F/student/rails/StudentProgressRail.tsx`, `F/student/rails/StudentJournalRail.tsx`, `F/student/rails/StudentAssignmentRail.tsx`, `F/student/rails/StudentFallbackRail.tsx`, `F/student/rails/StudentProfileRail.tsx`, `F/student/rails/StudentLearningProfileRail.tsx`, `F/student/rails/StudentSettingsRail.tsx`. Their deep contents and every mounting state are not certified here.
3. Explicitly unfinished deeper owners include CRUD columns/form sections, DataTable/ConfirmDialog and primitive portals; CurriculumMatrix/CellDetailSheet/HeatmapLegend; CoordinatorProfileNew/CoordinatorOutcomeAttainmentNew/CoordinatorInsightRail; LearningPath stage views; onboarding step components; quiz/review/tutor ChatPanel; team/challenge/marketplace cards; parent child-context/profile/reminder controls; timetable/calendar/fee/import/survey/report subcomponents; AgentTaskInbox, notification/account/search widgets and intelligence/security/governance panels. This is a priority list, not an exhaustive transitive dependency closure. Hooks, framework-free derivations, locale dictionaries, auth/tenant/child authorization and all conditional imports still require their appropriate scoped review.
4. Verify coherent shared theme ownership on actual components, including inherited card/text pairs and portalled controls; English/LTR and Arabic/RTL, light/dark/live theme transitions, narrow/wide/large-text layouts, keyboard/touch/reduced motion and unmeasured/failed states. Auth landing identity, fonts/assets, onboarding replacement, immersive quiz and standalone focus require their own chrome-aware cases.
5. Keep SOURCE_INVENTORIED separate from implemented, connected, deployed, verified and customer-ready. This202-role/212-leaf inventory is not a100% completion claim or permission to change protected contracts. Full integrated gates and route/state evidence remain parent-owned work.

Scoped documentation hygiene passed:212/212 rows matched the current AST's source line, URL pattern, target symbol and complete default re-export chain; role/public counts52/27/54/56/13/10;232 unique inventory IDs (212 leaf rows plus20 supplemental rows, including five layouts); all statuses SOURCE_INVENTORIED;283 abbreviated/literal source-path references and both new local Markdown links exist. Scoped `git diff --check` passed. At the append-only checkpoint, before the parent authorized the subsequent Batch7 completion update, the entire preexisting ledger prefix retained normalized SHA-256 `6c56f090f016cd35b5d848cb9a98e316b4c6f94b2da2702d36e3fda7f5f17af0` (LF-normalized, trailing whitespace excluded). The later authorized update changes only current completion wording and adds the collected127 failure/128 success provenance; the original70 finding IDs/statuses remain unchanged. This is documentation/source integrity evidence only.
