# Platform Hardening & Integration â€” Tasks

Format: `- [ ] T# (req) description â€” status/evidence appended when done.`

## Phase 1 â€” P0 deploy
- [x] T1 (E1.1) Deploy fixed agent-orchestrator; verify zero boot errors + OPTIONS 200 + all AI channels live. _(DONE 2026-09-02: CLI deploy 167kB; live OPTIONS â†’ 200 with CORS headers; function_logs zero boot errors post-deploy. check:edge-imports gate green on 99 files pre-deploy.)_

## Phase 2 â€” Verified bug & infra fixes (PR `fix/production-console-errors`)
- [x] T2 (E1.5) Rubrics `created_by` default/policy migration (MCP + mirror) + client sends created_by. _(DONE 2026-09-02: MCP migration `rubrics_created_by_default_auth_uid` applied live â€” `created_by` now `DEFAULT auth.uid()`; `useCreateRubric` sends `created_by: user.id` explicitly. Parity mirror file still to be committed with the PR.)_
- [x] T3 (E1.6) Fix RubricBuilder criteria-name validation. _(DONE 2026-09-02: validation was CORRECT (default criteria seed with empty names) but opaque â€” toasts now name the offending criterion number: "Criterion N needs a nameâ€¦"; same for level labels.)_
- [x] T4 (E1.7) Sort-order integer coercion + localized messages (ModuleManager, GradeCategoryManager, audit others). _(DONE 2026-09-02: new `src/lib/schemas/sortOrder.ts` â€” `sortOrderSchema` with friendly messages + `normalizeSortOrderInput` boundary helper; wired into ModuleManager (both sort_order inputs normalize on change), `courseModule.ts`, `gradeCategory.ts`. Root cause: raw number inputs submit ""/NaN â†’ Zod default "Invalid input: expected number, received NaN". 5 unit tests incl. never-surface-"Invalid input" invariant; tsc clean.)_
- [x] T5 (E1.8) Per-key pending for notification preference toggles (all settings screens). _(DONE 2026-09-02: root cause = `EmailPreferencesSection` had `disabled={isUpdating}` on EVERY switch (shared mutation pending). New `updateSinglePreference` mutation in `useEmailPreferences` â€” optimistic setQueryData flip, per-key `isKeyPending`, rollback on error. Only the clicked switch disables; others stay live. Old whole-object API kept for compatibility; existing 5-test suite green; tsc clean.)_
- [x] T6 (E1.13) SW same-origin only + CACHE_NAME v3. _(DONE 2026-09-02: sw.js fetch handler now returns early for any cross-origin request â€” fonts/CDNs handled natively, no index.html fallback possible; cache bumped to edeviser-v3 to force client update.)_
- [x] T7 (E1.14) CSP img-src += images.unsplash.com. _(DONE 2026-09-02: vercel.json CSP updated â€” Unsplash avatars unblocked on next deploy.)_
- [x] T8 (E1.15) Manifest icon-192/512 dims corrected. _(DONE 2026-09-02: BOTH PNGs were 4x4 pixels (!) â€” regenerated 192x192 and 512x512 from edeviser-logo-final.png (1200x1200, aspect-preserving, white canvas). Manifest declarations now match.)_
- [x] T9 (E1.16) Replace vite.svg favicon with Edeviser mark. _(DONE 2026-09-02: index.html favicon â†’ /icons/icon-192.png.)_
- [x] T10 (E1.2) Fix intervention-jobs measurement-claim 500. _(DONE 2026-09-02: failure was transient (next tick 200) but the bare error was undiagnosable â€” claim/lookup/complete failures now surface the Postgres message + SQLSTATE code. Redeployed; live tick 200 claimed:0.)_
- [x] T11 (E1.3/E1.9) Fix generate-quiz-questions 500 + document QB audit. _(DONE 2026-09-02 code+deploy: (a) LLM JSON now strips markdown fences â€” the 2026-08-30 log showed `Unexpected token '`'` from ```json-wrapped output; (b) retrieval step is now guarded + logged (the 2026-09-01 20:27Z 500 left NO quiz_generation_logs row â†’ failed pre-LLM in retrieval); warnings preserved for the client. QB hardcoded-vs-live audit: generation writes quiz_generation_logs + agent_runs + agent_action_proposals from real course/CLO/material data â€” full page-by-page audit continues in T19.)_
- [x] T12 (E1.4) Verify notification-digest CRON_SECRET binding. _(DONE 2026-09-02: function now accepts BOTH `x-cron-secret` and `Authorization: Bearer <CRON_SECRET>` (timing-safe forms). Live: 200 via both forms. The recurring 401s come from the USER's external scheduler sending no secret â€” scheduler config is user-owned: send `x-cron-secret: <CRON_SECRET>` header. Redeployed.)_
- [x] T13 (E1.19) White card headers across teacher/coordinator/student/parent. _(DONE 2026-09-02: teacher + student heroes were restyled in the `fix/production-console-errors` stack; this pass completed the remaining two â€” coordinator action-hub hero (`CoordinatorDashboardNew.tsx`) and parent AI Story Hero (`ParentDashboardScreen.tsx`) restyled to white liquid-glass (`bg-white/80 border-slate-200/60 backdrop-blur-xs` / CARD_CLASS pattern, icon containers white glass, slate text/chips), `ActionChip` restyled for the light card, unused `STORY_GRADIENT` removed.)_

## Phase 3 â€” Audits & feature repairs
- [x] T14 (E1.11) Gradebook letter-scale fix + property tests + real-data audit. _(DONE 2026-09-02: real-data audit via live MCP â€” ALL institution_settings rows carry the integer-adjacent scale (A 85â€“100, B 70â€“84, C 55â€“69, D 50â€“54, F 0â€“49) whose real gaps (84.0â€“85.0 etc.) made `mapToLetterGrade` fall back to the LOWEST grade â†’ 84.2% rendered "F". Fix (3 layers, no data migration needed): (1) `letterGradeMapper.ts` letter+GPA mapping is now TOTAL over [0,100] â€” unmatched percents map to the nearest LOWER band (84.2 â†’ B), out-of-range clamp to top/lowest; (2) `gradeScalesPartitionSchema` upgraded from integer-gap (`min > max+1`) to continuous coverage â€” strict gap (`min > max`) and strict overlap (`min < max`) rejected, boundary-touch allowed; (3) `DEFAULT_GRADE_SCALES` aligned to a touching partition (B 70â€“85, C 55â€“70, D 50â€“55, F 0â€“50) â€” all historical integer classifications unchanged. Tests: `letterGradeMapper.test.ts` E1.11 regression cases (incl. the exact live scale), `institutionSettingsSchema.test.ts` touching fixtures + SET-03/04 + legacy-scale rejection, new `gradeScale.property.test.ts` (4 fast-check properties â‰¥100 runs: totality on valid+legacy scales, 84â€“85 gap â†’ B, touching accepted / gaps+overlaps rejected) â€” 42/42 green. Existing live rows are NOT mutated: display is corrected by the mapping fallback; the hardened validator forces correct scales on the next save.)_
- [x] T15 (E1.10) Tutor handoffs: verify table/RLS/write path; fix broken link; drop `any`; regen types. _(DONE 2026-09-02: live-verified via MCP â€” `teacher_handoff_requests` table exists with all expected columns; RLS policies present (handoffs_student_own ALL, handoffs_teacher_read, handoffs_teacher_update, teachers_read_own_handoffs); 3 rows live (1 pending) so the page is not empty. `src/types/database.ts` already generates the table (line 9318) â€” no regen needed; dropped all three `(supabase as any)` casts in `useTeacherHandoffs.ts` in favor of the typed client (with null-guards on `.single()` results). Nav/route audit: `/teacher/tutor-handoffs` is registered in AppRouter and consistent across navItems, navPresentation and TeacherProfilePage â€” no broken link found. Trigger-condition enforcement (E2.F) remains T22.)_
- [x] T16 (E1.12) Attendance Marker: fix create-session backend + design-system rebuild. _(DONE 2026-09-02 (backend verified + UI header fix; full Marker UX rebuild folded into T21/E2.E): live-verified via MCP â€” `class_sessions` columns match `CreateSessionInput` exactly; RLS policies correct (`class_sessions_teacher_write` requires teacher role AND section ownership; `class_sessions_read`, `parent_read_class_sessions`); `attendance_records` policies present; `audit_logs` has an authenticated INSERT policy so the post-insert audit log cannot fail the mutation; all 16 course sections have teacher_id set (no orphan sections), 480 sessions exist (latest 2026-05-18 â€” the stale sessions the QA saw are user-owned data, not a code defect). No backend code defect found on the create path. UI: replaced the non-compliant solid-gradient "New Session" card header with the design-system `GradientCardHeader`. The E2.E record_attendance_v1 RPC + full UX rebuild remains T21.)_

## Phase 4 â€” Teacher sync (team feedback)
- [x] T17 (E2.A) Grading Queue: rubric checklist, regenerate semantics, confidence, edited-flag, Prev/Next, Why-explains-score.
  - DONE (2026-09-02). Surfaces: `src/lib/gradingInsights.ts` (pure coverage + why-explains-score), `src/pages/teacher/grading/GradingInterface.tsx` (coverage checklist badges, "Why this score?" breakdown, regenerate confirm, Prev/Next + "n of m pending", AI-provenance state), `src/hooks/useGrades.ts` (+`ai_applied`/`ai_edited_by_teacher`), `supabase/migrations/20260902133000_grades_ai_applied.sql` (MCP-applied; `grades.ai_applied boolean NOT NULL DEFAULT false` + column grants), regenerated `src/types/database.ts`. Tests: `gradingInsights.test.ts` (6) + `gradingInterface.test.tsx` (19) green; tsc clean. Live-verified column via information_schema before migration.
- [x] T18 (E2.B) Curriculum Studio: approval propagation + progress + ready-state + CLO source-of-truth.
- [x] T19 (E2.C) Question Bank: reuse hub, review-gate enforcement, live usage analytics view, approved-curriculum generation, Bloom health. _(DONE 2026-09-02, live-verified: (a) reuse hub â€” QB/ReviewQueue/Analytics pages + routes pre-existing and functional; (b) review-gate â€” `question_bank.status` live; serve-time `select-adaptive-question` already filters `status='approved'` on all 3 fetch paths; (c) NEW view `question_usage_stats_v1` (migration via MCP + repo parity mirror `20260902150000`): per-question `times_served` from `quiz_attempts.question_sequence` JSONB + analytics join, `security_invoker=true` (live-verified reloptions) so RLS applies; wired via `useQuestionUsageStats` â†’ dashboard merge â†’ sortable "Served" column; (d) approved-curriculum generation â€” `generate-quiz-questions` RAG retrieval now restricted to PUBLISHED materials only (moduleâ†’published-material allowlist on `source_material_id`, graceful warnings otherwise) + CLO scope-leak fix (requested CLOs deduped and must all resolve within the target course or 400); (e) Bloom health â€” filters/labels/coaching pre-existing in dashboard. Runtime: EDGE_FUNCTIONS (generate-quiz-questions) redeploy required post-merge.)_
- [x] T20 (E2.D) Gradebook: read-only enforcement, auto-columns, source links, auto attainment, propagation, live export, working filters. _(DONE 2026-09-02, live-verified: (a) read-only enforcement â€” no inline edit/`onCellEdit` surface exists in the matrix; grading happens only via the T17 Grade Review route; (b) auto-columns â€” matrix assembled from live grade_categories/assessments per course; (c) source links â€” NEW: `GradebookAssessment.submission_id` wired from the grades join in `useGradebook.ts`, graded assignment cells render as links to `/teacher/grading/{submission_id}` (quiz cells unlinked â€” no teacher quiz-attempt view exists), i18n `gradebook.openGradeReview` en/ar, MemoryRouter added to view tests + link href assertions; (d) auto attainment + propagation â€” verified LIVE: grade insert â†’ evidence â†’ CLO/PLO/ILO attainment rollup + XP + notification; (e) live export â€” `buildGradebookCsv` pre-existing with nullable finals; (f) filters â€” course/section/assignment via nuqs query state. No migrations, no edge functions (deploymentRequired: false).)_
- [x] T21 (E2.E) Attendance: save-triggered recalc RPC, computed %, risk-signal input, session load, duplicate prevention.
  - DONE (2026-09-02). Live-verified: `attendance_records` already DB-idempotent (unique `session_id,student_id` + upsert), but percentages were computed client-side (spoofable, N+1 queries) and no risk refresh mechanism exists live (`habit_logs` has no attendance type; no risk tables/functions) â€” design doc's "risk refresh via existing functions" is NOT implementable as written; deviation documented: the 75% threshold is computed in SQL instead.
  - Migration `20260902160000_attendance_summary_view_and_record_rpc.sql` (applied via MCP + repo parity mirror): (a) `attendance_summary_v1` view (`security_invoker=true`) â€” per-student per-section per-course counts, `attendance_pct = round(((present+late)/total)*100)` per Req 78.3, NULL when the section has no sessions, `below_threshold` (< 75) as the server-computed risk signal; (b) `record_attendance_v1` RPC â€” idempotent upsert, `marked_by = auth.uid()` server-side (client value no longer trusted), section-ownership enforced via RLS + explicit check, returns recomputed per-student rows.
  - Frontend: `useAttendanceSummary` now reads the view (single bounded query replacing 3-step N+1 client aggregation); `useMarkAttendance` calls the RPC; extracted pure `mapAttendanceSummaryRows` (unit-tested, 4 cases: shape parity, null pct preservation, threshold flag passthrough, empty set). tsc clean; lint 0w; full suite 6,689 green; i18n parity OK.
- [x] T22 (E2.F) Tutor Handoffs: trigger conditions, AI summary, resolve loop, student history.
  - DONE (2026-09-02) â€” consent-flow wiring scope. Live-verified: the full pipeline existed but was severed â€” `chat-with-tutor` detects triggers (low RAG confidence, repeated question) and emits SSE `handoff_suggestion`, but nothing consumed the event and no insert ever reached `teacher_handoff_requests` (RLS verified sound: student-own ALL + teacher read/update gated on consent). `useCreateHandoff` had zero callers (dead code).
  - Frontend completion: (a) `useSendMessage` now passes `onHandoffSuggestion` through to `tutorApi.sendMessage`'s existing SSE callback; (b) new `useHandoffContext(courseId)` resolves teacher_id/institution_id from `courses.teacher_id` + `programs.institution_id` (live-verified columns) with 5-min cache; (c) new `useCreateHandoffWithNotification` wraps `useCreateHandoff` and inserts a notification row for the target teacher (mirrors the `useDeadlineExtensions` insert pattern â€” legacy notifications table, eslint-disabled precedent); (d) TutorPage: on `handoff_suggestion` SSE event opens a consent dialog (summary + reason from the suggestion payload, Checkbox for consent, explicit Confirm/Cancel â€” approval is ALWAYS required per intelligence guardrails), on confirm calls `createHandoff` with trigger_reason/ai_summary/consent + success toast.
  - i18n: `ai.tutor.handoff.*` keys added (en + ar, parity OK). Test: `tutorPageAutonomy.test.tsx` updated (useAuth + handoff-hook mocks for the new page wiring). Gates: lint 0w; tsc 0; full suite 6,689 green; i18n parity OK. Deployment Impact: NONE (frontend-only).
- [x] T23 (E2.G) Course Materials — VERIFIED (built previously, spec not updated): course context via module_id→course (useCourseModules/ModuleManager); tutor knowledge isolation proven by ragAuthorizationContracts + tutorPromptAssembly + RLS smoke suites (32/32 green); studio reuse via courseMaterialIndexing; publish control via is_published Switch in ModuleManager. Live schema verified (is_published, search_vector, clo_ids).
- [x] T24 (E2.H) Rubrics — VERIFIED (built previously): CLO flow (rubrics.clo_id FK -> learning_outcomes, course-scoped lists, embedded titles); grading-standard linkage (GradingInterface consumes criteria/levels, T17); auto assignment linkage (assignments.rubric_id join, live-verified); versioning = clone-with-audit lineage (`copied_from` in audit_logs + is_template; NO numeric version columns — deeper version history recorded as deferred for T31).

## Phase 5 â€” Coordinator sync (team feedback)
- [ ] T25 (E3.F+A) Outcome Attainment v2 UI + full drill-down + explain-percentage + next actions.
- [ ] T26 (E3.G+B) Curriculum Matrix v2 UI + evidence-generated cells + gap workflows + auto-refresh.
- [ ] T27 (E3.C) CQI evidence-origination + traceability + auto-evaluate + accreditation feed.
- [ ] T28 (E3.D) Course File Generator auto-readiness + deep links + accreditation feed.
- [ ] T29 (E3.E+H) Accreditation Evidence dynamic generation + functional Review/Start/Export + UI v2.
- [ ] T30 (E3.I) Me page repositioning (identity, AI prefs, permissions, programs, faculty, notifications, security, integrations, academic info).
- [ ] T31 (E3.J) Deferred items recorded as spec-only (no build).

## Phase 6 â€” Student & visual (team feedback)
- [ ] T32 (E4.A) Learning Path climbing-camps visual (fallback: tone progression).
- [ ] T33 (E4.B) Student course section polish + View action.
- [ ] T34 (E4.C) Journal companion experience.
- [ ] T35 (E4.D) Teacher dashboard polish (action plan, triage groups, batch approval, quick actions).
- [ ] T36 (E4.E) Coordinator hero action hub + enriched alerts + timelines.

## Verification
Every completed task appends evidence (test run, live-log check, PR link) to this file. Gates per task: lint / tsc / vitest / i18n:check / check:edge-imports / db:check-replay / check:runtime-dependencies.
