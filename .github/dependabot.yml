version: 2

updates:
  # npm dependencies — security updates only
  # Setting open-pull-requests-limit to 0 disables version update PRs.
  # Dependabot security updates are a separate feature and still create PRs
  # automatically when vulnerabilities are detected.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 0
    rebase-strategy: auto
    labels:
      - dependencies
      - security
    commit-message:
      prefix: "chore(deps):"
    # Ignore major bumps for core tooling — these need manual migration
    ignore:
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]
      - dependency-name: eslint
        update-types: ["version-update:semver-major"]
      - dependency-name: vitest
        update-types: ["version-update:semver-major"]
      - dependency-name: react
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/react"
        update-types: ["version-update:semver-major"]
      - dependency-name: react-dom
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/react-dom"
        update-types: ["version-update:semver-major"]
      - dependency-name: react-router-dom
        update-types: ["version-update:semver-major"]
      - dependency-name: "@tanstack/react-query"
        update-types: ["version-update:semver-major"]
      - dependency-name: tailwindcss
        update-types: ["version-update:semver-major"]
      - dependency-name: vite
        update-types: ["version-update:semver-major"]

  # GitHub Actions
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    rebase-strategy: auto
    labels:
      - dependencies
      - ci
    commit-message:
      prefix: "chore(ci):"
    groups:
      # Group all action bumps into one PR
      github-actions:
        patterns:
          - "*"
