// =============================================================================
// XPHistory — production XP history page (redesigned UI)
// =============================================================================
// Implementation lives in `XPHistoryNew`, re-exported here so the route path
// stays stable. Legacy page and its feature-flag wrapper were removed in the
// UI-migration cleanup.
// =============================================================================

export { default } from "./XPHistoryNew";
