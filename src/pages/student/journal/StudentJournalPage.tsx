// =============================================================================
// StudentJournalPage — production journal page (redesigned UI)
// =============================================================================
// Implementation lives in `StudentJournalNew`, re-exported here so the route
// path stays stable. Legacy page and its feature-flag wrapper were removed in
// the UI-migration cleanup.
// =============================================================================

export { default } from "./StudentJournalNew";
