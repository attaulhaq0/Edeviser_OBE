// =============================================================================
// TeacherDashboard — production teacher dashboard (redesigned UI)
// =============================================================================
// Implementation lives in `TeacherDashboardNew`, re-exported here so the route
// path (`@/pages/teacher/TeacherDashboard`) stays stable. Legacy dashboard and
// its feature-flag wrapper were removed in the UI-migration cleanup.
// =============================================================================

export { default } from "@/components/shared/TeacherDashboardNew";
