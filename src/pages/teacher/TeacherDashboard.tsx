// =============================================================================
// TeacherDashboard — production teacher dashboard (prototype rebuild)
// =============================================================================
// Implementation lives in the prototype-rebuilt `TeacherDashboardScreen`
// (src/features/teacher/dashboard), re-exported here so the route path
// (`@/pages/teacher/TeacherDashboard`) and its guard stay stable. The legacy
// `TeacherDashboardNew` reskin was replaced in the prototype-frontend-rebuild
// (P2.2) cutover.
// =============================================================================

export { default } from "@/features/teacher/dashboard/TeacherDashboardScreen";
