// =============================================================================
// AdminDashboard — production admin dashboard (redesigned UI)
// =============================================================================
// Implementation lives in `AdminDashboardNew`, re-exported here so the route
// path (`@/pages/admin/AdminDashboard`) stays stable. Legacy dashboard and its
// feature-flag wrapper were removed in the UI-migration cleanup.
// =============================================================================

export { default } from "@/components/shared/AdminDashboardNew";
