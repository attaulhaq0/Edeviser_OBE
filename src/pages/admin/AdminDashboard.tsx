// =============================================================================
// AdminDashboard — production admin dashboard (prototype rebuild)
// =============================================================================
// Implementation lives in the prototype-rebuilt `AdminDashboardScreen`
// (src/features/admin/dashboard), re-exported here so the route path
// (`@/pages/admin/AdminDashboard`) and its guard stay stable. The legacy
// `AdminDashboardNew` reskin was replaced in the prototype-frontend-rebuild
// (P2.5) cutover.
// =============================================================================

export { default } from "@/features/admin/dashboard/AdminDashboardScreen";
