/**
 * L3 composition — Student.
 *
 * Role pages, widgets, and flows composed from `@/design-system` primitives +
 * patterns and bound to EXISTING hooks in `src/hooks` (no new backend). Student
 * treatment is the most gamified (rings, gradient bars, mascot, XP/streak).
 *
 * Populated across P2 (dashboard) and P3 (all student modules) per
 * `missing-screens-catalog.md`. Empty until then (task 0.8).
 */
export {};
