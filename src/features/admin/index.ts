/**
 * L3 composition — Admin.
 *
 * Role pages, widgets, and flows composed from `@/design-system` primitives +
 * patterns and bound to EXISTING hooks in `src/hooks` (no new backend). Staff
 * treatment: professional/restrained.
 *
 * Populated across P2 (dashboard) and P3 (users/import/invite, structure, ILOs,
 * reports, audit, marketplace mgmt, badges, fees, settings) + P3.6 net-new Admin
 * Security console, per `missing-screens-catalog.md`. Empty until then (task 0.8).
 */
export {};
