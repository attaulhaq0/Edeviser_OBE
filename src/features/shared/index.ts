/**
 * L3 composition — cross-cutting / role-aware screens.
 *
 * Screens that exist for multiple roles and are parameterized by role rather than
 * duplicated: announcements, notifications feed, discussions, surveys, fees.
 * Composed from `@/design-system` primitives + patterns and bound to EXISTING
 * hooks (no new backend).
 *
 * Corresponds to the `shared-*` ids in `visual/screen-map.ts`. Populated in P3.
 * Empty until then (task 0.8).
 */
export {};
