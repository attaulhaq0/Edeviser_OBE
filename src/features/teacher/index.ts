/**
 * L3 composition — Teacher.
 *
 * Role pages, widgets, and flows composed from `@/design-system` primitives +
 * patterns and bound to EXISTING hooks in `src/hooks` (no new backend). Staff
 * treatment: professional/restrained (no gamified chrome).
 *
 * Populated across P2 (dashboard) and P3 (CLOs, rubrics, grading, gradebook,
 * quiz studio, attendance, teams, etc.) per `missing-screens-catalog.md`.
 * Empty until then (task 0.8).
 */
export {};
