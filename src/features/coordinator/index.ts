/**
 * L3 composition — Coordinator.
 *
 * Role pages, widgets, and flows composed from `@/design-system` primitives +
 * patterns and bound to EXISTING hooks in `src/hooks` (no new backend). Staff
 * treatment: professional/restrained.
 *
 * Populated across P2 (dashboard) and P3 (PLOs, curriculum matrix, outcome chain,
 * CQI, course-file, competencies, teams) per `missing-screens-catalog.md`.
 * Empty until then (task 0.8).
 */
export {};
